interface:
  display_name: "主任务精简"
  short_description: "精简主任务步骤、验证和维护面，按需进行显式消融。"
  default_prompt: "非平凡主任务触发 $lean-simplify：只读取并应用当前实际动作命中的入口；已读且未变化则复用，不预读未触发的 $lean-stack 或完整 references。读取后下一次工具调用立即开始实际任务，独立收窄到产生首个可验证结果的最短完整路径；其他技能用途说明合并到唯一启动说明，不得等待或阻断 $lean-stack。"
