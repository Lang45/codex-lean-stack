interface:
  display_name: "Lean Stack"
  short_description: "Fast verified work with critical-path subagents."
  default_prompt: "Use $lean-stack; keep the parent moving while subagents run, prefer bounded-cost speed, and run only the essential checks."
