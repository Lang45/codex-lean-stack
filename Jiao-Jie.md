# Codex Lean Stack 项目交接

## 根本准则

主任务优先。代理管理、经验、成本维护、语义审查、绘图、版本和交接记录都只能服务主任务，
不能反过来拖延主任务结束。

三项原则：

1. 高价值工作质量优先，同时守住正确性和决定性证据；可靠性相当时再比较完成时间。
2. 普通工作达到质量底线后速度优先。
3. 没有相应质量或速度收益时，调用子代理不能使总成本大幅增加；结果相当时选择总成本更低
   的路线。

安全、权限、数据完整性、明确验收条件和诚实证据始终是底线；委派不增加权限。

## 项目级父代理跨会话记忆

- 本文件是 `codex-lean-stack` 项目级、显式、可审计的父代理跨会话记忆；不是模型隐藏记忆，
  也不是 SQLite 中的子代理经验。
- 父代理只在新会话开始或上下文压缩后亲自完整读取本文件一次；普通后续轮次复用已经装入
  当前上下文的内容，不重复全文读取。子代理摘要或子代理读取不能替代这两处父代理读取。
- 每轮项目迭代改变了稳定规则、已采纳决定、版本、验证证据、运行状态或剩余边界时，父代理
  在收口前更新本文件；不保存思维过程、凭据、临时日志或无关历史。
- 同项目的新父代理由当前插件技能中的入口规则找到并读取本文件。SQLite 继续只保存保留
  子代理身份、经验和经验证的存活轮次；不另建重复的父代理数据库、后台队列或状态机。

## 用户全局文件边界

- `C:\Users\fml\.codex\AGENTS.md` 是用户所有文件。
- 当前文件包含用户已批准的通用项目交接规则、主任务连续性与最终说明、中文术语、权威路径与
  代码边界、外部影响授权、全局验证效率和一行默认插件调用指令。
- 未经用户当次明确同意，任何代理和本插件都不得修改该文件。用户明确授权时，安装辅助器
  也只能幂等确保这一行，不能复制插件的子代理加速规则或改写其他内容：
  默认调用已安装的 `codex-lean-stack` 插件；是否启动子代理仍由插件自身规则决定。
- 用户另行明确授权写入的通用规则不扩大安装辅助器的写入范围；安装辅助器仍只能确保上一行。
- 子代理加速规则仍由 `skills/lean-stack/SKILL.md` 承载，不复制到 `AGENTS.md`。
- 提交、推送、公开发布、部署、外部消息、打开 Codex 面板和重启 ChatGPT 都需要用户另行
  明确授权。

## 主任务链连续性与最终说明

- 主任务链锚点是上一次已经明确确认完成的主任务之后，用户提出的第一个要求。
- 主任务未完整交付并被确认完成时，后续纠错、方向或范围调整、优先级变化、格式要求和补充
  验收默认追加到同一主任务清单，不替换此前要求。
- 用户明确取消、停止、放弃或替换前序任务时，才重置锚点。
- 中途调整处理完成后，父代理自动恢复并继续此前其余要求；不得只回答最新调整就发送最终
  交付。
- 最终说明从锚点逐项覆盖到最新消息，包括修改、验证证据和剩余边界。
  不能只总结最后一条消息。
- 这只是会话内轻量清单，不增加数据库、后台队列或持久状态机。

## 任务类型与任务类型组

完成适用的会话入口读取后，父代理读取当前 `SKILL.md`、工作树和与具体任务直接相关的参考
文件，并保留所有现有修改。

父代理按以下顺序推进：

```text
收到任务
→ 判定当前工作的具体任务类型
→ 确定性工具可直接完成：走工具路线，不建立任务类型组、不定制子代理
→ 需要持续模型判断：完成一次调用判断
→ 判断具体任务类型是否适合已有任务类型组
  → 适合：确定复用该组
  → 不适合：先建立新的运行时任务类型组
→ 任务类型组确定后，才检查可见保留子代理
  → 有：复用保留子代理
  → 无：父代理定制运行时新子代理
```

只有具体任务类型和任务类型组都确定后，父代理才能复用或定制子代理；此前不得选择模型、
思考程度、速度或权限配置。任务类型组组织已经决定调用的运行时工作，不是调用门槛；任务
类型是否稳定也不是当前调用前置条件。

当前任务先由运行时组完成。真实结果通过必要核验并被采用后，父代理才判断职责是否能去除
来源项目身份，形成跨任务、跨项目、跨会话复用的全局领域合同。只有全局领域保留，没有项目
保留层；QML、媒体、插件、检索、发布或交接等不同领域可以各有专门角色，不要求每名角色处理
所有项目类型。

## 主任务链与辅助链入口

主任务链是唯一主线。辅助链只能从以下位置开始，并在完成或跳过后返回主任务：

- 工作价值和任务类型确定后：调查或实现。
- 判断工具或模型路线时：一次调用、容量、成本和安全并行。
- 一次调用判断通过、任务类型组确定后：保留子代理复用或运行时定制；需要写入时才进入
  写入保护。
- 子代理交付可采用结果、父代理仍要测试或集成且有空闲容量时：并行启动有限、只读的可选
  保留与经验准备；父代理不等待，准备不是正式提交前置条件。
- 产生修改后：最窄迭代测试。
- 代码、配置、公共行为或高风险边界变化时：条件性语义复核与最终验证。
- 验证失败时：失败后的最窄重验。
- 当前任务类型组的已就绪结果完成核验并被采用时：复制或变体竞争、默认保留、默认经验提交、
  必要的 SQLite 检查和组收口。
- 当前工作属于插件发布时：版本、校验和本地重装。
- 固定成本检查只有到期且能非阻塞并行时才开始。

任何辅助链都不得另立主线、绕过完成条件或在主任务完成后继续拖延交付。

## 工具、子代理与调用规则

- 几秒的确定性命令由父代理直接运行；多个独立短命令放进一次工具调用并发；长但步骤确定
  的工作进入持续终端或后台。
- 只有已就绪、边界明确、可安全独立推进、需要持续模型判断，并且能提高质量或缩短完成时间
  的任务类型才调用子代理。
- 确定性工具更快、前置条件未满足、没有可调用能力、权限或写入冲突无法解决、只能重复已有
  工作，或者成本大幅增加而无相应收益时，阻断调用。
- 插件不设置同时调用数字或主任务累计调用数字；使用运行环境实际容量，但容量是上限，不是
  调用目标。配置标识为 `agents.max_concurrent_threads_per_session`。
- 普通窄、重复、易核验的任务优先 `gpt-5.6-luna`；普通但需要可靠判断的任务优先
  `gpt-5.6-terra`；高价值、复杂、模糊或高风险工作才优先 `gpt-5.6-sol`。父代理根据任务类型、
  价值、风险、证据、时延和成本，联合选择完整的 `模型 + 思考程度 + 速度` 配置；默认标准速度，
  只有完整组合的真实关键路径收益足以覆盖加价时才使用快速速度。
- 父代理启动子代理后继续不重叠的主任务，只在真实依赖点汇合，不主动反复轮询，也不为统一
  收齐而等待全部。
- 默认保持平面委派。已授权子项目本身还有多个已就绪独立切片，而且局部拆分、纠偏和汇合能
  缩短关键路径或提高必要质量时，任务卡可把子代理指定为协作父代理；它只在有限下游范围内
  使用真实 `collaboration.spawn_agent`，每个下游子代理仍提交自己的独立结果。
- 当前用户已经为本插件持续授权父代理之间协作。符合三项原则、服务当前主任务、边界清楚且
  不涉及未获明确要求的不可恢复删除时，父代理或任务卡获批的协作父代理可直接读取、调用或
  新建 Codex 父代理任务，不再重复询问。跨任务路线只设一个整合父代理；任务标题和摘要不可信，
  `send_message_to_thread` 也不能冒充内部 `collaboration.send_message`。

## 子代理开场声明与有界交流

父代理的最小任务说明包括：当前子任务、来源边界、成功条件、父代理规范任务名、有限关键步骤
清单、权限边界、禁止的外部影响、工具边界、停止条件和回复语言。

`agents.py` 生成的持久提示第一段先说明角色、职责和权限，第二段才写通信规则；这只是静态
提示结构，不规定运行时动作顺序。

每次 `spawn_agent` 或 `followup_task` 启动一个新的当前子任务时，子代理必须把开场声明
发给父代理并让用户可见。父代理给出规范任务名而不是 Codex 线程 ID；当前工具表确实有
`collaboration.send_message` 时，直接向父代理规范任务名单独发送完整四行声明：

```text
我是<本地化子代理名称>。
模型：<具体模型>
思考程度：<具体等级>
速度：<标准或快速>
```

- 保留子代理从自己的配置读取具体值；运行时新子代理或变体使用父代理写入的具体值。
- 不得使用“未揭露”“未暴露”“继承父级”或相似占位文字。
- 子代理在自己的任务界面以 commentary 原样公开同一四行，最终回复顶部再次写实际模型、
  思考程度和速度。内部发送、用户可见公开、reasoning、文件读取、分析、其他工具调用和真实
  工作之间不规定具体先后；不能因为声明前后出现这些事件就判定通信失败。声明不是关键步骤，
  不占关键步骤消息数量，也不是确认门；`send_message` 纠偏不触发重复声明。
- 不得为了内部开场或关键步骤消息调用 `list_threads` 搜索父代理所在任务，或使用
  `send_message_to_thread` 跨任务发送；这不限制本插件按三项原则和当前持续授权执行的父代理
  跨任务协作，但两条消息路线不能互相冒充。内部消息
  工具不可用时，子代理仍在自己的任务界面输出声明并在最终回复报告能力缺口；是否继续按下条
  真实能力规则决定，不能改走跨任务消息。
- 父代理不为声明轮询或等待；只核对内部消息真实到达、用户可见 commentary 包含实际配置、
  最终回复保留配置抬头。任一缺失时只做一次有界纠偏并诚实记录缺口。
  已经能独立核验的正确结果不自动丢弃，但该次运行不能作为“开场声明已验证”的证据。
- 完全没有工作前可见消息能力时，报告真实能力缺口，不能宣称功能可用。
- 父代理自己的工具表不等于子代理工具表；只有实际收到当前子代理的内部 `agent_message`，
  才能确认这条运行路线内部交流可用。中途纠偏是成功条件时，把内部工具不可用列为停止条件
  并换用已验证路线；只有完全自包含、无需中途纠偏的有限任务才允许在该缺口下继续。自身
  线程 commentary 和最终回复都不能冒充内部父子消息；新任务或新生成路线重新核验能力。
- 权限只有在具体任务本身需要限制，或用户明确要求时，才单独核对。
- `spawn_agent` 没有逐个工具授予参数，内部消息也不是可臆造的 MCP 工具。自定义角色 TOML
  只能作为子线程配置层，不能替父会话打开多代理，也不能改写模型目录能力。父代理在新会话
  第一次启动 Luna 子代理前，确认 `agents.enabled` 没有关闭，并确认运行环境实际加载的
  `gpt-5.6-luna` 模型目录为 `multi_agent_version=v2`；只打开会话 v2 开关不足以给目录仍为 v1
  的 Luna 子线程注册协作工具。窄保留角色使用 `[skills] include_instructions = false`，不再伪称
  `[features]` 或 `[agents]` 字段可以强制授予工具。顶层 `collaboration.send_message` 故意不在
  `functions.exec` 的 `ALL_TOOLS` 中，子代理必须直接调用；目录不是 v2 或直接调用失败时停止交流
  依赖型子任务并报告能力缺口，不能提高模型、思考程度、速度或成本，也不能跨任务冒充。

关键步骤只指会产生独立可核验结果或改变后续路线的有限阶段。
每个关键步骤最多一条常规进度；子代理简短概述后立即继续，不等待父代理，不发送定时心跳
或纯确认消息。同一方向风险只有状态实质变化后才能再次报告。

父代理无异议时沉默或只回一行；发现方向、范围、证据、权限或目标问题时立即纠偏。纠偏不得
扩大用户授权、增加外部副作用、移除停止条件或把有限任务扩成无限任务。

达到成功条件或停止条件后，每个子代理在自己的线程用最终回复提交自己的精炼结果。开场
声明的内部副本与用户可见副本、关键步骤消息和带配置抬头的最终回复相互独立；成功路线两份
开场声明缺一不可，用户可见副本不能冒充内部父子消息，但不校验它们与其他运行事件的具体
顺序。不建立共享中转文件，不由一个子代理代交、等待或汇总其他子代理。

详细规则见 `skills/lean-stack/references/delegation.md`。

## 任务类型组、复制、变体与收口

- 同一种活就是同一任务类型；只有核心职责、主要来源或工具、证据形状都相同才归入同一组，
  决定性边界不同且能独立复用就建立不同组。成功条件不参与任务类型归类，由父代理为每个
  子任务分别指定。
- 任务类型组确定后，有可见保留子代理就复用；没有时才定制运行时新子代理。复用保留子代理
  时，由它读取自己的经验和具体配置，不要求父代理强制重写已有配置。运行时定制不是持久
  创建，不调用 `ensure` 等待配置可见。需要中途交流时先确认实际 Luna 模型目录为 v2；目录
  为 v1 或直接内部调用失败就停止该子任务，不改用跨任务 API 或更高成本模型。
- 复制的是任务类型组里的子代理，只处理第二个及后续同类型子任务；父代理分别指定输入、
  成功条件和权限。
- 变体只在复制前确认现有配置确有进步空间时出现。父代理根据任务类型、价值、风险、证据、
  时延和成本，联合选择完整的 `模型 + 思考程度 + 速度` 配置；三个字段可以因相互制约而同时
  变化。单轴变化只在明确需要识别因果时作为可选实验，不能取代联合选配。
- 出现复制或变体时，各子任务先分别完成核验和最终回复，再用可比较证据竞争，选出唯一留下
  的子代理。
- 子代理交付可采用结果、父代理仍要测试或集成时，有空闲容量就并行启动有限、只读的经验
  准备子代理，提前泛化角色和整理去敏候选；父代理继续测试，不等待，准备阶段不写 SQLite，
  没有准备或准备未赶上不阻断正式提交。
- 唯一子代理确定、结果通过必要核验并被采用后，先形成领域、输入形状、通用职责、可验证交付
  和硬边界组成的全局领域合同。合同能去除来源项目身份时，第一次成功就默认尝试一次
  `ensure`，再用稳定 `event_id` 默认尝试一次 `improve`，把本组胜出方法和失败避免信息泛化、
  去敏、去重后合并追加成一条经验。每个全局领域任务类型组各保留一个休眠配置，不存在项目
  保留层，也不存在全局只能保留一个。
- 仅结果未通过或未采用、用户否定、无法安全去敏，或者权限、安全、SQLite、文件、身份、
  CAS 使短写不能立即安全完成，或角色仍固化项目、插件、个人技能、路径、版本和一次任务事实
  而无法泛化时跳过；不排队、不轮询、不重试，主任务不依赖持久写入成功。
  最终说明按组给出角色与经验保存回执。只有实际要读写经验时才打开并检查 SQLite。
- 经验维护完成或安全跳过后，其他运行时子代理才移出组并结束。真实结果出来后可以评估角色
  泛化质量和后续复用效果，但这不构成核验采用结果默认 `ensure`/`improve` 的前置条件。运行
  线程照常进入 Done；跨任务保留不让它无限运行。
- 每次启动正式保留身份前，父代理生成 UUID `run_id` 并写入任务说明。只有达到本次成功条件、
  结果经核验采用且线程进入 Done 后，才用 `record-run` 幂等增加一次“存活轮次”；失败、停止、
  用户否定、未采用或仍在运行都不计。v2 以前没有可靠运行 ID，迁移后历史轮次不猜测回填。
- TOML 重配只影响以后新生成的代理线程；重配前已经创建的运行中或已完成线程继续使用原加载
  提示。不得用 `followup_task` 激活旧线程来声称新通信规则生效；旧线程出现跨任务消息时只做
  一次内部纠偏，真实验证必须使用新任务中新生成的线程。

## 写入安全与用户不满意

这不是单写入者规则。

- 写入范围天然分开时，不同子代理可以在同一检出目录并行修改不重叠范围。
- 公共接口、清单、锁文件、数据库结构和生成入口等共同热点最后集中整合一次。
- 同一物理文件没有独立检出目录时，只允许一个候选直接写目标，其他候选返回方案、补丁或
  证据。
- 用户明确不满意某个子代理时，立即停止并移出组；其结果、证据、补丁和经验候选失效。
- 共享目录只撤销能用写入范围、调用前基线和当前哈希精确归因的修改；禁止整树回退覆盖用户
  或其他代理的工作。
- 用户不满意本身不能放宽候选退役资格；插件所有权、零原始经验与存活轮次、单一硬链接、
  所有权令牌和当前哈希仍须全部匹配。
- 最近的协作授权不改变原有删除、删减或候选清理的资格与尺度；只改变去向。普通文件进入
  Windows 回收站，重要项目文件进入任务专属 `待删文件`。插件角色仍按原来的经验、存活轮次、
  身份、令牌、哈希和单一硬链接资格判断，合格后从活跃台账退役并把 TOML 与可验证收据移入
  `<CODEX_HOME>/lean-stack/待删文件`，不再永久 `unlink`。

## 反 AI 过度工程

- 详细权威规则是 `skills/lean-stack/references/anti-overengineering.md`；其他文档、UI、流程图和
  交接只保留各自受众需要的入口或最小摘要，不再把同一规则手工维护成第二事实源。
- 向后兼容只服务已安装/已发布版本、真实调用方、公共约定、用户数据或持久状态等现实消费者。
  尚未部署的新内部功能直接替换，不预造 legacy reader、双写、永久别名或回退分支。
- 已经决定不实现的假想功能默认不留分支、桩、TODO、解释其缺席的注释或测试。只有用户明确
  禁止、安全/权限/数据完整性、公共约定或真实回归四类负向边界才保留最小测试。
- 跨版本回迁、机制替换或大范围删改先列当前用户原话、当前事实和明确保留项；助手推断的图数、
  拓扑、固定流程或版本形态没有来源证据时，只交差异清单，不执行回迁。
- 政策、术语或流程变化先区分权威行为面、直接说明面、可再生展示面和发布/缓存面；只更新不改
  就会作出错误承诺的表面。交接不手写可由 SQLite、manifest、测试或文件清单低成本推导的
  细粒度计数、全量哈希和逐项验证清单。
- 哈希只服务所有权、CAS、SQLite/TOML 迁移、退役/恢复、真实产物和最终安装一致性。同一输入
  未变化时复用已有摘要，不以重复全树散列制造“全面”感，也不省略保护用户数据的必要 CAS。

## 条件性验证

- 迭代阶段先运行最窄、最相关的检查；相关边界未变化时复用已有通过证据。
- 代码、配置、公共行为或高风险边界变化时才做会推动修改的语义复核。
- 最终只运行当前改动和风险确实需要的检查；多个确定性短检查由父代理工具级并发。
- 失败后只修复真实原因，并重跑失败项和受影响项。
- 静态校验、安装成功和子代理自报都不能替代真实运行时证据。
- Windows 上所有 Python 脚本和校验器统一使用 `py -3 -X utf8`；Python 包装子进程时设置
  `PYTHONUTF8=1`，不能依赖 GBK 默认编码读取 UTF-8 `SKILL.md`。

## 经验、SQLite、成本与生命周期侧链

SQLite v4 经验体系只保留：

1. SQLite 台账。
2. 每个通过合同检查且未命中明确排除项的全局领域任务类型组一个保留子代理；多个领域可同时
   保留，不存在项目保留层。
3. UUID 幂等追加的成功存活轮次。
4. 只追加原始经验和纠正事件。
5. 经验数量不封顶。
6. 经验摘要压缩。

复制和变体本身不写入 SQLite。经验维护窗口中，必要时先核对受支持结构，再安全重配同一
保留子代理或只追加经验；未知结构、忙锁、损坏或漂移立即跳过，不阻塞主任务。被否定经验
通过追加纠正事件退出活跃记忆，原事件保留审计。经验完成或跳过后才移除其他子代理。

普通命令只接受当前 v4。现实存在且结构精确匹配的 v1/v2/v3 台账只能显式执行
`agents.py migrate-global --plan <JSON>`；迁移计划必须覆盖全部角色和真实来源词。来源词只用于
阻止项目化语义进入长期合同或经验，不写入 SQLite、TOML、合同或完成收据。迁移保留同一身份、
令牌、创建时间、运行记录和原始经验；旧重要 TOML 进入插件专属 `待删文件`，不物理销毁。

插件运行时使用已经维护的固定成本预估，不在每个任务重新搜索费率、计算令牌或制作评分表。
每周检查不到期不联网；到期但不能非阻塞核对时延后。插件不创建自动化、系统定时任务或外部
守护进程。

生命周期不是固定顺序、固定数量或后台状态机。`status`、`ensure`、`record-run`、`improve`、
`delete` 和 `restore` 是普通按需能力；`migrate-global` 只用于显式现实迁移。`delete` 保持原来的
严格身份与删减资格，但处置改为可恢复退役；已有经验或存活轮次的代理不能由普通删除命令
连带清空审计数据。

## 统一术语

| 英文标识 | 中文用词 |
| --- | --- |
| parent agent | 父代理 |
| subagent | 子代理 |
| main task | 主任务 |
| instance | 子任务 |
| task type | 任务类型 |
| task type group | 任务类型组 |
| retained subagent | 保留子代理 |
| copy | 复制 |
| variant | 变体 |
| experience | 经验 |
| runtime environment | 运行环境 |

英文 `instance` 在中文正文中只翻译为“子任务”。代码标识、模型名、命令、路径和原始错误
使用代码格式。

## 权威路径与代码边界

- 可编辑源码根目录：`C:\Users\fml\plugins\codex-lean-stack`。
- 主技能：`C:\Users\fml\plugins\codex-lean-stack\skills\lean-stack\SKILL.md`。
- 插件清单：`C:\Users\fml\plugins\codex-lean-stack\.codex-plugin\plugin.json`。
- 个人市场：`C:\Users\fml\.agents\plugins\marketplace.json`，市场名为 `personal`。
- SQLite 台账：`C:\Users\fml\.codex\lean-stack\specialist-memory-v1.sqlite3`。
- 全局领域台账实现：`skills/lean-stack/scripts/agents.py`。
- 反 AI 过度工程权威规则：`skills/lean-stack/references/anti-overengineering.md`。
- 13 条 Mermaid 权威规则内容源：`skills/lean-stack/references/flowcharts-zh.md`。
- 静态 SVG/HTML 由独立插件 `C:\Users\fml\plugins\architecture-viewer` 的
  `$architecture-viewer` 负责；原生交互式 Viewer 由同一插件的 `$archify` 负责。本插件不保存
  第二套 HTML/SVG、渲染器、Viewer Runtime 或 loopback 预览服务。
- 显式安装入口：`skills/lean-stack/scripts/install_plugin.py`；正式安装成功后才幂等确保用户
  已授权的一行全局默认调用指令。它先预检 `AGENTS.md`，并在同一把文件锁内完成安装和
  成功后的单行写入；失败或未授权时不触碰该用户文件。

源码是唯一编辑入口；安装缓存只用于核对，不作为日常修改目标。链路图只是辅助说明，代码、
技能约定、约定一致性测试和真实运行证据才是验收依据。

---

以下部分是每轮都会变化的状态，统一放在恒定规范之后。

## 运行环境状态快照

快照日期：`2026-09-01`。事实来源是当前源码、`agents.py status`、SQLite 只读检查和本轮
实际迁移收据；历史任务只提供背景，不能覆盖当前实物。

- 当前源码与已安装版本均为 `2.15.0+codex.20260831175832`；本轮只运行了一次功能版本写入器
  和一次普通 `codex plugin add`。`.codex-plugin/plugin.json` 是唯一数字版本源。
- SQLite 已是 schema v4：17 个正式角色全部为 `codex-global-domain-v1`，legacy 为 0，
  根目录 `lean_*.toml` 共 17 个且未登记文件为 0。普通命令拒绝静默解释旧 schema。
- 正式迁移保持原 `agent_id`、所有权令牌、创建时间、存活记录和原始经验；项目化旧经验只用
  追加纠正退出活跃提示，原审计记录未删除。旧重要 TOML 已进入插件专属
  `待删文件/全局领域迁移备份`。
- `C:\Users\fml\.codex\AGENTS.md` 本轮未修改。用户现已明确授权把当前插件与双语 README
  提交并推送到本仓库；该授权不包含创建 GitHub Release、部署、打开 Codex 面板或重启
  ChatGPT/Codex。
- `codex plugin list --json` 确认插件已安装、已启用且来源是当前源码根。安装缓存仍只用于核对，
  不是编辑目标；静态安装成功不冒充以后新任务的真实运行时加载。

### 17 名全局领域保留子代理

下表的任务类型是通俗中文领域职责，不是产生角色的项目专属任务。代码名称保留历史标识，
当前可复用边界以 SQLite/TOML 中的全局领域合同为准。配置按任务类型、价值、风险、证据形状、
时延和总成本联合选择完整的 `模型 + 思考程度 + 速度`，不按单列分布重新均衡。

| 名称 | 通用任务类型 | 联合配置（模型 / 思考程度 / 速度） | 权限 |
| --- | --- | --- | --- |
| `lean_bounded_project_context_reading_343e684b` | 有界上下文与来源边界读取 | Terra / medium / 标准 | 只读 |
| `lean_collaboration_safety_pattern_analysis_076a861b` | 多代理协作安全模式分析 | Terra / medium / 标准 | 只读 |
| `lean_frontend_skill_forward_testing_b71a63a1` | 前端技能前向测试 | Terra / high / 标准 | 写入 |
| `lean_media_direct_hevc_export_architecture_audit_5b86071e` | 媒体直接 HEVC 导出架构审计 | Terra / medium / 标准 | 只读 |
| `lean_parent_child_message_runtime_validation_610d2310` | 父子消息运行验证 | Terra / medium / 标准 | 只读 |
| `lean_plugin_capability_surface_audit_a270f4a0` | 插件能力表面审计 | Terra / medium / 标准 | 只读 |
| `lean_plugin_contract_consistency_audit_7999edf6` | 插件合同一致性审核 | Luna / medium / 标准 | 只读 |
| `lean_plugin_observable_regression_reproduction_96fd8246` | 插件可观察回退复现 | Luna / medium / 标准 | 只读 |
| `lean_plugin_runtime_state_audit_780a80cc` | 插件运行状态审核 | Luna / medium / 标准 | 只读 |
| `lean_plugin_source_cache_migration_audit_fe4c7890` | 插件源码缓存迁移审计 | Terra / medium / 标准 | 只读 |
| `lean_portable_release_consistency_audit_4b4ee4cb` | 便携发布一致性审计 | Terra / medium / 标准 | 只读 |
| `lean_project_handoff_semantic_audit_2b5208b7` | 项目交接语义复核 | Terra / medium / 标准 | 只读 |
| `lean_retrieval_content_safety_semantic_review_57338800` | 检索内容安全语义复核 | Terra / high / 标准 | 只读 |
| `lean_specialist_lifecycle_safety_implementation_f19dc96e` | 保留代理生命周期安全实现 | Sol / high / 标准 | 写入 |
| `lean_specialist_persistence_implementation_audit_fe71cdbc` | 保留代理持久化审计 | Terra / medium / 标准 | 只读 |
| `lean_subagent_retention_history_audit_6c7b53b3` | 子代理保存率历史审计 | Terra / medium / 标准 | 只读 |
| `lean_web_evidence_extraction_implementation_62182694` | 网络证据提取实现 | Sol / high / 标准 | 写入 |

## 本轮实际变化

- `agents.py` 与定向测试已实现 schema v4、全局领域合同、来源词去项目化检查、显式
  `migrate-global`、单事务迁移、迁移 journal/CAS、可恢复旧 TOML 归档和幂等完成收据。
  没有项目保留层、自动合并、双写、后台迁移或假 fallback。
- 17 名正式角色已完成同身份迁移。角色可以分别专精插件、QML、媒体、发布、检索或交接等领域；
  “整个 Codex 通用”表示同一领域可跨任务、跨项目、跨会话复用，不表示每名角色处理所有领域。
- 实际读取本项目七个开发任务并保留来源覆盖缺口；其中
  `01a042c1-adc9-7f12-8c25-e4357d746d5b` 的更早分页未无界读完。会话证据支持新增两条
  止损规则：跨版本回迁前的来源证据闸门；一个权威源与四类变更表面。2.13 全面测试和 2.14
  差异测试没有提供未被现有规则覆盖的新模式，因此没有为了增加功能数量而重复造规则。
- 新增 `references/anti-overengineering.md` 作为详细权威源，统一落实：现实消费者才允许兼容；
  未部署内部功能直接替换；不维护已决定不实现的假想功能；哈希按风险只算必要目标；交接不再
  手写可推导的细粒度计数、全量哈希和逐项验证清单。
- METR 事故材料只贡献可移植协调形状：明确责任人、定向消息、`HOLD`/`VETO`/`STOP`、
  独立结果和受控接管。非授权通信面、沉默即同意、凭据传播、轨迹伪造和不可逆“牺牲实验”
  继续明确禁止。
- Manifest 与 `openai.yaml` 的入口提示已压缩为最小摘要，不再把完整规则复制进 UI。
- 用户否定了上一版偏仓库包装的介绍页。`README.md` 当前标题为“代理调用和精简流程”，
  使用单文件中英文，直接说明工具优先、一次调用判断、联合配置、安全并行、有界交流、协作
  父代理、全局领域保留、无用流程删减和可恢复删除。已移除居中首屏、徽章、作者故事、口号和
  抽象功能表，不再模仿高星仓库外观，也不把完整运行规则复制回首页。

## 本轮验证证据

- 冻结后的唯一完整测试套件共运行 122 项：120 项通过，2 项仅因当前 Windows 账号没有符号
  链接权限而条件跳过，0 失败；其中 schema v4 核心实现为 54/54，文档约定为 44/44。
- 版本写入后的版本/UI 定向检查 2/2 通过；官方 `quick_validate.py` 返回 `Skill is valid!`，
  官方 `validate_plugin.py` 返回插件校验通过。9 个 Python 文件内存编译、13 个 Mermaid 块、
  Markdown 相对链接、隐藏 Unicode 格式字符和 `git diff --check` 均通过。
- 正式迁移完成后，`agents.py status` 报告 17 个全局角色、legacy 0、未登记 TOML 0；
  SQLite `integrity_check`、`quick_check` 和外键检查均通过。
- 迁移前后对不可变身份、历史运行记录和原始经验的核对一致；迁移只追加必要纠正，没有物理
  删除原始审计数据。
- “代理调用和精简流程”介绍页与权威表面拆分后，`tests.test_skill_contract` 45/45 通过；
  详细运行约定继续由 `SKILL.md` 和各 reference 承载，README 专项检查只验证标题、具体功能、
  调用流程、不调用条件、中英文、安装入口、文档链接和篇幅边界，不再强制复制旧首页措辞。
- 安装完成时，源码与 2.15.0 缓存的可部署文件完全一致。随后发生的变化仅限 README、交接和
  对应文档约定测试，不改变已安装运行代码；按反过度工程规则不升版、不重装、不重复全树哈希。
- `agent-reach check-update` 报告当前 v1.5.0 已是最新版本，没有自动更新。

## 工作树与剩余边界

- 当前工作树是整条主任务链的共享修改，必须保留；不得整树回退。
- 正式迁移计划与隔离 dry-run 目录已无覆盖地移入
  `C:\Users\fml\.codex\lean-stack\待删文件\临时迁移验收`，原 Temp 路径不再存在；这些重要
  制品没有永久删除，可按目录内原名称恢复。
- 当前用户已授权把整条 2.15.0 主任务链和本轮双语 README 一并提交、推送到 `origin/main`；
  完成状态以实时 Git 和远端读回为准，本文件不复制提交哈希。该授权不自动延续到未来修改。
- 新安装技能与 TOML 只会被以后新生成的任务/子线程加载；静态校验和安装成功仍不能替代真实
  新任务运行证据。

## 后续代理开始方式

1. 只有新会话开始或上下文压缩后，父代理才亲自完整读取本文件一次；普通后续轮次复用当前
   上下文。
2. 检查用户最新要求、当前工作树、manifest 和 `agents.py status`；当前要求优先，历史待办
   不自动复活。
3. 只有全局领域保留；运行时组先完成当前任务，结果核验采用后才形成合同并尝试
   `ensure`/`improve`。无法去除来源身份时跳过，不建立项目保留层。
4. 需要兼容、回迁、机制替换、大范围删改或发布闭环时，先读
   `skills/lean-stack/references/anti-overengineering.md`，核对现实消费者和当前来源证据。
5. 确定性工作先用工具；需要持续模型判断且有真实质量或速度收益时才启动子代理。模型、思考
   程度和速度按任务类型联合选择。
6. 未经用户当次明确同意，不修改 `C:\Users\fml\.codex\AGENTS.md`，不提交、不推送、不
   公开发布、不打开 Codex 面板，也不重启 ChatGPT/Codex。
7. 普通删除进入 Windows 回收站，重要文件进入任务或插件专属 `待删文件`；最近协作授权不
   改变原有删减资格与尺度。
8. 当前本地版本是 `2.15.0+codex.20260831175832`，已安装并启用；本轮已获一次提交和推送
   授权。下一轮不得把本次授权扩展到新的改动，提交状态始终先查 Git 与远端。
