# Codex Lean Stack 项目交接

## 根本准则

三项原则：

1. 高价值工作质量优先，同时守住正确性和决定性证据；质量不足的低成本或快速路线不合格。
2. 质量达标后比较两条完整路线的资源总成本；墙钟时长不重复计入资源成本。
3. 资源成本没有显著差异或都处于可接受成本带时，再比较关键路径时间。模型价差足够大时，
   增加少量低成本子代理换取时间是可取的；没有相应质量或时间收益时不大幅增费。

### 当前有效要求摘要

以下内容只汇总仍有效且相互不冲突的要求；新要求只在冲突处覆盖旧要求，未冲突的旧要求继续
有效。具体实现细节仍由下方稳定章节和对应源码负责，不在这里重复历次讨论。

#### 目标与范围

- 主任务优先。代理管理、经验、成本维护、语义审查、绘图、版本和交接记录都只能服务主任务，
  不能反过来拖延主任务结束。
- 主目标是在一个插件中并行提供“主任务精简”和“子代理调用”：前者收窄主任务步骤与维护面，
  后者优化子代理的选择、协作、复用与收口；工具与 PowerShell 路径精简是两边共同遵守的执行
  底座。两边分别验收，不能反过来拖延主任务。
- 用户只授权当前明确范围；历史的一次性授权、停止或测试配置不自动扩展到新工作。没有用户明确
  要求时，不把一次失败、阶段性停止或助手判断总结成永久、绝对禁止。
- 不把人类可维护性另立为优化目标，但仍保护正确性、安全、数据完整性、真实消费者和必要验收。
  不为未决定实现的假想功能增加兼容层、优化器、桩或测试。

#### 规则与证据

- 避免规则越写越多却仍不可执行。新增或修改规则时，同时写清实际消费者、触发动作和可核验证据；
  同义规则只保留一个权威来源，其他入口只作必要摘要或链接。
- 用户对 `AGENTS.md` 和本项目 `Jiao-Jie.md` 的修改只约束对应用户文件或当前项目，不因写在这里
  自动成为插件功能。只有用户明确要求、决定或授权，才可在其明确范围内写成绝对语句；助手推断
  可以保留，但不得写成绝对语句，也不得擅自扩充适用范围。若用于插件，还要有插件内真实消费者、
  触发动作和可核验证据，不能仅以它曾出现在 `AGENTS.md` 或本交接为依据。
- 文案、测试断言、子代理自报、静态检查和安装完成各自只证明对应范围，不能冒充真实派发、
  运行、用户可见行为或可采用结果。
- 历史上已完成的大规模机制删减不得因双入口拆分、文档重写或兼容说明而恢复；现有四模型选配、
  运行时复制与变体、现实旧状态迁移、所有权/CAS 和 SQLite/TOML 数据保护不属于被删机制。

#### 两个并行功能

- 工具与 PowerShell 路径精简由两个入口共同遵守：确定性短工作优先直接工具，独立短命令使用
  一次工具调用并发，复杂转义及时转为任务专属脚本，不增加多余包装或通用执行层。
- `$lean-stack` 是“子代理调用”：负责调用判断与模型、成本、时间选配，上下文、来源和结果复用，
  内部交流与子代理结果收口，角色、经验和生命周期表面精简，
  以及迭代测试链。
- `$lean-simplify` 是“主任务精简”：负责最小完整任务方法，主任务状态和沟通收窄，调查、实现
  和任务手册，测试、复核和重验范围，反过度工程与维护面，显式消融，发布、安装和文档维护，
  以及条件性语义复核与最终验证链、失败后的最窄重验链。
- “精确删除与否定重做”完整归子代理调用。两边共同遵守上述执行底座，并保留用户要求的结果、
  当前授权、安全、权限、数据完整性、无功能回退和与结论匹配的证据，但不复制各自的详细执行规则。

#### 调用与选配

- 所有父代理使用本插件。确定性短工作直接用工具；需要持续判断的工作按质量、资源成本、时间
  联合选路，在有实际收益时尽早调用可独立完成的子代理，不设置最低调用数，也不为占满容量拆分。
- 总成本同时计算父代理继续独立工作的上下文、工具往返和返工，以及子代理的启动、交流、整合
  与核验；不能只把子代理算成新增成本。任务形状、范围或困难变化后，重新判断新出现的切片。
- Luna、Terra、Sol 与 Astra 的当前分工、思考程度和速度以现行技能为准；Astra 只用于相对
  GPT-5.6 有决定性质量差距或能降低整项资源成本的当前专家问题；普通视觉或控件布局工作不
  自动触发 Astra，派出的 Astra 最高为 `xhigh`。
- 当前没有新增独立模型路由器或自动调档脚本，是基于现有工具没有派发前拦截钩子的实施判断，
  不是用户禁令。出现真实消费者、稳定触发点，或现有链路反复偏航且无法纠正时重新评估。
- 并发容量是上限而不是目标；插件不另设单会话累计调用次数。实际工具、账户和运行环境限制仍
  按现时证据处理。

#### 上下文、沟通与结果采用

- 新会话或上下文重置只读交接中的当前要求和会改变下一动作的重要片段，再读一次技能入口；
  reference 按实际操作首次需要时读取。父子代理都使用最小充分上下文，截断只补缺段。
- 父代理及时向用户说明已知事实、已采用动作、实际进展和下一步；这不等于增加无依赖的内部
  消息、固定模板或空洞心跳。
- 子代理的进展 commentary 不是可采用交付；只有明确 final 或等价结果收据才采用。具名缺口
  只做一次明确补问，仍不足就关闭或只处理剩余缺口，不重复收取全文或完整重做可靠结果。

#### 保留子代理与经验

- 角色按可复用能力族组织，不因项目、框架、动作名称或配置档位不同而拆得过窄；工具、写权限、
  安全风险或决定性证据不兼容时仍分开，范围扩大不增加权限。
- 只把任务已有明确最终结论的失败记为 `failure`；运行中、中断、未采用、用户停止、工具阻断
  或结果未定不算失败。累计第二次明确失败后移除该角色资料，不另留退役副本或恢复入口。
- 经验只保存可跨任务复用、带适用范围和证据限制的内容；失败教训只留“情境、停止依据、重开
  条件”的短记录，不保存来源项目、长日志或无依据推断。
- 完整保留角色 TOML 不设聚合 KiB 或字节上限；仍保留逐字段的功能、安全、所有权、真实 TOML
  解析与回读边界。

#### 验证、说明与发布

- 源码、静态检查、安装缓存、已加载任务和真实运行分别取证。若要证明安装后的新会话行为，
  使用正式缓存和不读取本项目交接的独立新任务；静态文案、显式加载候选或阻断说明不能代替。
- 子代理调用验收使用最小调用探针，不让子代理完整做题。自然任务验证路由选择；强制指定组合
  只验证可调用性和公开协议，不能写成生产路由质量证明。
- 提交前解决仍在活动清单中的问题并完成必要验证；用户取消或留待自然场景的事项退出活动清单，
  同时如实保留尚未覆盖的范围，不把未验证写成已解决。
- 新版本只用一至三条说明概括用户可见变化，并同步 CHANGELOG、README 与 manifest 的可见
  版本信息。已有持续授权只在本插件有改动且验证通过时执行一次升版、提交、推送和重装；无改动
  或同轮已发布时不重复。
- 中文正文使用当前统一术语；代码标识、路径、模型名、命令和原始错误保持原样。全局用户文件、
  应用重启和其他外部动作按用户当前明确要求处理，不沿用已结束的一次性范围。

#### 交接维护

- 交接的作用是让新会话继续当前工作并避免重复犯错，不是保存所有原话或项目历史。已完成并
  已交付的要求、旧截图、旧探针、任务 ID、旧版本流水和逐轮测试记录不保留在当前交接。
- 稳定要求按主题浓缩；动态部分只维护一个注明日期、来源、缺口和最窄下一步的当前快照。新状态
  直接替换旧状态，不在文件末尾继续追加历史层。

## 项目级父代理跨会话记忆

- 本文件是 `codex-lean-stack` 项目级、显式、可审计的父代理跨会话记忆；不是模型隐藏记忆，
  也不是 SQLite 中的子代理经验。
- 父代理在新会话开始或工作上下文重置后（包括压缩或切换上下文窗口）亲自读取当前用户要求
  和会改变下一动作的重要片段：最新快照、最早活动项、最后可信状态、相关稳定决定、来源定位
  与授权范围；不重读已交付历史、旧快照、大日志、已采用结果或无关文书。具名缺口、冲突或
  来源变化才定向展开对应段落。子代理摘要或读取不能替代父代理这次按限定范围恢复。
- 随后只读取一次当前 `skills/lean-stack/SKILL.md` 入口；当前操作首次路由到尚未加载或已经
  变化的 reference 时才定向读取。实际调用子代理不重复读取入口或未变细则；普通后续轮次复用
  已加载且未变化的内容。恢复后简短告知用户已读范围和接着处理的剩余项；漏报时补说明，不为
  补通知重复读取。
- 同项目的新父代理由当前上层 `AGENTS.md` 的项目交接规则找到并读取本文件；插件不负责发现、
  创建、读取或更新项目交接。SQLite 只保存当前保留子代理
  身份、明确完成的成功或失败任务结果、经验与摘要；累计第二次明确失败后删除该角色全部资料，
  不另存退役角色，也不另建重复的父代理数据库、后台队列或状态机。

### 交接增删规则

- 新建或修订本项目交接时，只加入会改变后续动作，或能避免已证实错误复发的内容；注明来源、
  适用范围、证据或缺口、下一动作或重开条件。用户要求与助手理解分开，源码、静态、缓存、
  运行和交付状态分开。本节只服务当前仓库，不作为插件 reference 或跨项目模板。
- 新要求只替换与其冲突的旧要求，未冲突的有效要求继续保留。除非用户明确要求，不把单次失败、
  阶段性停止或助手推断扩成永久、绝对禁止。
- 用户要求“在现有基础上优化”时，保留现有结构和全部不冲突的稳定要求，只对冲突、失效、重复
  或无续做价值的内容做点状增删；只有用户明确要求结构性重写时才整体改造。
- 每轮项目迭代使规则、决定、版本、证据、运行或剩余事项变化时，收口前更新本文件并直接替换
  失效状态；删除已完成且已交付的要求、被覆盖的决定、旧快照、旧版本与测试流水、截图、探针、
  任务 ID 和无续做价值的过程。失败路线只留“情境 → 停止依据 → 重开条件”的短记录。
- 稳定内容按主题合并，动态部分只保留一个当前快照与接手入口；无变化不写，不保存思维过程、
  凭据、临时日志、可推导清单或无关历史。未改变的证据可以复用，仍在活动清单中的验收不能省略。

## 用户全局文件的修改范围

- `C:\Users\fml\.codex\AGENTS.md` 是用户文件；未经用户当次明确同意不修改。本轮按用户当前
  纠正，将相关规则改为“用户未明确提出、但根据用户要求泛化出来的助手规则可以保留；应标明
  其为助手推断并写清适用条件，不得把它生成或写成无条件的绝对指令。”，不改其他内容。
- 该用户文件包含用户已批准的通用项目交接规则、主任务连续性与最终说明、中文术语、权威路径与
  代码修改范围、外部影响授权、全局验证效率和一行默认插件调用指令。
- 未经用户当次明确同意，任何代理和本插件都不得修改该文件。用户明确授权时，安装辅助器
  也只能幂等确保这一行，不能复制插件的子代理加速规则或改写其他内容：
  默认调用已安装的 `codex-lean-stack` 插件；是否启动子代理仍由插件自身规则决定。
- 用户另行明确授权写入的通用规则不扩大安装辅助器的写入范围；安装辅助器仍只能确保上一行。
- 子代理加速规则仍由 `skills/lean-stack/SKILL.md` 承载，不复制到 `AGENTS.md`。
- 普通子代理从已提供的任务范围与决定性证据开始；不承担父代理的完整交接及插件初始化。
  父代理给出已知文件定位与相关交接摘录，具体扩读、路径失效和截断规则见委派细则。
- 本项目升版、提交、推送与重装的既有持续授权只在当前范围包含插件改动且必要验证通过时使用
  一次；同轮已发布或当前要求限定为交接文件时不扩展。部署、外部消息、打开 Codex 面板和
  重启 ChatGPT 仍需另行明确授权。

## 未完成要求锚点与当前交付

- 主任务链锚点只指向最早一个尚未完成，或已完成但尚未通过用户可见回复交付的要求。
- 要求完成并在回复中交付后自动退出活动清单，不要求用户再次确认，也不继续纳入后续锚点。
- 纠正只重新打开真正受影响的已交付项；无关完成项保持历史状态。“继续”只恢复剩余活动项，
  不复活已经完成并交付的要求。
- 新要求和对活动项的调整不能挤掉其他尚未完成或尚未交付的事项；取消、停止、放弃或替换只
  作用于用户指定的项。
- 最终说明只报告上一次用户可见交付后新完成但尚未报告的结果，以及当前未完成、受阻或需要
  决定的事项；此前已经交付的完成项不重复总结，除非用户明确要求回顾或当前结论必须引用。
- 这只是会话内轻量清单，不增加数据库、后台队列或持久状态机。交接中的历史记录也不自动
  变成活动要求。

## 首条快速说明

- 父代理处理每条新用户要求时，先快速发一条简短、用户可见的说明，写清当前理解、立即动作
  和必要范围与限制；需要工具或代理时在第一次调用前发出，随后继续，不等待用户确认。
- 收到纠正、追加要求或状态询问时，首条说明先交代怎样应用新信息，再自动恢复主任务其余工作。
- 能够一次直接答完且不需要工具时，最终答案本身就是首条说明，不额外发送重复前言。不得把
  首条说明写成长计划、模板客套或尚未完成的结果声明。
- 父代理对用户的进展说明不受内部“每个关键步骤一条”或“无异议沉默”限制；同一阶段
  有具体进展就及时说明。最终独立交付必要原因、可观察变化、实际入口、验证对象与剩余事项和限制。

## 任务类型与任务类型组

完成适用的会话入口读取后，父代理按当前工作读取 `$lean-stack`、`$lean-simplify` 中实际适用的
技能入口、工作树和与具体任务直接相关的参考文件，并保留所有现有修改。

父代理按以下顺序推进：

```text
收到任务
→ 判定当前工作的具体任务类型
→ 确定性工具可直接完成：走工具路线，不建立任务类型组、不定制子代理
→ 需要持续模型判断：完成一次调用判断
→ 判断具体任务类型是否适合已有任务类型组
  → 适合：确定复用该组
  → 不适合：先建立新的运行时任务类型组
→ 任务类型组确定后，才检查可见保留子代理
  → 有：复用保留子代理
  → 无：父代理定制运行时新子代理
```

只有具体任务类型和任务类型组都确定后，父代理才能复用或定制子代理；此前不得选择模型、
思考程度、速度或权限配置。任务类型组组织已经决定调用的运行时工作，不是调用门槛；任务
类型是否稳定也不是当前调用前置条件。

当前任务先由运行时组完成。真实结果通过必要核验并被采用后，父代理才判断职责是否能去除
来源项目身份，形成跨任务、跨项目、跨会话复用的全局领域规则。只有全局领域保留，没有项目
保留层；QML、媒体、插件、检索、发布或交接等不同领域可以各有专门角色，不要求每名角色处理
所有项目类型。

## 主任务链与辅助链入口

主任务链是唯一主线。辅助链只能从以下位置开始，并在完成或跳过后返回主任务：

- 主任务需要调查或实现时：进入 `$lean-simplify`，只选适用任务手册。
- 判断工具或模型路线时：一次调用、容量、成本和安全并行。
- 一次调用判断通过、任务类型组确定后：保留子代理复用或运行时定制；需要写入时才进入
  写入保护。
- 子代理交付可采用结果、父代理仍要测试或集成且有空闲容量时：只有必要质量或总成本收益
  明确才并行启动有限、只读的可选保留与经验准备；父代理不等待，准备不是提交前置条件。
- 产生修改后：由 `$lean-stack` 执行迭代测试链；检查对象、预期和受影响范围来自主任务精简。
- 代码、配置、公共行为或高风险事项的范围变化时：由 `$lean-simplify` 进入条件性语义复核与
  最终验证链。
- 验证失败时：由 `$lean-simplify` 进入失败后的最窄重验链。
- 当前任务类型组的已就绪结果完成核验并被采用时：复制或变体竞争、默认保留、默认经验提交、
  必要的 SQLite 检查和组收口。
- 当前工作属于插件发布时：由 `$lean-simplify` 收窄版本、校验、本地重装和文档维护。
- 固定成本检查只有到期且能非阻塞并行时才开始。

任何辅助链都不得另立主线、绕过完成条件或在主任务完成后继续拖延交付。

## 工具、子代理与调用规则

- 几秒的确定性命令由父代理直接运行；多个独立短命令放进一次工具调用并发；长但步骤确定
  的工作进入持续终端或后台。
- 简单、短小的 PowerShell 命令仍内联。出现多层引号、嵌套 JSON/正则、反引号、多行逻辑或
  复杂变量插值时，首次执行前写成任务专属临时 `.ps1`；第一次内联失败若明确是解析或转义
  问题，就停止继续改写 one-liner，后续只编辑同一个脚本。业务错误不得误判为转义问题。
- 临时 PowerShell 脚本使用 `param()` 和 `-LiteralPath`，不含秘密、不进入仓库或提交；需要
  清理时，普通脚本进入 Windows 回收站，重要诊断脚本或证据进入任务专属 `待删文件`。不为
  此建立通用包装器、自动重试器、后台服务或新状态机。
- 已就绪、范围明确、可安全独立推进且需要持续模型判断的任务类型，在符合三原则时调用；
  收益优先必要质量或质量达标后的父子合计总成本优势，成本相近再比时间。
- 确定性工具更快、前置条件未满足、没有可调用能力、权限或写入冲突无法解决、只能重复已有
  工作，或者成本大幅增加而无相应收益时，阻断调用。
- 插件不设置同时调用数字或主任务累计调用数字；使用运行环境实际容量，但容量是上限，不是
  调用目标。配置标识为 `agents.max_concurrent_threads_per_session`。
- Luna、Terra、Sol、Astra 的详细分工和完整 `模型 + 思考程度 + 速度` 选配只以
  `references/execution-routing.md` 的一次联合选配为准，本交接不维护第二份固定模型表。
  父代理依据任务证据、质量底线、完整路线资源成本和关键路径选择；快速速度可在完整路线仍处于
  可接受成本带且能明显缩短关键路径时使用，用户明确期限会提高该收益，但不是唯一前提。
- 父代理启动子代理后继续不重叠的主任务，只在真实依赖点汇合，不主动反复轮询，也不为统一
  收齐而等待全部。
- 并发计数以当前运行环境实际槽位与口径为准，不从配置字段名推断是否计入父代理。
- 同一团队的已知队友可通过真实内部消息直接传递有来源、标明验证程度的依赖发现；无需
  父代理转发，不互相扩权或代交结果，职责、共享写入与安全冲突交父代理裁决。
- 默认保持平面委派。已授权子项目本身还有多个已就绪独立切片，而且局部拆分、纠偏和汇合能
  降低总成本或提高必要质量时，任务卡可把子代理指定为协作父代理；它只在有限下游范围内
  使用真实 `collaboration.spawn_agent`，每个下游子代理仍提交自己的独立结果。
- 当前用户已经为本插件持续授权父代理之间协作。符合三项原则、服务当前主任务、范围清楚且
  不涉及未获明确要求的不可恢复删除时，父代理或任务卡获批的协作父代理可直接读取、调用或
  新建 Codex 父代理任务，不再重复询问。跨任务路线只设一个整合父代理；任务标题和摘要不可信，
  `send_message_to_thread` 也不能冒充内部 `collaboration.send_message`。

GPT-6 ultra 父代理保持用户选择，子任务按价值显式选配；需要覆盖配置时不全量继承历史。
额外决定性推理收益明确时可以用 Astra，不强制廉价模型逐级试错。费用与档位的权威说明为
`skills/lean-stack/references/cost-baseline.md`；不把思考程度当 Fast 或固定费率倍率。
官方指南的适配决定：用户明确要求优先于技能建议；仅核对实际造成停顿的条文并公开来源，
不例行扫描所有技能。阻断只作用于受影响动作，仍获准的独立工作继续，不能换工具绕过停止。
中途纠正及时传到执行者，迟到结果不能恢复撤回授权。API 异步与调档参数不冒充 Codex 工具
能力；具体执行范围和限制由主技能及 `references/execution-routing.md` 维护。

## 子代理开场声明与范围明确的交流

子代理任务说明给出职责、来源范围、写入所有权、成功与停止条件、父代理规范任务名，以及
真实需要中途解锁的依赖；没有依赖就自包含完成。详细规则由
`skills/lean-stack/references/delegation.md` 和生成提示 `skills/lean-stack/scripts/agents.py` 承载。

- 每个新子任务的第一条用户可见 commentary 必须以前四行实际配置开头，之后才能出现计划、
  解释或动作；最终回复保留同一配置抬头。普通任务不再发送重复内部配置声明，不为声明索要
  确认、升模或发送无业务探针。
- 只有会解锁父代理或队友下一动作的发现、必要纠偏、风险或阻断才使用真实内部消息。
  无依赖的普通过程随最终回复交付，不按步骤机械发送，不把最终结果再复制为内部消息。
- 四行字段依次为本地名称、模型、思考程度和速度，必须使用真实值，不能以角色名、任务卡文字
  或行为猜测速率。内部通道缺失且交流是成功条件时停止；自包含任务可继续，公开文字不能冒充
  内部消息。
- 已知 Luna 目录 v1 时只分配无需中途纠偏的自包含工作；需要队友消息时首次选配具备能力
  的组合。配置不授予工具，不能用跨任务 API 替代内部通道，也不恢复旧模型目录。
- 父代理继续面向用户说明关键进展、实际采用结果和剩余事项与限制；内部节省不削弱用户可见交付。
- 复用已完成子代理处理新工作时，正常进展中点名对象与职责，派发以真实工具回执为准。
  不为侧栏显示而重派、换模型或凑调用。

## 任务类型组、复制、变体与收口

- 同一可复用能力族归入同一任务类型组；兼容的调查、诊断、实现、修复、测试与验收按角色权限
  复用。项目、框架、动作动词、交付名称、单次成功条件或配置档位不单独建立窄组；只有工具、
  写入权限、安全风险或决定性证据形状不兼容才拆分。范围放宽不授予只读角色写权限，父代理仍
  为每个子任务分别指定输入、成功条件和停止条件。
- 任务类型组确定后，有可见保留子代理就复用；没有时才定制运行时新子代理。复用保留子代理
  时，由它读取自己的经验和具体配置，不要求父代理强制重写已有配置。运行时定制不是持久
  创建，不调用 `ensure` 等待配置可见。需要中途交流时先确认实际 Luna 模型目录为 v2；目录
  为 v1 或直接内部调用失败就停止该子任务，不改用跨任务 API 或更高成本模型。
- 描述不足、可能遗漏跨项目复用时，从已加载技能的绝对目录运行 `agents.py status --for-routing`
  做一次范围明确的目录查询，当前上下文复用。按职责、输入、证据匹配；目录存在不等于生成工具
  已加载该角色。配置不适配时优先复用职责与必要经验适配本次调用，不新造同领域记录，
  不把临时运行记作原保留身份。详细入口只由 `references/specialist-memory.md` 维护。
- 复制的是任务类型组里的子代理，只处理第二个及后续同类型子任务；父代理分别指定输入、
  成功条件和权限。
- 变体只在复制前确认现有配置确有进步空间时出现。父代理根据任务类型、价值、风险、证据、
  时延和成本，联合选择完整的 `模型 + 思考程度 + 速度` 配置；三个字段可以因相互制约而同时
  变化。单轴变化只在明确需要识别因果时作为可选实验，不能取代联合选配。
- 出现复制或变体时，各子任务先分别完成核验和最终回复，再用可比较证据竞争，选出唯一留下
  的子代理。
- 子代理交付可采用结果、父代理仍要测试或集成时，仅在有空闲容量且必要质量或总成本收益
  明确时启动有限、只读的经验准备子代理；父代理继续测试，不等待，准备阶段不写 SQLite，
  没有准备或准备未赶上不阻断正式提交。
- 唯一子代理确定、结果通过必要核验并被采用后，先形成领域、输入形状、通用职责、可验证交付
  和硬性限制组成的全局领域规则。规则能去除来源项目身份时，第一次成功就默认尝试一次
  `ensure`，再用稳定 `event_id` 默认尝试一次 `improve`，把本组胜出方法和失败避免信息泛化、
  去敏、去重后合并追加成一条经验。每个全局领域任务类型组各保留一个休眠配置，不存在项目
  保留层，也不存在全局只能保留一个。
- 仅结果未通过或未采用、用户否定、无法安全去敏，或者权限、安全、SQLite、文件、身份、
  CAS 使短写不能立即安全完成，或角色仍固化项目、插件、个人技能、路径、版本和一次任务事实
  而无法泛化时跳过；不排队、不轮询、不重试，主任务不依赖持久写入成功。
  最终说明按组给出角色与经验保存回执。只有实际要读写经验时才打开并检查 SQLite。
- 经验维护完成或安全跳过后，其他运行时子代理才移出组并结束。真实结果出来后可以评估角色
  泛化质量和后续复用效果，但这不构成核验采用结果默认 `ensure`/`improve` 的前置条件。运行
  线程照常进入 Done；跨任务保留不让它无限运行。
- 每次启动正式保留身份前，父代理生成 UUID `run_id` 并只在父代理侧关联调用与结果；
  不写入自然语言任务说明，子代理也不在 commentary 或最终回复中回显。成功条件达到、结果经
  核验采用且线程进入 Done 后，才用 `record-run --outcome success` 记录一次成功存活轮次；整个
  任务有决定性失败结论时用独立 UUID 记录 `--outcome failure`。累计第二次明确失败后永久删除
  该角色 TOML、身份、全部运行、原始经验、纠正事件与摘要。运行中、中断、用户停止或否定、
  未采用、工具阻断及结果未定不记录。
  v2 以前没有可靠运行 ID；v4 既有运行只迁为成功，不猜测历史失败。
- TOML 重配只影响以后新生成的代理线程；重配前已经创建的运行中或已完成线程继续使用原加载
  提示。不得用 `followup_task` 激活旧线程来声称新通信规则生效；旧线程出现跨任务消息时只做
  一次内部纠偏，真实验证必须使用新任务中新生成的线程。

## 经验、SQLite、成本与生命周期侧链

SQLite v5 经验与任务结果体系只保留：

1. SQLite 台账。
2. 每个通过规则检查且未命中明确排除项的全局领域任务类型组一个保留子代理；多个领域可同时
   保留，不存在项目保留层。
3. UUID 幂等追加的明确成功或失败任务结果；累计第二次明确失败后永久删除该角色全部资料。
4. 只追加原始经验和纠正事件。
5. 经验数量不封顶。
6. 经验摘要压缩。

复制和变体本身不写入 SQLite。经验维护窗口中，必要时先核对受支持结构，再安全重配同一
保留子代理或只追加经验；未知结构、忙锁、损坏或漂移立即跳过，不阻塞主任务。被否定经验
通过追加纠正事件退出活跃记忆，原事件保留审计。经验完成或跳过后才移除其他子代理。

普通命令只接受当前 v5。现实存在且结构精确匹配的 v1/v2/v3 台账只能显式执行
`agents.py migrate-global --plan <JSON>`；精确 v4 只通过 `agents.py migrate-attempts` 升级，
既有 `agent_runs` 保留为成功且不推断历史失败。迁移计划必须覆盖全部角色和真实来源词。来源词只用于
阻止项目化语义进入长期规则或经验，不写入 SQLite、TOML、规则或完成收据。迁移保留同一身份、
令牌、创建时间、运行记录和原始经验；旧重要 TOML 进入插件专属 `待删文件`，不物理销毁。

插件运行时使用已经维护的固定成本预估，不在每个任务重新搜索费率、计算令牌或制作评分表。
每周检查不到期不联网；到期但不能非阻塞核对时延后。插件不创建自动化、系统定时任务或外部
守护进程。

生命周期不是固定顺序、固定数量或后台状态机。`status`、`recall`、`ensure`、`record-run`、`improve`
和 `delete` 是普通按需能力；`migrate-global` 与 `migrate-attempts` 只用于显式现实迁移。
`status --for-dashboard` 输出一次只读聚合；`--watch-seconds 1..3600` 在前台逐行周期输出 NDJSON，
`Ctrl+C` 结束，不创建后台任务，也不声称 Codex 插件卡自动刷新。手动 `delete` 保持严格身份
与删减资格，只有零经验、零摘要、零任务尝试的精确插件自有角色可永久删除；累计第二次明确
失败的自动移除则清除该角色全部关联资料。

## 统一术语

| 英文标识 | 中文用词 |
| --- | --- |
| parent agent | 父代理 |
| subagent | 子代理 |
| main task | 主任务 |
| instance | 子任务 |
| task type | 任务类型 |
| task type group | 任务类型组 |
| retained subagent | 保留子代理 |
| copy | 复制 |
| variant | 变体 |
| experience | 经验 |
| runtime environment | 运行环境 |

英文 `instance` 在中文正文中只翻译为“子任务”。代码标识、模型名、命令、路径和原始错误
使用代码格式。

## 权威路径与代码修改范围

- 可编辑源码根目录：`C:\Users\fml\plugins\codex-lean-stack`。
- 子代理调用技能：`C:\Users\fml\plugins\codex-lean-stack\skills\lean-stack\SKILL.md`。
- 主任务精简技能：`C:\Users\fml\plugins\codex-lean-stack\skills\lean-simplify\SKILL.md`。
- 插件清单：`C:\Users\fml\plugins\codex-lean-stack\.codex-plugin\plugin.json`。
- 个人市场：`C:\Users\fml\.agents\plugins\marketplace.json`，市场名为 `personal`。
- SQLite 台账：`C:\Users\fml\.codex\lean-stack\specialist-memory-v1.sqlite3`。
- 全局领域台账实现：`skills/lean-stack/scripts/agents.py`。
- 工具、子代理、PowerShell 复杂转义、迭代测试调用路线，以及主任务条件性验证的单一详细来源：
  `skills/lean-stack/references/execution-routing.md`；各段归属由两个技能入口说明。
- 可写并行、共同热点与精确撤销：`skills/lean-stack/references/write-parallelism.md`。
- 委派及用户否定后的停止与重做：`skills/lean-stack/references/delegation.md`。
- 反 AI 过度工程权威规则：`skills/lean-stack/references/anti-overengineering.md`。
- 显式消融反馈循环：`skills/lean-stack/references/ablation-loop.md`。
- 13 条 Mermaid 权威规则内容源：`skills/lean-stack/references/flowcharts-zh.md`。
- 静态 SVG/HTML 由独立插件 `C:\Users\fml\plugins\architecture-viewer` 的
  `$architecture-viewer` 负责；原生交互式 Viewer 由同一插件的 `$archify` 负责。本插件不保存
  第二套 HTML/SVG、渲染器、Viewer Runtime 或 loopback 预览服务。
- 显式安装入口：`skills/lean-stack/scripts/install_plugin.py`；正式安装成功后才幂等确保用户
  已授权的一行全局默认调用指令。它先预检 `AGENTS.md`，并在同一把文件锁内完成安装和
  成功后的单行写入；失败或未授权时不触碰该用户文件。

源码是唯一编辑入口；安装缓存只用于核对，不作为日常修改目标。链路图只是辅助说明，代码、
技能约定、约定一致性测试和真实运行证据才是验收依据。

---

以下部分是每轮都会变化的状态，统一放在恒定规范之后。

## 当前快照与接手入口

当前快照（2026-09-13）。来源为用户当前要求、源码差异、运行与合同测试、技能校验、正式安装
缓存，以及安装后全新父任务的真实调用事件。

### 最后可信状态

- 源码与正式缓存版本均为 `5.2.0+codex.20260912224705`。本轮已经只升版和安装一次；不得因
  收口、交接、重复验证或提交再次升版或重装。
- 插件现在提供两个并行而分工明确的功能：`$lean-stack` 管子代理调用精简，
  `$lean-simplify` 管主任务精简。共享边界只保留用户结果、当前授权、安全、权限、数据完整性、
  无功能回退和与结论相称的证据；详细反过度工程规则只有一个权威来源。
- 正式安装发现全局 `C:\Users\fml\.codex\AGENTS.md` 已有用户授权的默认调用指令，返回
  `modified: false`；本轮没有改写全局规则，也没有新增全局查看、暂停或完整撤销功能。

### 本轮实际变化

- 非平凡主任务默认先进入 `$lean-simplify` 选择必要步骤和最窄验证，只有选中的步骤需要模型或
  子代理时才进入 `$lean-stack`；两个技能和插件 UI 共用同一条组合入口，不再以两个割裂提示
  诱发只加载一半功能。
- 父代理必须把名称、模型、思考程度、速度、存活轮次和经验状态的实际值写进每个直接
  `spawn_agent` 或 `followup_task` 任务卡；通用 `explorer`、`worker` 和继承历史都不再视为
  自动获得保留角色开场提示。
- 测试范围由改动面、现实消费者和风险决定，不从现有测试文件数、函数数、历史全套命令或发布
  标签反推。插件合同测试停止读取本项目交接，并移除 README、Mermaid 和交接布局快照；角色
  状态、安装、成本状态、版本 CAS 与文件安全等运行行为覆盖保留。链接逃逸用例改用确定性
  元数据模拟，不再因当前 Windows 缺少创建符号链接权限而显示跳过。
- 同一长来源只由一个所有者完成发现和完整读取；父代理用 `SOURCE_ROUTE` 记录来源身份、所有者、
  快照、消费者和具名缺口，其他子代理复用可定位证据包。只有来源变化、覆盖缺口、结果冲突或
  必要高风险事实才定向复核，不因观点、角色或模型不同重复通读。
- 保留角色的固定提示只保留子代理自己执行的职责；模型路由、成本、生命周期清理、跨任务编排和
  其他父侧规则不再复制进去。代表性默认角色的固定提示由约 `13,628` UTF-8 字节降为 `3,223`
  字节；这是输入体积测量，不是 token、质量或返工收益证明。完整角色 TOML 仍不设总 KiB 上限。
- 保留子代理首条可见回复仍以名称、模型、思考程度、速度四行开头，紧接存活轮次与经验状态两行；
  不要求声明经验适用性，最终仍只重复四行配置。权限、限定读取、测试与失败修补、自身线程交付、
  CAS、幂等、第二次明确失败清理和故障回滚均保留。
- SQLite schema v6 在 `agent_runs` 增加可空的经验版本摘要。父代理明确提供且摘要与记录时已验证的
  当前角色内存一致时，运行结果可与该版本关联；这不是宿主实际加载回执，也不证明经验造成成败。
  省略摘要保持原运行记录语义，v4/v5 显式迁移不回填历史关联。

### 本轮验证证据

- 受影响的技能合同、安装入口写入/幂等/失败保护均通过；两个技能通过官方 `quick_validate.py`，
  manifest JSON、Python 编译和 `git diff --check` 通过。链接/重解析点拒绝在两个实际读取入口
  之前由确定性用例验证；没有运行无关测试模块或依赖本机链接权限的集成用例。
- 正式安装成功并读回 `5.2.0+codex.20260912224705`。只核对本轮改变的运行时部署文件，源码与缓存一致；
  没有对整仓、安装缓存或 `.git` 做全量哈希，也没有重启 ChatGPT；全局 `AGENTS.md` 返回
  `modified: false`。安装后追加的测试精简和交接记录不参与运行时生效结论，也不触发第二次重装。
- 实际用户台账由 v5 显式迁移到 v6，保留 81 条既有成功、0 条失败；未回填历史失败或经验关联。
  迁移后的只读状态仍因既有角色职责缺少当前 canonical global contract 而返回
  `auxiliary_skipped`；该问题在本轮修改前已存在，本轮未擅自重配、删除或接管角色。
- 全新父任务 `01a097cf-cbee-7c22-8527-ebf0f27ac8f7` 只从正式缓存加载技能：短算术直接处理；
  随后先采用 `$lean-simplify` 收窄步骤，再由 `$lean-stack` 启动
  `gpt-5.6-terra/medium` 运行时子代理。子代理首条 commentary 直接显示完整六行开场，研究正文前
  被父任务中断。此证据只证明入口顺序、真实调用与开场，不证明业务答案质量、保留角色复用或费用。

### 工作树与剩余事项

- 5.2.0 的实现、验证、正式安装和新父任务调用探针已完成；本快照随代码收口到 `main` 并同步
  远端。若工作树干净且本地 `main` 与 `origin/main` 一致，则活动事项为空并停止，不重复安装、
  迁移、全量测试、重启宿主或再升版本。
- 新父任务只保存调用测试事实，不能从中恢复业务待办。既有角色合同不匹配只在后续明确要求处理
  角色台账时重开；不得据此擅自迁移角色内容或放宽验证。
- 本地 `stash@{0}: deferred handoff optimization 2026-09-12` 仍是旧恢复候选，不自动应用、删除
  或复活为当前工作。

### 后续代理开始方式

1. 复用本轮测试、安装、迁移和调用探针证据；只有相关源码、配置、依赖、来源快照或风险变化时
   才扩大验证，不让多个代理重新完整读取同一来源。
2. 后续涉及经验结果时，只称“父代理声明的经验版本与记录时已验证角色内存一致并关联结果”，
   不称宿主加载证明、自主学习或因果证据。
3. 不新增用户已排除的全局查看、暂停或完整撤销功能；不恢复已删除机制，不把助手推断扩成绝对规则。

### 发布收据

- 版本已从 `5.1.0+codex.20260912220513` 提升一次到 `5.2.0+codex.20260912224705`，正式安装
  成功；同轮不再提升或重装。
- 安装验证只读回 manifest 并核对本轮 14 个变更文件，没有对全部文件逐项计算长度或 SHA-256。
