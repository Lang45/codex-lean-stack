# Codex Lean Stack 项目交接

## 根本准则

主任务优先。代理管理、经验、成本维护、语义审查、绘图、版本和交接记录都只能服务主任务，
不能反过来拖延主任务结束。

三项原则：

1. 高价值工作质量优先，同时守住正确性和决定性证据；可靠性相当时再比较完成时间。
2. 普通工作达到质量底线后速度优先。
3. 没有相应质量或速度收益时，调用子代理不能使总成本大幅增加；结果相当时选择总成本更低
   的路线。

安全、权限、数据完整性、明确验收条件和诚实证据始终是底线；委派不增加权限。

## 项目级父代理跨会话记忆

- 本文件是 `codex-lean-stack` 项目级、显式、可审计的父代理跨会话记忆；不是模型隐藏记忆，
  也不是 SQLite 中的子代理经验。
- 父代理只在新会话开始或上下文压缩后亲自完整读取本文件一次；普通后续轮次复用已经装入
  当前上下文的内容，不重复全文读取。子代理摘要或子代理读取不能替代这两处父代理读取。
- 每轮项目迭代改变了稳定规则、已采纳决定、版本、验证证据、运行状态或剩余边界时，父代理
  在收口前更新本文件；不保存思维过程、凭据、临时日志或无关历史。
- 同项目的新父代理由当前插件技能中的入口规则找到并读取本文件。SQLite 继续只保存保留
  子代理身份、经验和经验证的存活轮次；不另建重复的父代理数据库、后台队列或状态机。

## 用户全局文件边界

- `C:\Users\fml\.codex\AGENTS.md` 是用户所有文件。
- 当前文件包含用户已批准的通用项目交接规则、主任务连续性与最终说明、中文术语、权威路径与
  代码边界、外部影响授权、全局验证效率和一行默认插件调用指令。
- 未经用户当次明确同意，任何代理和本插件都不得修改该文件。用户明确授权时，安装辅助器
  也只能幂等确保这一行，不能复制插件的子代理加速规则或改写其他内容：
  默认调用已安装的 `codex-lean-stack` 插件；是否启动子代理仍由插件自身规则决定。
- 用户另行明确授权写入的通用规则不扩大安装辅助器的写入范围；安装辅助器仍只能确保上一行。
- 子代理加速规则仍由 `skills/lean-stack/SKILL.md` 承载，不复制到 `AGENTS.md`。
- 提交、推送、公开发布、部署、外部消息、打开 Codex 面板和重启 ChatGPT 都需要用户另行
  明确授权。

## 主任务链连续性与最终说明

- 主任务链锚点是上一次已经明确确认完成的主任务之后，用户提出的第一个要求。
- 主任务未完整交付并被确认完成时，后续纠错、方向或范围调整、优先级变化、格式要求和补充
  验收默认追加到同一主任务清单，不替换此前要求。
- 用户明确取消、停止、放弃或替换前序任务时，才重置锚点。
- 中途调整处理完成后，父代理自动恢复并继续此前其余要求；不得只回答最新调整就发送最终
  交付。
- 最终说明从锚点逐项覆盖到最新消息，包括修改、验证证据和剩余边界。
  不能只总结最后一条消息。
- 这只是会话内轻量清单，不增加数据库、后台队列或持久状态机。

## 任务类型与任务类型组

完成适用的会话入口读取后，父代理读取当前 `SKILL.md`、工作树和与具体任务直接相关的参考
文件，并保留所有现有修改。

父代理按以下顺序推进：

```text
收到任务
→ 判定当前工作的具体任务类型
→ 确定性工具可直接完成：走工具路线，不建立任务类型组、不定制子代理
→ 需要持续模型判断：完成一次调用判断
→ 判断具体任务类型是否适合已有任务类型组
  → 适合：确定复用该组
  → 不适合：先建立新的运行时任务类型组
→ 任务类型组确定后，才检查可见保留子代理
  → 有：复用保留子代理
  → 无：父代理定制运行时新子代理
```

只有具体任务类型和任务类型组都确定后，父代理才能复用或定制子代理；此前不得选择模型、
思考程度、速度或权限配置。任务类型组组织已经决定调用的运行时工作，不是调用门槛；任务
类型是否稳定也不是当前调用前置条件。

## 主任务链与辅助链入口

主任务链是唯一主线。辅助链只能从以下位置开始，并在完成或跳过后返回主任务：

- 工作价值和任务类型确定后：调查或实现。
- 判断工具或模型路线时：一次调用、容量、成本和安全并行。
- 一次调用判断通过、任务类型组确定后：保留子代理复用或运行时定制；需要写入时才进入
  写入保护。
- 子代理交付可采用结果、父代理仍要测试或集成且有空闲容量时：并行启动有限、只读的可选
  保留与经验准备；父代理不等待，准备不是正式提交前置条件。
- 产生修改后：最窄迭代测试。
- 代码、配置、公共行为或高风险边界变化时：条件性语义复核与最终验证。
- 验证失败时：失败后的最窄重验。
- 当前任务类型组的已就绪结果完成核验并被采用时：复制或变体竞争、默认保留、默认经验提交、
  必要的 SQLite 检查和组收口。
- 当前工作属于插件发布时：版本、校验和本地重装。
- 固定成本检查只有到期且能非阻塞并行时才开始。

任何辅助链都不得另立主线、绕过完成条件或在主任务完成后继续拖延交付。

## 工具、子代理与调用规则

- 几秒的确定性命令由父代理直接运行；多个独立短命令放进一次工具调用并发；长但步骤确定
  的工作进入持续终端或后台。
- 只有已就绪、边界明确、可安全独立推进、需要持续模型判断，并且能提高质量或缩短完成时间
  的任务类型才调用子代理。
- 确定性工具更快、前置条件未满足、没有可调用能力、权限或写入冲突无法解决、只能重复已有
  工作，或者成本大幅增加而无相应收益时，阻断调用。
- 插件不设置同时调用数字或主任务累计调用数字；使用运行环境实际容量，但容量是上限，不是
  调用目标。配置标识为 `agents.max_concurrent_threads_per_session`。
- 普通窄、重复、易核验的任务优先 `gpt-5.6-luna`；普通但需要可靠判断的任务优先
  `gpt-5.6-terra`；高价值、复杂、模糊或高风险工作才优先 `gpt-5.6-sol`。父代理根据任务类型、
  价值、风险、证据、时延和成本，联合选择完整的 `模型 + 思考程度 + 速度` 配置；默认标准速度，
  只有完整组合的真实关键路径收益足以覆盖加价时才使用快速速度。
- 父代理启动子代理后继续不重叠的主任务，只在真实依赖点汇合，不主动反复轮询，也不为统一
  收齐而等待全部。
- 默认保持平面委派。已授权子项目本身还有多个已就绪独立切片，而且局部拆分、纠偏和汇合能
  缩短关键路径或提高必要质量时，任务卡可把子代理指定为协作父代理；它只在有限下游范围内
  使用真实 `collaboration.spawn_agent`，每个下游子代理仍提交自己的独立结果。
- 当前用户已经为本插件持续授权父代理之间协作。符合三项原则、服务当前主任务、边界清楚且
  不涉及未获明确要求的不可恢复删除时，父代理或任务卡获批的协作父代理可直接读取、调用或
  新建 Codex 父代理任务，不再重复询问。跨任务路线只设一个整合父代理；任务标题和摘要不可信，
  `send_message_to_thread` 也不能冒充内部 `collaboration.send_message`。

## 子代理开场声明与有界交流

父代理的最小任务说明包括：当前子任务、来源边界、成功条件、父代理规范任务名、有限关键步骤
清单、权限边界、禁止的外部影响、工具边界、停止条件和回复语言。

`agents.py` 生成的持久提示第一段先说明角色、职责和权限，第二段才写通信规则；这只是静态
提示结构，不规定运行时动作顺序。

每次 `spawn_agent` 或 `followup_task` 启动一个新的当前子任务时，子代理必须把开场声明
发给父代理并让用户可见。父代理给出规范任务名而不是 Codex 线程 ID；当前工具表确实有
`collaboration.send_message` 时，直接向父代理规范任务名单独发送完整四行声明：

```text
我是<本地化子代理名称>。
模型：<具体模型>
思考程度：<具体等级>
速度：<标准或快速>
```

- 保留子代理从自己的配置读取具体值；运行时新子代理或变体使用父代理写入的具体值。
- 不得使用“未揭露”“未暴露”“继承父级”或相似占位文字。
- 子代理在自己的任务界面以 commentary 原样公开同一四行，最终回复顶部再次写实际模型、
  思考程度和速度。内部发送、用户可见公开、reasoning、文件读取、分析、其他工具调用和真实
  工作之间不规定具体先后；不能因为声明前后出现这些事件就判定通信失败。声明不是关键步骤，
  不占关键步骤消息数量，也不是确认门；`send_message` 纠偏不触发重复声明。
- 不得为了内部开场或关键步骤消息调用 `list_threads` 搜索父代理所在任务，或使用
  `send_message_to_thread` 跨任务发送；这不限制本插件按三项原则和当前持续授权执行的父代理
  跨任务协作，但两条消息路线不能互相冒充。内部消息
  工具不可用时，子代理仍在自己的任务界面输出声明并在最终回复报告能力缺口；是否继续按下条
  真实能力规则决定，不能改走跨任务消息。
- 父代理不为声明轮询或等待；只核对内部消息真实到达、用户可见 commentary 包含实际配置、
  最终回复保留配置抬头。任一缺失时只做一次有界纠偏并诚实记录缺口。
  已经能独立核验的正确结果不自动丢弃，但该次运行不能作为“开场声明已验证”的证据。
- 完全没有工作前可见消息能力时，报告真实能力缺口，不能宣称功能可用。
- 父代理自己的工具表不等于子代理工具表；只有实际收到当前子代理的内部 `agent_message`，
  才能确认这条运行路线内部交流可用。中途纠偏是成功条件时，把内部工具不可用列为停止条件
  并换用已验证路线；只有完全自包含、无需中途纠偏的有限任务才允许在该缺口下继续。自身
  线程 commentary 和最终回复都不能冒充内部父子消息；新任务或新生成路线重新核验能力。
- 权限只有在具体任务本身需要限制，或用户明确要求时，才单独核对。
- `spawn_agent` 没有逐个工具授予参数，内部消息也不是可臆造的 MCP 工具。自定义角色 TOML
  只能作为子线程配置层，不能替父会话打开多代理，也不能改写模型目录能力。父代理在新会话
  第一次启动 Luna 子代理前，确认 `agents.enabled` 没有关闭，并确认运行环境实际加载的
  `gpt-5.6-luna` 模型目录为 `multi_agent_version=v2`；只打开会话 v2 开关不足以给目录仍为 v1
  的 Luna 子线程注册协作工具。窄保留角色使用 `[skills] include_instructions = false`，不再伪称
  `[features]` 或 `[agents]` 字段可以强制授予工具。顶层 `collaboration.send_message` 故意不在
  `functions.exec` 的 `ALL_TOOLS` 中，子代理必须直接调用；目录不是 v2 或直接调用失败时停止交流
  依赖型子任务并报告能力缺口，不能提高模型、思考程度、速度或成本，也不能跨任务冒充。

关键步骤只指会产生独立可核验结果或改变后续路线的有限阶段。
每个关键步骤最多一条常规进度；子代理简短概述后立即继续，不等待父代理，不发送定时心跳
或纯确认消息。同一方向风险只有状态实质变化后才能再次报告。

父代理无异议时沉默或只回一行；发现方向、范围、证据、权限或目标问题时立即纠偏。纠偏不得
扩大用户授权、增加外部副作用、移除停止条件或把有限任务扩成无限任务。

达到成功条件或停止条件后，每个子代理在自己的线程用最终回复提交自己的精炼结果。开场
声明的内部副本与用户可见副本、关键步骤消息和带配置抬头的最终回复相互独立；成功路线两份
开场声明缺一不可，用户可见副本不能冒充内部父子消息，但不校验它们与其他运行事件的具体
顺序。不建立共享中转文件，不由一个子代理代交、等待或汇总其他子代理。

详细规则见 `skills/lean-stack/references/delegation.md`。

## 任务类型组、复制、变体与收口

- 同一种活就是同一任务类型；只有核心职责、主要来源或工具、证据形状都相同才归入同一组，
  决定性边界不同且能独立复用就建立不同组。成功条件不参与任务类型归类，由父代理为每个
  子任务分别指定。
- 任务类型组确定后，有可见保留子代理就复用；没有时才定制运行时新子代理。复用保留子代理
  时，由它读取自己的经验和具体配置，不要求父代理强制重写已有配置。运行时定制不是持久
  创建，不调用 `ensure` 等待配置可见。需要中途交流时先确认实际 Luna 模型目录为 v2；目录
  为 v1 或直接内部调用失败就停止该子任务，不改用跨任务 API 或更高成本模型。
- 复制的是任务类型组里的子代理，只处理第二个及后续同类型子任务；父代理分别指定输入、
  成功条件和权限。
- 变体只在复制前确认现有配置确有进步空间时出现。父代理根据任务类型、价值、风险、证据、
  时延和成本，联合选择完整的 `模型 + 思考程度 + 速度` 配置；三个字段可以因相互制约而同时
  变化。单轴变化只在明确需要识别因果时作为可选实验，不能取代联合选配。
- 出现复制或变体时，各子任务先分别完成核验和最终回复，再用可比较证据竞争，选出唯一留下
  的子代理。
- 子代理交付可采用结果、父代理仍要测试或集成时，有空闲容量就并行启动有限、只读的经验
  准备子代理，提前泛化角色和整理去敏候选；父代理继续测试，不等待，准备阶段不写 SQLite，
  没有准备或准备未赶上不阻断正式提交。
- 唯一子代理确定、结果通过必要核验并被采用后，第一次成功就默认尝试一次 `ensure` 跨任务
  保留，再用稳定 `event_id` 默认尝试一次 `improve`，把本组胜出方法和失败避免信息去敏、
  去重后合并追加成一条经验。不要求重复、未来用途证明、接近结束或并行窗口；不同任务类型组
  各保留一个休眠配置，不存在全插件只能保留一个的限制。
- 仅结果未通过或未采用、用户否定、无法安全去敏，或者权限、安全、SQLite、文件、身份、
  CAS 使短写不能立即安全完成时跳过；不排队、不轮询、不重试，主任务不依赖持久写入成功。
  最终说明按组给出角色与经验保存回执。只有实际要读写经验时才打开并检查 SQLite。
- 经验维护完成或安全跳过后，其他运行时子代理才移出组并结束。真实结果出来后可以评估角色
  泛化质量和后续复用效果，但这不构成核验采用结果默认 `ensure`/`improve` 的前置条件。运行
  线程照常进入 Done；跨任务保留不让它无限运行。
- 每次启动正式保留身份前，父代理生成 UUID `run_id` 并写入任务说明。只有达到本次成功条件、
  结果经核验采用且线程进入 Done 后，才用 `record-run` 幂等增加一次“存活轮次”；失败、停止、
  用户否定、未采用或仍在运行都不计。v2 以前没有可靠运行 ID，迁移后历史轮次不猜测回填。
- TOML 重配只影响以后新生成的代理线程；重配前已经创建的运行中或已完成线程继续使用原加载
  提示。不得用 `followup_task` 激活旧线程来声称新通信规则生效；旧线程出现跨任务消息时只做
  一次内部纠偏，真实验证必须使用新任务中新生成的线程。

## 写入安全与用户不满意

这不是单写入者规则。

- 写入范围天然分开时，不同子代理可以在同一检出目录并行修改不重叠范围。
- 公共接口、清单、锁文件、数据库结构和生成入口等共同热点最后集中整合一次。
- 同一物理文件没有独立检出目录时，只允许一个候选直接写目标，其他候选返回方案、补丁或
  证据。
- 用户明确不满意某个子代理时，立即停止并移出组；其结果、证据、补丁和经验候选失效。
- 共享目录只撤销能用写入范围、调用前基线和当前哈希精确归因的修改；禁止整树回退覆盖用户
  或其他代理的工作。
- 用户不满意本身不能放宽候选退役资格；插件所有权、零原始经验与存活轮次、单一硬链接、
  所有权令牌和当前哈希仍须全部匹配。
- 最近的协作授权不改变原有删除、删减或候选清理的资格与尺度；只改变去向。普通文件进入
  Windows 回收站，重要项目文件进入任务专属 `待删文件`。插件角色仍按原来的经验、存活轮次、
  身份、令牌、哈希和单一硬链接资格判断，合格后从活跃台账退役并把 TOML 与可验证收据移入
  `<CODEX_HOME>/lean-stack/待删文件`，不再永久 `unlink`。

## 条件性验证

- 迭代阶段先运行最窄、最相关的检查；相关边界未变化时复用已有通过证据。
- 代码、配置、公共行为或高风险边界变化时才做会推动修改的语义复核。
- 最终只运行当前改动和风险确实需要的检查；多个确定性短检查由父代理工具级并发。
- 失败后只修复真实原因，并重跑失败项和受影响项。
- 静态校验、安装成功和子代理自报都不能替代真实运行时证据。
- Windows 上所有 Python 脚本和校验器统一使用 `py -3 -X utf8`；Python 包装子进程时设置
  `PYTHONUTF8=1`，不能依赖 GBK 默认编码读取 UTF-8 `SKILL.md`。

## 经验、SQLite、成本与生命周期侧链

经验体系只保留：

1. SQLite 台账。
2. 每个结果核验采用且未命中明确排除项的不同任务类型组一个保留子代理；多个组可同时保留。
3. UUID 幂等追加的成功存活轮次。
4. 只追加新增经验。
5. 经验数量不封顶。
6. 经验摘要压缩。

复制和变体本身不写入 SQLite。经验维护窗口中，必要时先核对受支持结构，再安全重配同一
保留子代理或只追加经验；未知结构、忙锁、损坏或漂移立即跳过，不阻塞主任务。被否定经验
通过追加纠正事件退出活跃记忆，原事件保留审计。经验完成或跳过后才移除其他子代理。

插件运行时使用已经维护的固定成本预估，不在每个任务重新搜索费率、计算令牌或制作评分表。
每周检查不到期不联网；到期但不能非阻塞核对时延后。插件不创建自动化、系统定时任务或外部
守护进程。

生命周期不是固定顺序、固定数量或后台状态机。`status`、`ensure`、`record-run`、`improve`、
`delete` 和 `restore` 是按需能力；`delete` 保持原来的严格身份与删减资格，但处置改为可恢复退役。已有经验或存活轮次的代理
不能由普通删除命令连带清空审计数据。

## 统一术语

| 英文标识 | 中文用词 |
| --- | --- |
| parent agent | 父代理 |
| subagent | 子代理 |
| main task | 主任务 |
| instance | 子任务 |
| task type | 任务类型 |
| task type group | 任务类型组 |
| retained subagent | 保留子代理 |
| copy | 复制 |
| variant | 变体 |
| experience | 经验 |
| runtime environment | 运行环境 |

英文 `instance` 在中文正文中只翻译为“子任务”。代码标识、模型名、命令、路径和原始错误
使用代码格式。

## 权威路径与代码边界

- 可编辑源码根目录：`C:\Users\fml\plugins\codex-lean-stack`。
- 主技能：`C:\Users\fml\plugins\codex-lean-stack\skills\lean-stack\SKILL.md`。
- 插件清单：`C:\Users\fml\plugins\codex-lean-stack\.codex-plugin\plugin.json`。
- 源码开发基线：`1.2.0+codex.20260825174400`；当前工作树、测试和本文件共同决定现状。
- 个人市场：`C:\Users\fml\.agents\plugins\marketplace.json`，市场名为 `personal`。
- SQLite 台账：`C:\Users\fml\.codex\lean-stack\specialist-memory-v1.sqlite3`。
- 13 条 Mermaid 权威规则内容源：`skills/lean-stack/references/flowcharts-zh.md`。
- 静态 SVG/HTML 由独立插件 `C:\Users\fml\plugins\architecture-viewer` 的
  `$architecture-viewer` 负责；原生交互式 Viewer 由同一插件的 `$archify` 负责。本插件不保存
  第二套 HTML/SVG、渲染器、Viewer Runtime 或 loopback 预览服务。
- 显式安装入口：`skills/lean-stack/scripts/install_plugin.py`；正式安装成功后才幂等确保用户
  已授权的一行全局默认调用指令。它先预检 `AGENTS.md`，并在同一把文件锁内完成安装和
  成功后的单行写入；失败或未授权时不触碰该用户文件。

源码是唯一编辑入口；安装缓存只用于核对，不作为日常修改目标。链路图只是辅助说明，代码、
技能约定、约定一致性测试和真实运行证据才是验收依据。

---

以下部分是每轮都会变化的状态，统一放在恒定规范之后。

## 运行环境状态快照

快照日期：`2026-08-30`。

- 当前源码与已安装版本均为 `2.14.0+codex.20260829171207`。本轮主任务的协作功能阶段曾从
  `2.12.0` 发布到 `2.13.0`；用户随后补充“模型、思考程度、速度按任务类型联合选配”后，
  又只执行了一次功能版本写入和一次普通本地重装，将当前版本提升到 `2.14.0`。当前版本不得
  再次运行版本写入器、刷新 cachebuster 或重复安装。
- 全局 `C:\Users\fml\.codex\AGENTS.md` 重装前后 SHA-256 均为
  `8cda137ba90509d1cf472ab70be176c11b657d1bc5a055564318988f40ae7ee3`。本轮没有授权也没有修改
  该文件；普通重装没有调用显式安装器或写入辅助器。
- SQLite schema 仍为 v3，正式保留子代理和磁盘登记 TOML 均为 12，存活记录 32，原始经验
  24，摘要 0；未登记 `lean_*.toml` 为空。`integrity_check`、`quick_check` 均为 `ok`，外键
  检查为空。
- 同一主任务内的层级前向测试已真实使用 `collaboration.spawn_agent`；整合父代理实际收到
  下游的具体模型、思考程度和速度四行，最终配置抬头一致。父代理工具面不能读取下游自身
  commentary，因此该副本明确记为 `unobservable`，没有用内部消息或最终回复冒充。
- 当前源码工作树包含整条主任务链的未提交修改；全部保留，不得整树回退。当前未提交、未推送、
  未公开发布，也没有重启 ChatGPT/Codex。

### 部件可调用状态

| 部件 | 当前状态与边界 |
| --- | --- |
| Manifest、技能入口、UI 元数据 | 源码版本为 `2.14.0+codex.20260829171207`；manifest 与 `openai.yaml` 的默认入口一致，默认提示为 115 字符。 |
| 显式安装入口 | `install_plugin.py` 校验 manifest、市场名、唯一插件条目与本地源码根；先预检全局 `AGENTS.md`，在同一把锁内调用正式 `codex plugin add`，成功后才幂等写入唯一默认调用行。 |
| 默认保存策略 | 不同职责、主要来源或工具、证据形状形成不同任务类型组；唯一结果核验采用后默认一次 `ensure` 和一次稳定 `event_id` 的 `improve`。同组只留一个，跨组可多个；只有明确排除项才跳过并给保存回执。 |
| 协作父代理与父代理之间 | 默认平面委派；任务卡可把有多个已就绪独立切片的子代理指定为协作父代理，并限制下游范围。当前用户持续授权符合三项原则、服务主任务且边界清楚的父代理跨任务协作；只设一个整合父代理，不再逐次询问。 |
| 普通任务堵点 | 有界多源读取、工具级短命令并发、单一热点写入者、稳定切片窄验证、固定 `BLOCKED`/`SUBTREE_HANDOFF` 和只等待真实依赖共同减少串行等待与重复读取。 |
| 保留子代理台账 | `agents.py` 保留 4 KiB 经验窗口和 16 KiB 完整 TOML 安全边界；`status`、`ensure`、`record-run`、`improve`、`delete`、`restore` 都是按需能力。角色退役不改原资格，合格 TOML 与无令牌收据进入插件专属 `待删文件`。 |
| 固定成本侧链 | `cost_check.py` 只维护七天到期时间，本身不联网、不改插件、不创建自动化；到期后的官方核对仍由父代理在非阻塞窗口执行。 |
| 版本写入器 | `bump_plugin_version.py` 使用完整旧版本 CAS、单次锁和原子替换；联合配置纠正阶段已产生唯一 2.14.0 构建，不得再次运行。 |
| 静态与动态图 | `flowcharts-zh.md` 恰有 13 条 Mermaid 内容；本插件没有第二套 HTML/SVG/渲染器。静态路线调用 `$architecture-viewer`，原生交互路线调用 `$archify`。 |
| SQLite 与正式 TOML | 12 名角色包括三名 Luna、八名 Terra 和一名 Sol；均为标准速度，按职责使用只读或写入权限，且 `include_instructions=false`。SQLite SHA-256 与磁盘一致。 |
| 内部父子消息与用户可见配置 | 新层级路线的内部四行已真实到达父代理，最终配置抬头一致；下游 commentary 本轮不可观察。验收仍只检查真实可见渠道、内容和实际值，不比较 reasoning、读取、分析、工具调用或真实工作的先后。 |
| 个人市场与安装缓存 | 市场名为 `personal`，唯一条目解析到当前源码根；`codex-lean-stack@personal` 已以 2.14.0 安装并启用。最终交接补写前，源码/缓存排除 `.git`、`__pycache__`、`.pyc` 后 30/30 完全匹配。 |

### SQLite 与保留子代理

当前插件正式保留子代理总数为 **12**，原始经验总数为 **24**：

| 名称 | 任务类型 | 联合配置（模型 / 思考程度 / 速度） | 存活轮次 | 经验 | 当前 SHA-256 |
| --- | --- | --- | ---: | ---: | --- |
| `lean_bounded_project_context_reading_343e684b` | 有界项目上下文读取 | Terra / medium / 标准 | 6 | 4 | `42f8c38b52111a8839980295571fd412eeef36d49209a4f97d527e6f8820d03d` |
| `lean_collaboration_safety_pattern_analysis_076a861b` | 协作安全模式分析 | Terra / medium / 标准 | 1 | 1 | `4066f17bc7e4abb1d0ad41e1f277084307879b966a26d9350b0f78742f7c4933` |
| `lean_lean_stack_plugin_consistency_audit_7999edf6` | 精益插件一致性审核 | Luna / medium / 标准 | 10 | 6 | `b070e2049c52402925349f8957820f63a5fa14fc4dde46c8dc724b10efa09d6c` |
| `lean_lean_stack_regression_reproduction_96fd8246` | 精益插件回退复现 | Luna / medium / 标准 | 5 | 2 | `7796437a693e9b6b8e865295cf3400a4dcf3d72b00cda8d202cfec4135c17afd` |
| `lean_lean_stack_runtime_state_audit_780a80cc` | 精益插件运行状态审核 | Luna / medium / 标准 | 3 | 1 | `0ef976b0ea224ea828a72ef97df6db73d260d1ad07269323a4ae3a2aa702a99c` |
| `lean_parent_child_message_runtime_validation_610d2310` | 父子消息运行验证 | Terra / medium / 标准 | 1 | 2 | `50870ca557e126b289c671880786225e01c6bfedfc2a1e04f5d9759af9c99b2d` |
| `lean_plugin_source_cache_migration_audit_fe4c7890` | 插件源码缓存迁移审计 | Terra / medium / 标准 | 0 | 1 | `d133a060f883ca2ed0cba88f67eb79c93ebef35aa680a912b30d037d56d8106b` |
| `lean_portable_release_consistency_audit_4b4ee4cb` | 便携发布一致性审计 | Terra / medium / 标准 | 0 | 1 | `cf37d1137116b501ae20987877a9310eaaaa12fb90d1a64d56e36d6c17a8d5b6` |
| `lean_project_handoff_semantic_audit_2b5208b7` | 项目交接语义复核 | Terra / medium / 标准 | 0 | 1 | `bcd1f7a63a494db0c244be0e38832e666ea3455bfb890e0dc2510f6f44e69a7c` |
| `lean_specialist_lifecycle_safety_implementation_f19dc96e` | 专门代理生命周期安全实现 | Sol / high / 标准 | 1 | 1 | `061fb76b296f3a8f9a0968b0457a74deeefe06987f43a75c959ed82bf8e72b36` |
| `lean_specialist_persistence_implementation_audit_fe71cdbc` | 专门代理持久实现审计 | Terra / medium / 标准 | 4 | 3 | `0b90bc43d462f3bb6f587a1412e08b3a2a1b9ade27bf06f40c037faa769b7107` |
| `lean_subagent_retention_history_audit_6c7b53b3` | 子代理保存率历史审计 | Terra / medium / 标准 | 1 | 1 | `ec0ff19c98737bef65ae1970197502eb45e19eefa5412032674a95d8248fd9f3` |

这里的“任务类型”使用通俗中文展示；`名称` 和 SQLite 中仍保留原始代码标识。配置选择的单位
不是彼此独立的模型列、思考程度列或速度列，而是根据任务类型、工作价值、风险、证据形状、
时延要求和总成本一次确定完整的 `模型 + 思考程度 + 速度` 组合。模型能力会改变所需思考深度，
思考深度会影响时延，快速速度会改变时延与成本，三者必须联合判断，不能按单列分布是否平均
来决定重配。

当前 12 个完整组合中，三组 `Luna / medium / 标准` 用于窄、重复、易核验工作，一组
`Sol / high / 标准` 用于高风险写入实现，方向清楚；八组 `Terra / medium / 标准` 必须分别按
各自任务类型判断，不能因为组合相同就视为同一种选择。其中三名角色尚无已验证存活轮次，
目前只能标为待实际任务证据验证，不能直接降到 Luna、提高思考程度或切换快速速度。以后出现
同类任务时，应比较满足同一成功条件的完整可行组合，用质量、耗时、成本和失败情况决定最终
配置；单轴变体最多是识别因果的可选实验方法，不能取代三者联合选配。

本轮保存回执：

| 任务类型组 | 角色结果 | 经验结果 | 存活轮次 | 说明 |
| --- | --- | --- | --- | --- |
| 有界项目上下文读取 | 复用 | 记录 1 条 | 记录 2 次 | 两个历史任务均读到工具覆盖终点；分页字段限制被保留为证据边界。 |
| 协作安全模式分析 | 创建 | 记录 1 条 | 记录 1 次 | METR 事故材料中的可移植协调模式与不可移植安全禁区形成独立任务类型组。 |
| 精益插件一致性审核 | 复用 | 记录 2 条 | 记录 3 次 | 协作功能初审、改后复核和联合配置改后复核均核验采用并 Done；两次改后复核均为 PASS。 |
| 父子消息运行验证 | 复用 | 记录 1 条 | 记录 1 次 | 第二次层级前向测试完成；第一次因下游配置不具体而只算部分完成，未记录。 |
| 专门代理持久实现审计 | 复用 | 记录 1 条 | 记录 1 次 | 可恢复退役的收据、令牌、CAS、回滚和崩溃窗口建议已采用。 |
| 专门代理生命周期安全实现 | 创建 | 记录 1 条 | 记录 1 次 | 可恢复退役与显式恢复实现通过核验并 Done。 |
| 其他六个任务类型组 | 原样保留 | 原样保留 | 不新增 | 本轮未调用，现有身份、经验和存活轮次保持。 |

## 本轮实际变化

- 完整读取两个用户指定的 Codex 历史任务后，吸收了有界多源读取、单一热点写入者、有限关键
  步骤、独立结果、真实内部消息验收、窄测试和派生产物重建等机制，并保留分页或字段缺口。
- METR 事故调查只作为反例与机制来源：采用明确协调者、去重分派、可交接状态包、受控接管和
  经验证结果再传播；禁止非授权留言板、凭据传播、目标漂移、轨迹伪造、沉默即同意和不可逆
  风险实验。为读取该页安装了 `tavily-cli 0.1.6`；`agent-reach check-update` 因 GitHub API 限流
  未完成，未升级 Agent Reach。
- 新增 `references/collaboration.md`，把平面子代理、同一主任务内协作父代理和其他或新建 Codex
  父代理任务分成三条显式路线；完整任务卡包含下游委派、跨任务范围、写入所有权、成功/停止
  条件、证据和返回格式。父代理跨任务协作使用当前用户持续授权，不再逐次询问。
- 用户进一步明确模型、思考程度和速度不是三个独立分布轴；父代理现在根据任务类型、工作价值、
  风险、证据形状、时延要求和总成本联合选择完整组合，三个字段可因相互制约而同时变化。
  单轴变化只保留为明确因果实验的可选方法，不再是普通变体门槛，也不按单列是否均衡重配。
- `SQLite 与保留子代理` 表的 12 个任务类型和本轮保存回执已改为通俗中文，原始角色代码标识
  继续保留在名称列和 SQLite；配置表头改为“联合配置（模型 / 思考程度 / 速度）”。
- 普通任务堵点改用有界来源复制、工具级并发、单一热点写入者、稳定切片窄验证、固定阻塞包和
  只等待真实依赖疏通；不建立共享中转文件、后台协调器或第二条主线。
- 最近协作授权没有改变删除、删减或候选清理的资格与尺度：原来应删的仍处理，原来不允许的
  仍拒绝。变化只有去向：普通文件进入 Windows 回收站，重要文件进入任务专属 `待删文件`；
  插件角色继续要求身份、所有权令牌、CAS 哈希、直接普通文件、单一硬链接、零经验和零存活轮次。
- `agents.py delete` 不再永久 `unlink` 合格角色，而是把 TOML 与无 `owner_token` 的收据原子移入
  `<CODEX_HOME>/lean-stack/待删文件`；新增 `restore`，重新核对令牌、哈希、身份、路径、冲突和
  文件边界，成功后把收据归档到 `已恢复收据`。普通文件回收站规则没有错误塞入角色台账实现。
- 向后兼容的新行为只运行一次版本写入器，将 `2.12.0+codex.20260829144041` 提升为
  `2.13.0+codex.20260829161119`，随后只执行一次普通本地重装，没有调用全局写入辅助器。
- 联合配置补充同样按向后兼容公共行为处理，只运行一次完整旧版本 CAS，将
  `2.13.0+codex.20260829161119` 提升为 `2.14.0+codex.20260829171207`，随后只执行一次普通
  本地重装；没有调用第二个 cachebuster 工具或全局写入辅助器。

## 本轮验证证据

- 可恢复退役/恢复的 10 项首轮定向测试和实现子代理的完整 `tests.test_agents` 46/46 通过；
  父代理随后核对了关键退役、恢复、冲突与回滚路径。
- 联合配置改动后，`tests.test_agents` 与 `tests.test_skill_contract` 共 88/88 通过；最终
  `unittest discover -s tests -v` 运行 112 项，110 项通过，2 项因 Windows 当前账号没有 symlink
  创建特权而条件跳过，没有失败。独立 Luna 一致性审核员随后定向运行两项联合配置约定测试，
  2/2 通过并给出 PASS；版本写入后两项版本/UI 约定测试也 2/2 通过。
- 官方 `quick_validate.py` 返回 `Skill is valid!`，官方 `validate_plugin.py` 返回插件校验通过；
  全部 Python 文件 `py_compile`、隐藏 Unicode 格式字符检查和 `git diff --check` 通过；格式字符
  数为 0，差异检查只有既有 LF→CRLF 提示。
- SQLite 最终为 `integrity_check=ok`、`quick_check=ok`、外键检查为空、`user_version=3`、
  `agents=12`、`agent_runs=32`、`experience_events=24`、`experience_summaries=0`，未登记 TOML 为空。
- 普通 `codex plugin add codex-lean-stack@personal --json` 对唯一 2.14.0 构建重装成功；
  `codex plugin list --json` 确认 `installed=true`、`enabled=true`、来源为当前源码根。安装后、
  最终交接补写前，源码和缓存均为 30 个文件，无缺失、多余或哈希差异。
- 用户要求的新父代理任务 `01a04e86-4b9a-7fb1-a3c3-3c627c493c3b` 只测试 2.13.0 → 2.14.0
  差异点，没有运行全面测试。它确认联合配置、任务类型中文化、manifest/UI/生成提示、四项定向
  约定测试、官方校验、安装状态和当前源码↔2.14.0 缓存均通过；首次发现本表标题的原始经验
  总数仍写 23，修正为 24 后单点复验 `FIX VERIFIED PASS`。旧 2.13.0 缓存目录已经不存在，
  因此旧缓存↔新缓存逐文件实物对比保持 `UNVERIFIED`，没有用历史材料替代。
- 全局 `AGENTS.md` SHA-256 在重装前后均为
  `8cda137ba90509d1cf472ab70be176c11b657d1bc5a055564318988f40ae7ee3`，本轮没有修改。

## 工作树与剩余边界

- 功能提交 `001b611`（`feat: 发布 2.14.0 协作调度与联合配置`）已推送到 `origin/main`；本段
  交接收口更新作为紧随其后的文档提交同步推送。未修改个人市场 JSON、`config.toml`、模型目录
  或全局 `AGENTS.md`。SQLite 的存活轮次、经验和正式 TOML 更新均由插件现有 CAS 与短事务
  命令完成。
- 最终交接补写后，核对确认源码与已安装缓存只在 `Jiao-Jie.md` 上出现预期哈希差异；动态
  证据不触发第二次重装。安装缓存仍只用于核对，不是编辑目标。
- 没有关闭、重启或重新启动 ChatGPT/Codex，没有创建自动化、后台服务或常驻预览。
- 本轮没有实际执行 Windows 回收站操作。用户可见父代理任务
  `01a04e55-59fd-7c62-abcb-8fff07c84012` 完成的是 2.13.0 旧快照全面测试，不能冒充当前版本验收；
  `01a04e86-4b9a-7fb1-a3c3-3c627c493c3b` 完成的是 2.14.0 差异测试和单点修正复验。新任务仍
  观察到会话注入技能清单指向已不存在的 2.12.0 缓存路径；磁盘实际源码与安装缓存为 2.14.0，
  该运行时清单陈旧现象不能用源码、安装成功或重启声明冒充已修复。
- 文件系统移动与 SQLite 无法组成单一跨资源原子事务；普通异常会按精确字节对称回滚，强制
  终止或断电仍可能在收据、移动、提交和归档之间留下证据完整的中间状态。本轮没有增加 schema
  或后台自动恢复状态机。
- 新安装的技能与脚本由新任务加载，不需要重启。`2.14.0+codex.20260829171207` 的联合配置阶段
  唯一版本写入和唯一普通重装已经完成；不得再次运行
  版本写入器、刷新 cachebuster 或为交接动态证据重复安装。

## 后续代理开始方式

1. 只有新会话开始或上下文压缩后，父代理才亲自完整读取本文件一次；普通后续轮次复用当前
   上下文，不重复全文读取。
2. 检查工作树和用户最新要求，保留所有现有修改；未经用户当次明确同意不得修改
   `C:\Users\fml\.codex\AGENTS.md`。
3. 先判定具体任务类型并确定任务类型组；只有之后才能复用或定制子代理。
4. 确定性工作先用工具；只有需要持续模型判断且有质量或速度收益时才启动子代理。
5. 新任务启动 Luna 子代理前仍核对实际加载目录为 v2；目录不是 v2 或内部工具直接调用失败时，
   需要中途交流的子任务停止并报告缺口，不能用角色 TOML、`ALL_TOOLS`、跨任务消息或更高
   成本冒充。
6. 新生成子代理验收三处行为：父代理收到内部 `agent_message`，子代理自身任务界面的
   commentary 显示完整实际配置，最终回复顶部再次显示。只检查渠道、内容和实际值，不比较
   它们与 reasoning、读取、分析、工具调用或真实工作的具体顺序；静态提示、TOML 和安装成功
   不能替代运行证据。
7. 每个任务类型组结果核验采用后默认一次 `ensure` 和一次稳定 `event_id` 的 `improve`，按组
   给保存回执；只有明确排除项才跳过。启动正式保留身份都传入新的 UUID `run_id`，仅达到
   成功条件、结果核验采用且线程 Done
   后调用一次 `record-run`。
8. 当前已安装并验证版本是 `2.14.0+codex.20260829171207`；源码、正式 TOML、模型目录和运行
   环境未变化时复用本轮通过证据，只运行当前改动可能失败的最窄检查。
9. 每轮结束前更新本文件中“运行环境状态快照”之后的动态章节；删除漂移状态，不把历史快照
   继续写成当前事实。
