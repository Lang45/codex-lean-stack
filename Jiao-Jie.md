# Codex Lean Stack 项目交接

## 根本准则

- 所有父代理调用已安装的 `codex-lean-stack`；实际委派、模型与思考程度的联合选配、上下文、通信、保留状态、生命周期、并行写入、测试链与收口，完全由插件当前规则决定。本交接不形成第二套路由，也不收窄插件的调用判断。`spawn_agent` 没有每次委派的速度、`service_tier` 或 `priority` 参数；具名自定义角色可预配置 `service_tier`，但它不是本次选配轴，也不进入任务卡或可见声明；通用子代理继承会话全局 Fast mode。
- 在本项目中完成的插件优化，功能目标是让任意新项目的新会话自动得到相同效果，包括委派、模型与思考程度联合选配、可见的角色名称/模型/思考程度三行配置、存活轮次/经验状态两行保留状态与生命周期；不得依赖本项目交接、当前会话注入、手工补充提示或直接读取源码。正式安装缓存与全局调用入口是验收这一功能目标的取证边界；只有本项目中的成功不能算插件功能完成。
- 修改 `Jiao-Jie.md` 或全局用户文件本身不算插件功能实现，也不触发插件升版。交接规则只约束当前项目，不能自动成为插件功能；需要成为插件能力的内容，必须有插件内实际消费者、触发动作和可核验证据。
- 用户最新明确要求决定目标与授权。源码、安装缓存、已加载任务和真实运行分别取证；文案、静态检查、任务卡或安装收据不能互相冒充。
- 三项根本原则由插件保持：高价值工作先守住质量和决定性证据；质量达标后比较完整路线总成本；成本相近或都在可接受带时再比较关键路径时间。

## 委派规则的权威映射

本轮已对照当前插件源码。以下类别均有完整的插件承载，因此从本项目交接删除重复执行细则；后续变更只修改对应的插件权威位置，并按其实际消费者验证。

| 类别 | 权威位置 | 当前消费者或可核验证据 |
| --- | --- | --- |
| 调用判断、数量/容量、工具与 PowerShell 路线 | `skills/lean-stack/SKILL.md` 的“一次调用判断”；`references/execution-routing.md` | 父代理按入口与路由执行 |
| 模型、思考程度和最小上下文 | `skills/lean-stack/SKILL.md` 的“选配与上下文”；`references/execution-routing.md` 的一次联合选配 | 父代理原生 `spawn_agent` 的 `model`/`reasoning_effort` 选配与 `MODEL_ROUTE` 收据 |
| 任务卡、可见配置声明、来源覆盖、内部消息和结果采用 | `skills/lean-stack/SKILL.md` 的“委派与收口入口”；`references/delegation.md`、`references/collaboration.md` | 角色名称/模型/思考程度三行配置、存活轮次/经验状态两行保留状态，以及父代理任务卡和真实 `collaboration` 调用 |
| 共享写入、复制与变体 | `references/write-parallelism.md` | 父代理的 `WRITE_ROUTE` 与文件所有权边界 |
| 保留角色、经验、迁移和生命周期 | `references/specialist-memory.md` 与 `skills/lean-stack/scripts/agents.py` | `status`、`recall`、`ensure`、`improve`、`record-run` 和迁移命令 |
| 迭代测试、发布边界与新项目运行验收 | `skills/lean-stack/SKILL.md` 的“按需细则”；`references/execution-routing.md`、`references/versioning.md` | 定向验证与正式缓存后的隔离新项目验收 |

若将来某类别在插件中缺失、弱化，或没有实际消费者，不得仅靠本交接声称已迁移；先在插件中补足并验证，或把明确缺口列为活动事项。

## 项目交接维护

- 本文件是本仓库的父代理跨会话交接，不是隐藏记忆、保留角色经验或插件安装入口。新会话或上下文重置时，父代理先读当前用户要求、此文件的当前快照和与下一动作直接相关的项目事实；再按当前操作读取已安装插件入口与必要引用。普通后续轮次复用未变化的范围。
- 只保留会改变后续动作或能避免已证实错误复发的内容：当前目标和授权、权威来源、关键事实、证据边界、活动缺口、最窄下一步与停止条件。动态状态只保留一个当前快照；已交付事项、旧版本、任务 ID、探针、截图、完整测试流水和思维过程不保留。
- 本项目源码根为 `C:\Users\fml\plugins\codex-lean-stack`；可编辑插件入口为 `skills/lean-stack/SKILL.md`，运行时保留状态实现为 `skills/lean-stack/scripts/agents.py`。源码是日常修改入口；正式安装缓存只用于安装后核对与隔离验收。
- 用户文件 `C:\Users\fml\.codex\AGENTS.md` 仍需当次明确授权才能修改。插件安装辅助器只能处理已获授权的全局调用入口，不能把本项目交接复制成全局或插件规则。
- 每轮有插件代码、配置或公共行为改动且通过必要验证后，依项目 `AGENTS.md` 执行一次升版；本轮仅改交接文件时不升版、不重装、不提交、不推送。全局用户文件的单独修改同样不构成插件改动。
- 本项目已有的持续发布授权允许在插件源码、配置或公共行为完成必要验证后，本轮执行一次升版、提交、推送和重装；仅修改 `Jiao-Jie.md` 或全局用户文件不触发这些动作。

---

## 当前快照与接手入口

当前快照（2026-09-13）。来源为当前插件源码、必要测试、正式安装缓存、保留角色台账和隔离新项目运行收据。

### 已确认事实

- 当前正式发布为 `5.2.3+codex.20260913105553`。任何层级、任何角色的每次 `spawn_agent` 都必须显式传 `model` 与 `reasoning_effort`；具名保留角色从 TOML 取得实际值后仍在原生调用中传入，不能省略为继承。新派发禁止 `fork_turns="all"`，规则递归到下游。
- 每个新当前子任务的第一条可见 commentary 必须以角色名称、模型、思考程度、存活轮次和经验五行开头；保留角色和运行时角色没有例外。Sol `max` 可按任务需要选配；普通 UI、视觉和简单审计只限制 Sol `ultra`，Astra 仍最高 `xhigh` 且是唯一高成本专家路线。
- 新项目或新会话只识别 `lean-simplify` 与 `lean-stack` 两个按需入口，当前动作命中哪个才读哪个；禁止开头通读两份、增加第二段插件前言、预造验收交付物或连续创建同根因探针。全局用户文件已按本轮用户明确要求同步一句式启动规则，未来修改仍需当次授权。
- 28 个现有保留角色已用本版脚本逐个完成 CAS 刷新；身份、模型、思考程度、权限、固定 `service_tier`、81 次成功/0 次失败运行和 73 条有效经验保持不变。每个角色的生成式开场已经核对为严格五行合同。
- 隔离新项目运行确认三次通用派发均显式使用 Terra `high`/`medium`/`high`、`fork_turns=none`，没有 Ultra 或速度参数；另一次启动复验只读取实际命中的 `lean-stack`，一句开场后用一次工具调用完成交接创建、入口读取和当前任务。
- 正式缓存已安装 `5.2.3+codex.20260913105553`；本轮委派、保留角色与公开入口的 145 项合同/单元测试全部通过，安装辅助器返回成功且未改写已存在的全局调用入口。
- 本轮交接已删除项目级的委派数量、时机、模型选择、上下文、通信、任务类型组、保留收口和测试链细则；这些只由上表中的插件权威位置承载。

### 活动事项与最窄下一步

本轮无遗留实现或发布事项。用户讨论的“主任务连续 30 秒没有推进”自动规则尚未采用；后续除非用户明确要求实施，不得把时间审计自行写成计时器、Hook 或后台状态。
