# Codex Lean Stack 项目交接

## 根本准则

三项原则：

1. 高价值工作质量优先，守住正确性；当遇到困难时派遣专家协助。
2. 普通工作可靠性相当后，总成本优先。
3. 总成本处于可接受成本带时，为加快父任务完成速度，增加或并发派遣子代理来节省时间是可取的；


本项目对插件的优化在本会话里依赖本交接和本会话里用户明确要求才能达成，不算最终完成：只有在正式安装新版插件的任意新项目、新会话中自动取得与在本会话里相同实际行为效果时才算完成；
对交接和全局文件的修改不算对插件的优化。


## 未完成要求锚点与当前交付

- 主任务链锚点只指向最早一个尚未完成，或已完成但尚未通过用户可见回复交付的要求。
- 要求完成并在回复中交付后自动退出活动清单，不要求用户再次确认，也不继续纳入后续锚点。
- 纠正只重新打开真正受影响的已交付项；无关完成项保持历史状态。“继续”只恢复剩余活动项，
  不复活已经完成并交付的要求。
- 新要求和对活动项的调整不能挤掉其他尚未完成或尚未交付的事项；取消、停止、放弃或替换只
  作用于用户指定的项。
- 最终说明只报告上一次用户可见交付后新完成但尚未报告的结果，以及当前未完成、受阻或需要
  决定的事项；此前已经交付的完成项不重复总结，除非用户明确要求回顾或当前结论必须引用。
- 这只是会话内轻量清单，不增加数据库、后台队列或持久状态机。交接中的历史记录也不自动
  变成活动要求。

## 主任务链与辅助链入口

主任务链是唯一主线。辅助动作按实际触发进入，完成或安全跳过后立即返回主任务：

- 调查、实现、测试、复核、发布或文档维护等非简单主任务按需使用 `$lean-simplify`。
- 首次准备派发或让现有子代理承担新的相关子任务时，使用 `$lean-stack` 判断是否委派并配置调用。
- 产生修改后按改动与风险运行最窄必要检查；只有新证据、依赖、公共约定或风险范围变化才扩大。
- 只有结果形成跨任务价值且身份与 CAS 能安全核对时，才进入可选的保留身份或经验写入。
- 当前工作属于插件发布时，按授权完成版本、校验、正式重装和源码/缓存核对。

任何辅助动作都不得另立主线、绕过完成条件或拖延已经达到成功条件的交付。

## 用户全局文件的修改范围

- `C:\\Users\\fml\\.codex\\AGENTS.md` 是用户全局文件。修改前必须展示准确增删文字、原因和影响，
  并取得用户对该具体变更的明确同意；内容变化后须重新取得同意。
- 插件规则与实现以本仓库源码为权威。修改、验证和普通重装插件不自动授权修改用户全局文件；
  本轮压缩后重读交接的一行已按用户逐字批准修改，其他变更仍须单独取得同意。
- 打开 Codex 面板、重启应用、外部消息及其他超出当前插件修复范围的动作，仍按用户当前明确授权
  执行，不沿用历史一次性授权。

## 项目级父代理跨会话记忆

- 本文件是 `codex-lean-stack` 项目级、显式、可审计的父代理跨会话记忆；不是模型隐藏记忆，
  也不是 SQLite 中的子代理经验。
- 父代理在新会话开始时亲自读取项目根 `Jiao-Jie.md` 和当前用户要求。工作上下文自动压缩、重置或切换
  上下文窗口后，恢复主任务前的第一项文件动作是重新打开并完整读取当前项目根 `Jiao-Jie.md`；
  压缩摘要、此前读取或子代理转述不能代替。读后首条进展说明写明已读交接、最早尚未完成或尚未
  交付的事项及紧接着的实际动作；文件读取记录及说明与当前快照相符是核验证据。普通后续轮次
  复用已读内容，具名缺口、冲突或来源变化才定向补读。
- 新会话不因进入会话而通读两个入口。非平凡主任务触发已加载的 `skills/lean-simplify/SKILL.md`；
  首次真实派发或启动新 `followup_task` 前触发 `skills/lean-stack/SKILL.md`。只读当前动作命中的入口，
  不预读另一入口或完整 reference；入口未变化时复用，读取后的下一次工具调用推进实际任务。
- 同项目的新父代理由当前上层 `AGENTS.md` 的项目交接规则找到并读取本文件；插件不负责发现、
  创建、读取或更新项目交接。SQLite 只保存当前保留子代理
  身份、明确完成的成功或失败任务结果、经验与摘要；累计第二次明确失败后删除该角色全部资料，
  不另存退役角色，也不另建重复的父代理数据库、后台队列或状态机。

## 首条快速说明

- 首条用户可见说明遵循当前上层 `AGENTS.md`；本交接不再复制其模板。普通后续轮次先简述当前
  理解和立即动作，随后继续。
- 收到纠正、追加要求或状态询问时，首条说明先交代怎样应用新信息，再自动恢复主任务其余工作。
- 能够一次直接答完且不需要工具时，最终答案本身就是首条说明，不额外发送重复前言。不得把
  首条说明写成长计划、模板客套或尚未完成的结果声明。
- 父代理对用户的进展说明不受内部“每个关键步骤一条”或“无异议沉默”限制；同一阶段
  有具体进展就及时说明。最终独立交付必要原因、可观察变化、实际入口、验证对象与剩余事项和限制。

## 当前有效要求与证据边界

以下内容只汇总仍有效且相互不冲突的要求；新要求只在冲突处覆盖旧要求，未冲突的旧要求继续
有效。具体实现细节仍由下方稳定章节和对应源码负责，不在这里重复历次讨论。

### 目标与范围

- 主任务优先。代理管理、经验、成本维护、语义审查、绘图、版本和交接记录都只能服务主任务，
  不能反过来拖延主任务结束。
- 主目标是在一个插件中并行提供“主任务精简”和“子代理调用”：前者收窄主任务步骤与维护面，
  后者优化子代理的选择、协作、复用与收口；工具与 PowerShell 路径精简是两边共同遵守的执行
  底座。两边分别验收，不能反过来拖延主任务。
- 用户只授权当前明确范围；历史的一次性授权、停止或测试配置不自动扩展到新工作。没有用户明确
  要求时，不把一次失败、阶段性停止或助手判断总结成永久、绝对禁止。
- 保护正确性、安全、数据完整性、真实消费者和必要验收。
  不为未决定实现的假想功能增加兼容层、优化器、桩或测试。

### 规则、证据与交接维护

- 新增或修改规则时，写清实际消费者、触发动作和可核验证据；同义规则只留一个权威来源，其他
  入口只作必要摘要或链接。本文件只服务当前仓库，不成为插件规则或跨项目模板。
- 交接只保留能改变接手动作或避免已证实错误复发的内容，并注明来源、适用范围、证据或缺口、
  下一动作或重开条件。用户要求与助手理解分开，源码、静态、安装缓存、运行和交付状态分开。
  文案、测试断言、子代理自报和安装完成不能冒充真实派发、用户可见行为或可采用结果。
- 新要求仅在冲突处覆盖旧要求。只有用户明确要求、决定或授权，才在其范围内写成绝对语句；
  单次失败、阶段性停止和助手推断不扩成永久禁令。用户要求“在现有基础上优化”时，保留不冲突
  的稳定要求，点状处理冲突、失效或重复；结构重写以用户明确要求为准。
- 清理、压缩、迁移或恢复交接和全局规则时，不按主题、关键词或章节整段删除。执行修改的父代理
  须逐条证明待删内容已被用户覆盖、已完成交付，或已由另一权威位置的消费者、触发动作和验证
  完整承载；无法证明时保留。
- 稳定要求按主题合并；动态部分只保留一个注明日期、来源、缺口和最窄下一步的当前快照与接手
  入口。规则、决定、版本、证据、运行或剩余事项变化时收口前直接替换旧状态；无变化不写。删去
  已完成且已交付事项、被覆盖决定、旧截图/探针/任务 ID/版本与测试流水及无续做价值的过程；
  失败路线仅留“情境 → 停止依据 → 重开条件”，不存思维过程、凭据、临时日志或可推导清单。
  未变化的证据可复用，活动验收不得省略。
- 已完成的大规模机制删减不因入口拆分、文档重写或兼容说明而恢复；三款 GPT 6 选配、运行时
  复制与变体、旧状态迁移、所有权/CAS 和 SQLite/TOML 数据保护仍是现行能力。

### 两个并行功能

- 工具与 PowerShell 路径精简由两个入口共同遵守：确定性短工作优先直接工具，独立短命令使用
  一次工具调用并发，复杂转义及时转为任务专属脚本，不增加多余包装或通用执行层。
- `$lean-stack` 是“子代理调用”：负责调用判断与模型、成本、时间选配，上下文、来源和结果复用，
  内部交流与子代理结果收口，角色、经验和生命周期表面精简，
  以及迭代测试链。
- `$lean-simplify` 是“主任务精简”：负责最小完整任务方法，主任务状态和沟通收窄，调查、实现
  和任务手册，测试、复核和重验范围，反过度工程与维护面，显式消融，发布、安装和文档维护，
  以及条件性语义复核与最终验证链、失败后的最窄重验链。
- “精确删除与否定重做”完整归子代理调用。两边共同遵守上述执行底座，并保留用户要求的结果、
  当前授权、安全、权限、数据完整性、无功能回退和与结论匹配的证据，但不复制各自的详细执行规则。

### 调用与选配

- 所有父代理使用本插件。确定性短工作直接用工具；需要持续判断的工作按质量、资源成本、时间
  联合选路，在有实际收益时尽早调用所有已就绪、可独立完成且互不冲突的子代理切片。
- 总成本同时计算父代理继续独立工作的上下文、工具往返和返工，以及子代理的启动、交流、整合
  与核验；不能只把子代理算成新增成本。任务形状、范围或困难变化后，重新判断新出现的切片。
- Luna、Sol 与 Astra 的当前分工和思考程度以现行技能为准；新调用只选三款 GPT 6，思考程度
  从 `medium` 起。Astra 只用于相对最强可行 Sol 有决定性质量收益的当前专家问题，成本只在质量
  充分的组合之间比较；普通视觉或控件布局工作不自动触发 Astra，派出的 Astra 最高为 `xhigh`。
- 当前没有新增独立模型路由器或自动调档脚本，是基于现有工具没有派发前拦截钩子的实施判断，
  不是用户禁令。出现真实消费者、稳定触发点，或现有链路反复偏航且无法纠正时重新评估。
- 插件不另设单会话累计调用次数；总成本处于可接受带且可提速时，可在实际工具、账户和
  运行环境允许的并发容量内多派子代理。

### 上下文、沟通与结果采用

- 新会话或上下文重置读交接；
  reference 按实际操作首次需要时读取。父子代理都使用最小充分上下文，截断只补缺段。
- 父代理及时向用户说明已知事实、已采用动作、实际进展和下一步；这不等于增加无依赖的内部
  消息、固定模板或空洞心跳。
- 子代理的进展说明不是可采用交付；只有明确 final 或等价结果收据才采用。具名缺口
  只做一次明确补问，仍不足就关闭或只处理剩余缺口，不重复收取全文或完整重做可靠结果。

### 保留子代理与经验

- 角色按可复用能力族组织，不因项目、框架、动作名称或配置档位不同而拆得过窄；工具、写权限、
  安全风险或决定性证据不兼容时仍分开，范围扩大不增加权限。
- 只把任务已有明确最终结论的失败记为 `failure`；运行中、中断、未采用、用户停止、工具阻断
  或结果未定不算失败。累计第二次明确失败后移除该角色资料，不另留退役副本或恢复入口。
- 经验只保存可跨任务复用、带适用范围和证据限制的内容；失败教训只留“情境、停止依据、重开
  条件”的短记录，不保存来源项目、长日志或无依据推断。
- 完整保留角色 TOML 不设聚合 KiB 或字节上限；仍保留逐字段的功能、安全、所有权、真实 TOML
  解析与回读边界。

### 验证、说明与发布

- 源码、静态检查、安装缓存、已加载任务和真实运行分别取证。若要证明安装后的新会话行为，
  使用正式缓存和不读取本项目交接的独立新任务；静态文案、显式加载候选或阻断说明不能代替。
- 子代理调用验收使用最小调用探针，不让子代理完整做题。自然任务验证路由选择；强制指定组合
  只验证可调用性和公开协议，不能写成生产路由质量证明。
- 提交前解决仍在活动清单中的问题并完成必要验证；用户取消或留待自然场景的事项退出活动清单，
  同时如实保留尚未覆盖的范围，不把未验证写成已解决。
- 新版本只用一至三条说明概括用户可见变化，并同步 CHANGELOG、README 与 manifest 的可见
  版本信息。已有持续授权只在本插件有改动且验证通过时执行一次升版、提交、推送和重装；无改动
  或同轮已发布时不重复。
- 中文正文使用当前统一术语；代码标识、路径、模型名、命令和原始错误保持原样。全局用户文件、
  应用重启和其他外部动作按用户当前明确要求处理，不沿用已结束的一次性范围。

## 子代理复用

父代理只在决定委派后选择执行者，顺序为：

1. 同一能力族的 live child 仍有相关上下文，且权限和证据边界兼容时，直接用 `followup_task`
   发送新增目标、证据或纠偏。
2. 没有合适 live child 时，复用原生工具目录中配置兼容的 `agent_type`。
3. 两者都没有时，创建满足当前切片的最小运行时子代理。

能力族按职责、输入形状、可验证交付、权限、安全风险和证据要求判断；项目名、框架名、动作词、
中文展示名或模型档位不单独拆分角色。先用原生目录选择候选，不做全表查询；仅在确定复用一个
保留类型后，对该身份执行一次 `recall` 取得五行开场的状态与经验，随即派发；子代理读一遍
已提供的经验便开始任务。召回失败改派运行时子代理，不延后主任务；状态全表、旧经验收据、
成本和身份维护不在派发前，运行时子代理也不以持久化为前置。

## 工具、子代理与调用规则

- 几秒的确定性命令由父代理直接运行；多个独立短命令放入一次工具调用并发。出现多层引号、
  嵌套 JSON 或正则、多行逻辑时，改用任务专属脚本和 `-LiteralPath`，不增加通用包装层。
- 只有切片已就绪、范围清楚、可独立推进且需要持续模型判断时才考虑委派：高价值工作先守
  正确性，普通工作在可靠性相当后比总成本，成本可接受时可增派提速。不要求质量、成本和时间
  各自同时有正收益；插件不设置调用配额，没有任何实际收益时父代理直接完成。
- 模型与思考程度以当前 `skills/lean-stack/SKILL.md` 和 `references/dispatch-start.md` 为准。
  新调用只选三款 GPT 6，思考程度不选 `low`；Astra 的 `xhigh` 是上限，不是默认值。
- 每次 `spawn_agent` 显式传入实际模型、思考程度及非全量 `fork_turns`。复用 live child 时沿用
  已确认配置，不重新选模。
- 父代理派发后继续不重叠的主线，只在真实依赖点汇合。子代理进展说明和内部消息不构成交付；
  采用需要明确 final 或等价结果收据。
- 默认保持平面委派。只有子任务自身还有已就绪独立切片、局部拆分能改善质量或时间，并且权限和
  汇合边界明确时，才把子代理设为协作父代理。
- 同一团队只为会解锁下一动作的发现、纠偏、风险或阻断发送内部消息，不互相扩权或代交结果。
  创建新的 Codex 任务、跨任务消息和其他外部动作仍需当前用户明确要求。

## 子代理开场声明与范围明确的交流

新子任务的任务卡及首条用户可见进展说明以五行实际配置与状态开头：

```text
子代理名称：<用户可理解的名称>（复用或新建，按实际只写其一）
模型：<实际模型>
思考程度：<实际等级>
存活轮次：<已核验轮次或运行时 0 轮>
经验：<已核验记录或未加载保留经验>
```

任务卡直接写明：第一条用户可见进展说明必须原样展示以上五行，最终回复顶部重复前三行配置。
沿用当前子代理或兼容保留身份时写“复用”，创建运行时子代理时写“新建”；
开场前不写计划、父代理模板或内部标识。随后只提供唯一目标、必要来源、写入所有权、成功条件、
停止条件和回复语言。

- 复用保留类型时，父代理对选中的单个身份执行有界 `recall`，用返回的 `opening_status` 原样填写
  后两行；读取失败时改派最小运行时子代理。运行时角色写 0 轮与未加载保留经验，不能编造。
- 新任务卡缺少状态时，由子代理通过父子内部消息报告缺失字段并暂停，父代理补齐或改派；用户
  可见进展不显示占位文字，也不伪造五行。
- 同一任务的 `followup_task` 只发送新增信息并沿用已确认配置，不重复开场。它若启动新的当前
  子任务，则按新任务卡重新提供五行和展示指令。
- 子代理进展说明使用中文“进展说明”；`commentary` 只作为宿主通道名出现在需要精确说明时。
- 内部 `run_id`、SHA、经验摘要和机器身份不进入用户可见开场。宿主未显示子代理进展说明时，
  父代理只能依据真实调用回执说明配置，不能把计划值写成实际结果。

## 同能力族复用与按需保留

- 已决定委派时，优先续用权限、证据边界和实际 GPT 6 配置兼容的当前子代理，再复用已加载的同能力族 `agent_type`；都没有才建运行时子代理。项目名、框架或展示名不同不单独建角色。每个切片仍有自己的范围、成功和停止条件。
- 复制只用于独立同类切片，变体只用于真实且可比较的选配疑问；两者不自动成为新保留身份，也不把变体结果计入原身份。
- 已采用的新能力族有跨任务价值且没有兼容身份时，可无经验、无 `run_id` 单独 `ensure`。已采用的同身份配置胜者可按当前 SHA 与权限边界重配；新配置只在以后新建的线程生效。
- 已核验并采用的保留身份仅在成功产生新跨任务经验时记录成功；无新经验的成功、临时调用及未定结果不记运行。明确失败可不带 `--lesson` 记录失败计数；累计第二次明确失败按脚本资格删除。运行中、中断、用户停止、工具阻断和未采用的结果均非失败。

## 台账写入与按需维护

- **日常派发：**原生目录给出可调用类型；选定保留类型后仅对该身份一次 `recall` 取得真实开场状态。受管 TOML 的子代理提示已含经验，任务卡只传五行状态和本次业务目标，不重复贴经验正文。台账回执不能单独证明原生宿主加载了经验。成本、迁移、摘要和旧经验签发不在派发路径。
- **实际写入：**`agent_ref` 是机器身份，展示名不代入。`agents.py` 执行身份、所有权、权限、CAS、事务、去敏、回读、竞争保护和严格删除；父代理按回执判断结果。只有明确关联某版旧经验参与并发完成时，才需要派发前同身份、同 `run_id`、同摘要的数据库签发收据。原始经验与纠正只追加，被否定经验退出活跃摘要。Windows 受管文件操作持句柄保护；归档目录搬移及 POSIX 非合作写者仍有边界，SQLite 与文件系统没有共同原子事务。
- **失败与版本：**健康结构下直接按需保存新经验，不等摘要纠错或成本检查。写入失败时保留具名未完成写入，先继续主任务，随后修复实际阻断并回读，不能把未保存说成已保存。每次更改正式台账或隔离副本后，核对 `user_version` 与实际源码或缓存的结构版本，用同一实现只读运行 `status --for-dashboard`；正式库另核正式安装缓存。结构不合规须备份并显式迁移，未知或损坏结构拒写，不把读取失败当空台账。
- **成本：**仅在独立维护动作明确触发且不阻塞主任务时核对；每次调用、复用、`ensure` 或保存经验均不查成本状态，不为维护启动自动化或守护进程。

## 权威路径与代码修改范围

- 可编辑源码根目录：`C:\\Users\\fml\\plugins\\codex-lean-stack`。
- 两个快速入口：`skills/lean-stack/SKILL.md` 与 `skills/lean-simplify/SKILL.md`。
- 首次派发、结果汇合、来源收据、写入并行、协作父代理和保留经验分别由
  `dispatch-start.md`、`agent-results.md`、`source-results.md`、`write-parallelism.md`、
  `collaboration.md` 和 `specialist-memory.md` 承载。
- 保留身份与经验实现：`skills/lean-stack/scripts/agents.py`；SQLite 台账位于
  `C:\\Users\\fml\\.codex\\lean-stack\\specialist-memory-v1.sqlite3`。
- 中文链路说明：`skills/lean-stack/references/flowcharts-zh.md`。图的数量由源码中的独立真实
  链路决定；只有用户明确调用流程图时才重新检查，届时按真实链路增、并或删图，不为固定数量
  拆分，也不强制合成一张图。
- 插件清单：`.codex-plugin/plugin.json`；个人市场：
  `C:\\Users\\fml\\.agents\\plugins\\marketplace.json`，市场名为 `personal`。
- `install_plugin.py` 只用于用户已明确授权同步全局调用合同的安装路线；普通正式重装使用插件
  CLI，不修改用户全局文件。

源码是唯一编辑入口；安装缓存只用于核对。链路图、文案、静态测试、安装完成和真实宿主行为分别
取证，不能相互替代。

## 统一术语

| 英文标识 | 中文用词 |
| --- | --- |
| parent agent | 父代理 |
| subagent | 子代理 |
| progress note | 进展说明 |
| live child | 当前可复用子代理 |
| agent type | 子代理类型 |
| retained identity | 保留身份 |
| experience | 经验 |
| runtime copy or variant | 运行时复制或变体 |
| main task | 主任务 |
| subtask | 子任务 |

代码标识、模型名、命令、路径和原始错误使用代码格式。

## 当前快照与接手入口

### 2026-09-24 当前状态

- **来源与目标：**本会话用户要求子代理只用三款 GPT 6 且不选 `low`，旧保留角色同步迁移；优先复用同能力族，五行开场标明“复用／新建”，按需保留新能力族。最近要求对经验、SQLite、成本和生命周期侧链做实际消融并大幅精简，以全局质量、总成本、速度三原则为准；流程图仅在用户明确调用时重查。本轮插件改动已获持续的升版、正式重装和推送授权。全局文件不得再未经逐字同意修改。
- **源码与消融：**源码版 `7.1.0+codex.20260923162212`。隔离消融删除“每次委派必须同时改善三项／必须提速”的旧硬门槛，并把同能力族派发、五行状态、可选保留分成短入口和按需细则；派发任务卡不再重复贴受管 TOML 已含的经验。`dispatch-start.md`、`specialist-memory.md`、入口三文档由 404 缩至 300 行，`cost-baseline.md` 由 168 缩至 63 行。成本脚本整删的候选使现有公开入口悬空，已在隔离实验中撤回；本轮不改流程图。首次 `ensure` 与已有身份按 SHA 重配已分开，旧身份 CAS 不再挡新身份。
- **数据与保护：**正式 v8 台账仍以 28 个活跃身份、97 条经验事件和 1 条摘要为本轮前基线，本轮不写正式台账；28 不是数量上限，隔离 v8 副本曾成功创建第 29 身份。已采用的新能力族可无新经验、无 `run_id` 单独 `ensure`；有新经验或已采用保留身份的明确失败才按需记账。脚本仍执行身份、所有权、权限、CAS、事务、去敏、回读和 Windows 受管文件竞争保护，结构版本错位须在每次台账更改后核对；SQLite 与文件系统之间没有共同原子事务，归档目录搬移及 POSIX 非合作写者有既有边界。普通派发仅在选中已加载保留类型时做一次定向 `recall`，失败改派运行时角色；不做成本、迁移、摘要和旧经验签发预检。
- **验证与边界：**合并后的源码完整测试 221 项运行、3 项按条件跳过；两个技能官方校验与插件校验通过。前版 `7.0.1` 已在独立新任务证明两个正式技能入口可见且可读、一个已加载保留类型的原生调用及五行开场／三行最终输出；该任务曾明确提示检查保留类型，不能证明任意业务任务自然选对当前子代理、类型或模型。新 `7.1.0` 的源码、静态和隔离消融证据也不能单独证明正式新任务的无提示自然路线；v8 收据证明脚本签发与精确绑定，不独立证明原生宿主加载经验。
- **发布核对：**本版正式安装以普通插件命令执行，不能修改用户全局文件或重启应用。接手时核对正式缓存版本、发布文件集合及 SHA，再核对本地与远端提交；若已经一致且无新要求，不重复升版、安装或推送。必要验证与发布回执以实际命令记录为准。

### 接手入口

先判断 `7.1.0` 的正式缓存与远端是否已和源码一致；若不一致，只完成缺少的发布步骤。若上下文再次自动压缩、重置或切换，恢复主任务前完整重读本文件。新版本无提示业务选路仍缺正式独立新任务证据；没有用户明确授权新建任务时，不自行创建该验收任务。
