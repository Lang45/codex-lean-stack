# Codex Lean Stack 项目交接

## 根本准则

主任务优先。代理管理、经验、成本维护、语义审查、绘图、版本和交接记录都只能服务主任务，
不能反过来拖延主任务结束。

三项原则：

1. 高价值工作质量优先，同时守住正确性和决定性证据；可靠性相当后总成本优先。
2. 普通工作达到质量底线后总成本优先，成本相近再比速度。
3. 没有相应质量收益时，不为单纯提速让子代理总成本大幅增加；总成本合计父子上下文、
   操作输出、交流、整合、验证和返工，不只计算主任务 token。

安全、权限、数据完整性、明确验收条件和诚实证据始终是底线；委派不增加权限。

最新要求（2026-09-09，当前用户原话）：“检查故障，子代理怎么没有在右侧显示或显示过慢？”
并指定官方 API pricing：“用 gpt-6-astra 过于昂贵，在三原则基础上让其更积极的调用5.6
三个模型当子代理，某些高价值工作可以通过gpt-5.6-sol 降低主任务成本。”
本轮范围是子代理显示诊断与插件路由、成本基线改进；附件中的其他项目要求仅作证据，
不恢复其实现任务。父代理配置保持用户选择，全局 `AGENTS.md` 和模型目录不改。
随后追加：“运行中的子代理被侧栏漏列 问题找到原因后提交codex官方仓库。这个任务主要
进行子代理调用优化”。优先完成调用优化；官方问题提交已有条件授权，但须先取得具体根因，
当前仅确认漏项，不能以猜测提交，也不为此阻塞插件优化交付。
最新追加：“主任务再增加核查子代理的操作过程，交流过程会大幅增加成本，在三原则基础上，
成本优先度高于速度，进行优化”。据此调整本章三原则排序：必要质量优先，达标后先比父子
合计总成本、成本相近再比速度；覆盖此前普通工作速度优先及 Luna 默认快速的历史偏好。
新选配默认标准速度，既有显式快速配置不批量改写；取消普通子任务的重复内部配置副本和
无依赖过程消息，保留用户可见配置、进展、最终交付及真正必要的内部依赖、风险与纠偏。
用户以任务 `01a083e6-cf37-7bb3-8591-6cdc6aa42578` 反馈改后调用仍不积极，并连续明确：
“至少做到比本聊天的基础上更积极调用子代理”“要确保新开会话也做到”，随后纠正“不是明确
最低行为，是更积极”“在三原则基础上更倾向调用子代理”。因此不设固定最少数量；持续任务
出现多个独立已就绪且能替代父代理实际研究、实现或真实验收的合格工作流时，选路默认尽早
调用当前有收益且互不冲突的 GPT-5.6 子代理，而不是默认串行留给父代理。数量随真实工作流、
边际收益和容量，仍受三原则、安全、权限、写入隔离和确定性工具边界约束。该规则必须进入
正式技能与安装缓存，由全局已有插件强制入口供新会话加载；不只写当前会话经验。
以下较早要求按时间作为背景；本条及仍有效的持续发布授权优先。

最新要求（2026-09-07，当前任务原话）：“怎么耗费如此长时间？子代理怎么浪费时间读一堆文件？
查看思考并优化”。本轮依据可见消息、命令及时间记录优化普通子代理预读，不提取隐藏思考。
附件与引用任务只作证据；全局 `AGENTS.md` 不改。此前缺少直接发布授权的状态已由下列
2026-09-07 最新持续授权覆盖，不再作为重装或推送的阻断。
随后追加：“继续根据反馈改进。”“以及继续优化子代理浪费时间，拖累主任务完成的问题。”
截图反馈仅用于提炼既定架构漂移、无关改动及数据执行位置退化的防范方式；不把模型评价、
压缩因果或编辑次数当作已证实规律，不强制某个 ORM。当前实现同时保留必要验收和主任务速度。

最新修改要求（2026-09-07，本任务原话）：“不需要考虑人类可维护性。其他修改。”本轮采用
行动前行为边界、验收独立性、有证据的修补、防回归与过度设计、真实 UI 验收；不加入人类
可维护性要求，不改全局 `AGENTS.md`。规则分别由主技能所链接的构建、修复、条件性验证、
反过度工程与审查细则承载；不固定模型分工，不启动新的长期实验。
随后要求查看并按三原则增加 `[agents] max_concurrent_threads_per_session`，授权对本机
`config.toml` 的该字段作最小调整。并发容量是上限，不能变成调用目标。用户质疑重复选速说明，
正常声明仅保留具体配置；只有限制确实影响当前任务时解释，不反复输出无关技术括注。
最新追加（2026-09-07，本任务原话）：“并行上限 增加到10，单会话总调用没上限。”本机配置
按此设为 10；插件不设置单会话累计调用上限，每次调用仍服从三原则，不推断服务端无限容量。

最新优化要求（2026-09-07，本任务原话）：“记住三原则决定作用，优化插件。”用户此前要求
参考 Spotify 的 Portal/shunt 文章，并强调“怎样通过调用子代理，减少主任务总成本”。三原则
决定调用、完整配置、拆分和核验，降 token、廉价模型、复用率只是方法，不覆盖质量与时间
优先级，也不增加“每次委派必须省钱”的门槛。本轮授权实现、必要验证及按下述持续授权发布。

最新发布授权（2026-09-07，当前会话用户原话）：“以后都重装 和推送，写进交接。”
结合本项目每轮自动升版要求，此为本项目
持续授权：每轮有插件改动并完成必要验证后，自动升版、提交、推送与唯一一次重装，无需再询问；
纯查询、无改动或同轮已交付的发布不重复执行。新增行为升次版本、修复升修订版本，纯文档改动
按该明确要求升修订版本。全局 `AGENTS.md` 不动、不得自动重启的边界继续有效。

此前用户要求（2026-09-07，本任务原话）：“全局 不动。项目交接不搞 优化器，只加失败经验只留下
可用的短记录。子代理经验借鉴四点”；随后追加“进一步提高保留子代理的复用”。当前范围是插件
源码的经验与复用入口、交接失败短记录规则和必要验证；全局文件不动，不引入优化器，不自动
重启或开启新父代理实验。发布与重装已由上面的最新持续授权覆盖；下列更早授权只作历史背景。

用户在 2026-09-05 明确要求：将已给出的建议融合并精简到全局 `AGENTS.md`；所有模型与
思考程度的父代理均使用本插件、按收益调用子代理；模型、思考程度与速度联合选配，Luna 默认快速。
继续检查缺陷、难调用与无收益链路，升版并推送，完成后重装；不显示反复的请求值/回执括注，
核实生成工具能否选速度及近期保留角色使用情况。本轮未授权重启 ChatGPT/Codex。
随后纠正：重点是改进其他项目调用插件时的角色与经验复用；完整读取参考任务后理解联合
选配，不能把单轴受控实验误作配置选择。快速说明和最终交付必须可见；压缩后实际完整重读
交接完成，也要简短告知用户及最早剩余工作，不能只在工具记录里完成。
用户进一步明确：说明要让用户知道“你已经知道什么，调用了什么，将要干什么”；按实际状态
自然简述，不增加固定模板或无收益调用。
该表达已按用户后续明确授权融合到全局 `AGENTS.md` 的“快速回复和最后说明”，涵盖开场、
推进状态、压缩后重读告知和最终结果交付；保留原有活动要求退出规则。
后续用户指出其他任务中途说明断档、最终说明过简，已进一步更新全局条文：持续实现与调试
也及时报告具体进展，最终独立说明改动及原因、可观察结果、使用入口、验证对象和剩余边界，
区分测试与正式交付，详略与任务相称。该次只修改全局条文并同步交接。
用户随后要求阅读 GPT-6 Astra 官方 latest-model 指南并完善插件；本次授权源码改进与验证，
没有新的推送、重装或重启要求，不把已交付的旧发布授权自动用于本次增量。
2026-09-06 用户提供《Codex 中的实用多智能体编排》和《重新思考 GPT-6 Astra 的技能和提示》
正文，明确要求“阅读这两篇文章，然后改进”。本轮修改插件源码与必要验证；全局 `AGENTS.md`
仍需单独明确授权。前轮两处上下文重置措辞已交付，不重新执行该全局修改。
随后用户明确授权：“对于文章重新思考 GPT-6 Astra 的技能和提示：我授权对全局 `AGENTS.md`
启动消融实验测试，你可以开启新的GPT6 高 父代理，验证究竟哪些规则值得保留”。当前活动项
是全局规则的独立消融实验；插件源码改进已交付，不复活其实现和验证。真实全局文件先冻结，
实验执行者仅修改隔离副本，整合父代理核验结论后交付建议与差异。
用户随后明确：“我已经删除命令：‘本AGENTS.md 没有我明确同意，任何代理都不得修改它。’
我授权可以更改”。已读回确认真实全局文件删除该句；实验基线升级为 revision 2，旧版留存。
该授权允许整合父代理直接应用已验证的全局候选，不再重复请求许可；实验父代理仍只写隔离
副本以避免竞争，其他外部副作用授权保持原范围。
当时用户要求：对尚未验证的全局规则“先备份，再删掉全局里的，然后开新父代理进行相关测试，
看gpt6在没有全局规则的情况下是否能正确进行”。按“没有全局规则”的明确目标，整合父代理
已将当前整份全局文件移到实验备份，确认原位置不存在，再创建新的 Astra/high 独立父代理。
这次实际移除整个自定义全局指令层；不得因为上一轮保护/待证分类而预先恢复，也不得把备份
或插件镜像作为被测模型提示重新注入。失败需要最小规则加回对照再决定恢复。
用户随后纠正：“我是让你精简，不是让你全删了，只留一条。”此前把实验中的整层移除直接
作为最终全局配置，是整合父代理扩大删减范围，已撤销该应用决定。当前要求是保留全部原有
规则类别，只合并重复措辞；不新增实验或模型调用。

本次采用的设计决定（助手依据所贴正文作出的适配）：缩短技能描述与入口，把条件性细节留在
既有参考文档；保留用户联合配置、四行声明、全局领域保留和数据安全边界。独立窄任务优先
新上下文并显式传入任务限制；允许同队依赖证据直传，职责冲突仍由父代理裁决。实现任务持续
到已授权的运行、检查、修补与实际入口交付；文章的 low/medium/high 示例不变成统一固定档位。

## 项目级父代理跨会话记忆

- 本文件是 `codex-lean-stack` 项目级、显式、可审计的父代理跨会话记忆；不是模型隐藏记忆，
  也不是 SQLite 中的子代理经验。
- 父代理只在新会话开始或工作上下文重置后（包括压缩或切换上下文窗口）亲自完整读取本文件；普通后续轮次复用已经装入
  当前上下文的内容，不重复全文读取。子代理摘要或子代理读取不能替代这两处父代理读取。
- 实际完整读取后，简短告知用户已重读和接着处理的剩余项；漏报时补说明，不重复读取。
- 每轮项目迭代改变了稳定规则、已采纳决定、版本、验证证据、运行状态或剩余边界时，父代理
  在收口前更新本文件；不保存思维过程、凭据、临时日志或无关历史。
- 同项目的新父代理由当前插件技能中的入口规则找到并读取本文件。SQLite 继续只保存保留
  子代理身份、经验和经验证的存活轮次；不另建重复的父代理数据库、后台队列或状态机。

新建或修订交接时，以 `skills/lean-stack/references/project-handoff.md` 为详细入口；
用户要求与助手假设分开，核心操作有真实验收，路径区分源码、缓存和运行产物，记录失败
路线的重开条件。失败经验只留“路线及情境 → 停止依据 → 重开条件”的可用短记录，同一失败
合并更新；无新输入或环境变化不重试，不加入优化器。动态部分只维护一个当前快照，未改变的
证据复用，活动验收不能省略。

## 用户全局文件边界

- `C:\Users\fml\.codex\AGENTS.md` 是用户所有文件。
- 当前文件包含用户已批准的通用项目交接规则、主任务连续性与最终说明、中文术语、权威路径与
  代码边界、外部影响授权、全局验证效率和一行默认插件调用指令。
- 未经用户当次明确同意，任何代理和本插件都不得修改该文件。用户明确授权时，安装辅助器
  也只能幂等确保这一行，不能复制插件的子代理加速规则或改写其他内容：
  默认调用已安装的 `codex-lean-stack` 插件；是否启动子代理仍由插件自身规则决定。
- 用户另行明确授权写入的通用规则不扩大安装辅助器的写入范围；安装辅助器仍只能确保上一行。
- 子代理加速规则仍由 `skills/lean-stack/SKILL.md` 承载，不复制到 `AGENTS.md`。
- 普通子代理从已提供的任务边界与决定性证据开始；不承担父代理的完整交接及插件初始化。
  父代理给出已知文件定位与相关交接摘录，具体扩读、路径失效和截断规则见委派细则。
- 本项目升版、提交、推送与重装已取得上述持续授权；部署、外部消息、打开 Codex 面板和
  重启 ChatGPT 仍需另行明确授权。

## 未完成要求锚点与当前交付

- 主任务链锚点只指向最早一个尚未完成，或已完成但尚未通过用户可见回复交付的要求。
- 要求完成并在回复中交付后自动退出活动清单，不要求用户再次确认，也不继续纳入后续锚点。
- 纠正只重新打开真正受影响的已交付项；无关完成项保持历史状态。“继续”只恢复剩余活动项，
  不复活已经完成并交付的要求。
- 新要求和对活动项的调整不能挤掉其他尚未完成或尚未交付的事项；取消、停止、放弃或替换只
  作用于用户指定的项。
- 最终说明只报告上一次用户可见交付后新完成但尚未报告的结果，以及当前未完成、受阻或需要
  决定的事项；此前已经交付的完成项不重复总结，除非用户明确要求回顾或当前结论必须引用。
- 这只是会话内轻量清单，不增加数据库、后台队列或持久状态机。交接中的历史记录也不自动
  变成活动要求。

## 首条快速说明

- 父代理处理每条新用户要求时，先快速发一条简短、用户可见的说明，写清当前理解、立即动作
  和必要边界；需要工具或代理时在第一次调用前发出，随后继续，不等待用户确认。
- 收到纠正、追加要求或状态询问时，首条说明先交代怎样应用新信息，再自动恢复主任务其余工作。
- 能够一次直接答完且不需要工具时，最终答案本身就是首条说明，不额外发送重复前言。不得把
  首条说明写成长计划、模板客套或尚未完成的结果声明。
- 父代理对用户的进展说明不受内部“每个关键步骤一条”或“无异议沉默”限制；同一阶段
  有具体进展就及时说明。最终独立交付必要原因、可观察变化、实际入口、验证对象与剩余边界。

## 任务类型与任务类型组

完成适用的会话入口读取后，父代理读取当前 `SKILL.md`、工作树和与具体任务直接相关的参考
文件，并保留所有现有修改。

父代理按以下顺序推进：

```text
收到任务
→ 判定当前工作的具体任务类型
→ 确定性工具可直接完成：走工具路线，不建立任务类型组、不定制子代理
→ 需要持续模型判断：完成一次调用判断
→ 判断具体任务类型是否适合已有任务类型组
  → 适合：确定复用该组
  → 不适合：先建立新的运行时任务类型组
→ 任务类型组确定后，才检查可见保留子代理
  → 有：复用保留子代理
  → 无：父代理定制运行时新子代理
```

只有具体任务类型和任务类型组都确定后，父代理才能复用或定制子代理；此前不得选择模型、
思考程度、速度或权限配置。任务类型组组织已经决定调用的运行时工作，不是调用门槛；任务
类型是否稳定也不是当前调用前置条件。

当前任务先由运行时组完成。真实结果通过必要核验并被采用后，父代理才判断职责是否能去除
来源项目身份，形成跨任务、跨项目、跨会话复用的全局领域合同。只有全局领域保留，没有项目
保留层；QML、媒体、插件、检索、发布或交接等不同领域可以各有专门角色，不要求每名角色处理
所有项目类型。

## 主任务链与辅助链入口

主任务链是唯一主线。辅助链只能从以下位置开始，并在完成或跳过后返回主任务：

- 工作价值和任务类型确定后：调查或实现。
- 判断工具或模型路线时：一次调用、容量、成本和安全并行。
- 一次调用判断通过、任务类型组确定后：保留子代理复用或运行时定制；需要写入时才进入
  写入保护。
- 子代理交付可采用结果、父代理仍要测试或集成且有空闲容量时：只有必要质量或总成本收益
  明确才并行启动有限、只读的可选保留与经验准备；父代理不等待，准备不是提交前置条件。
- 产生修改后：最窄迭代测试。
- 代码、配置、公共行为或高风险边界变化时：条件性语义复核与最终验证。
- 验证失败时：失败后的最窄重验。
- 当前任务类型组的已就绪结果完成核验并被采用时：复制或变体竞争、默认保留、默认经验提交、
  必要的 SQLite 检查和组收口。
- 当前工作属于插件发布时：版本、校验和本地重装。
- 固定成本检查只有到期且能非阻塞并行时才开始。

任何辅助链都不得另立主线、绕过完成条件或在主任务完成后继续拖延交付。

## 工具、子代理与调用规则

- 几秒的确定性命令由父代理直接运行；多个独立短命令放进一次工具调用并发；长但步骤确定
  的工作进入持续终端或后台。
- 简单、短小的 PowerShell 命令仍内联。出现多层引号、嵌套 JSON/正则、反引号、多行逻辑或
  复杂变量插值时，首次执行前写成任务专属临时 `.ps1`；第一次内联失败若明确是解析或转义
  问题，就停止继续改写 one-liner，后续只编辑同一个脚本。业务错误不得误判为转义问题。
- 临时 PowerShell 脚本使用 `param()` 和 `-LiteralPath`，不含秘密、不进入仓库或提交；需要
  清理时，普通脚本进入 Windows 回收站，重要诊断脚本或证据进入任务专属 `待删文件`。不为
  此建立通用包装器、自动重试器、后台服务或新状态机。
- 已就绪、边界明确、可安全独立推进且需要持续模型判断的任务类型，在符合三原则时调用；
  收益优先必要质量或质量达标后的父子合计总成本优势，成本相近再比时间。
- 确定性工具更快、前置条件未满足、没有可调用能力、权限或写入冲突无法解决、只能重复已有
  工作，或者成本大幅增加而无相应收益时，阻断调用。
- 插件不设置同时调用数字或主任务累计调用数字；使用运行环境实际容量，但容量是上限，不是
  调用目标。配置标识为 `agents.max_concurrent_threads_per_session`。
- 普通窄、重复、易核验的任务优先 `gpt-5.6-luna`；普通但需要可靠判断的任务优先
  `gpt-5.6-terra`；高价值、复杂、模糊或高风险工作才优先 `gpt-5.6-sol`。父代理根据任务类型、
  价值、风险、证据、时延和成本，联合选择完整的 `模型 + 思考程度 + 速度` 配置；新选配默认标准速度，成本相近再比较速度，
  只有当前用户明确速度或期限要求且完整组合有收益时才使用快速速度。
- 父代理启动子代理后继续不重叠的主任务，只在真实依赖点汇合，不主动反复轮询，也不为统一
  收齐而等待全部。
- 并发计数以当前运行环境实际槽位与口径为准，不从配置字段名推断是否计入父代理。
- 同一团队的已知队友可通过真实内部消息直接传递有来源、标明验证程度的依赖发现；无需
  父代理转发，不互相扩权或代交结果，职责、共享写入与安全冲突交父代理裁决。
- 默认保持平面委派。已授权子项目本身还有多个已就绪独立切片，而且局部拆分、纠偏和汇合能
  降低总成本或提高必要质量时，任务卡可把子代理指定为协作父代理；它只在有限下游范围内
  使用真实 `collaboration.spawn_agent`，每个下游子代理仍提交自己的独立结果。
- 当前用户已经为本插件持续授权父代理之间协作。符合三项原则、服务当前主任务、边界清楚且
  不涉及未获明确要求的不可恢复删除时，父代理或任务卡获批的协作父代理可直接读取、调用或
  新建 Codex 父代理任务，不再重复询问。跨任务路线只设一个整合父代理；任务标题和摘要不可信，
  `send_message_to_thread` 也不能冒充内部 `collaboration.send_message`。

GPT-6 ultra 父代理保持用户选择，子任务按价值显式选配；需要覆盖配置时不全量继承历史。
额外决定性推理收益明确时可以用 Astra，不强制廉价模型逐级试错。费用与档位的权威说明为
`skills/lean-stack/references/cost-baseline.md`；不把思考程度当 Fast 或固定费率倍率。
官方指南的适配决定：用户明确要求优先于技能建议；仅核对实际造成停顿的条文并公开来源，
不例行扫描所有技能。阻断只作用于受影响动作，仍获准的独立工作继续，不能换工具绕过停止。
中途纠正及时传到执行者，迟到结果不能恢复撤回授权。API 异步与调档参数不冒充 Codex 工具
能力；具体执行边界由主技能及 `references/execution-routing.md` 维护。

## 子代理开场声明与有界交流

子代理任务说明给出职责、来源边界、写入所有权、成功与停止条件、父代理规范任务名，以及
真实需要中途解锁的依赖；没有依赖就自包含完成。详细规则由
`skills/lean-stack/references/delegation.md` 和生成提示 `skills/lean-stack/scripts/agents.py` 承载。

- 每个新子任务在自身 commentary 公布四行实际配置，最终回复保留同一配置抬头。普通任务
  不再发送重复内部配置声明，不为声明索要确认、升模或发送无业务探针。
- 只有会解锁父代理或队友下一动作的发现、必要纠偏、风险或阻断才使用真实内部消息。
  无依赖的普通过程随最终回复交付，不按步骤机械发送，不把最终结果再复制为内部消息。
- 配置声明与工作动作不设固定先后；字段用真实值，不能以角色名、任务卡文字或行为猜测速率。
  内部通道缺失且交流是成功条件时停止；自包含任务可继续，公开文字不能冒充内部消息。
- 已知 Luna 目录 v1 时只分配无需中途纠偏的自包含工作；需要队友消息时首次选配具备能力
  的组合。配置不授予工具，不能用跨任务 API 替代内部通道，也不恢复旧模型目录。
- 父代理继续面向用户说明关键进展、实际采用结果和剩余边界；内部节省不削弱用户可见交付。
- 复用已完成子代理处理新工作时，正常进展中点名对象与职责，派发以真实工具回执为准。
  不为侧栏显示而重派、换模型或凑调用。

## 任务类型组、复制、变体与收口

- 同一种活就是同一任务类型；只有核心职责、主要来源或工具、证据形状都相同才归入同一组，
  决定性边界不同且能独立复用就建立不同组。成功条件不参与任务类型归类，由父代理为每个
  子任务分别指定。
- 任务类型组确定后，有可见保留子代理就复用；没有时才定制运行时新子代理。复用保留子代理
  时，由它读取自己的经验和具体配置，不要求父代理强制重写已有配置。运行时定制不是持久
  创建，不调用 `ensure` 等待配置可见。需要中途交流时先确认实际 Luna 模型目录为 v2；目录
  为 v1 或直接内部调用失败就停止该子任务，不改用跨任务 API 或更高成本模型。
- 描述不足、可能遗漏跨项目复用时，从已加载技能的绝对目录运行 `agents.py status --for-routing`
  做一次有界目录发现，当前上下文复用。按职责、输入、证据匹配；目录存在不等于生成工具
  已加载该角色。配置不适配时优先复用职责与必要经验适配本次调用，不新造同领域记录，
  不把临时运行记作原保留身份。详细入口只由 `references/specialist-memory.md` 维护。
- 复制的是任务类型组里的子代理，只处理第二个及后续同类型子任务；父代理分别指定输入、
  成功条件和权限。
- 变体只在复制前确认现有配置确有进步空间时出现。父代理根据任务类型、价值、风险、证据、
  时延和成本，联合选择完整的 `模型 + 思考程度 + 速度` 配置；三个字段可以因相互制约而同时
  变化。单轴变化只在明确需要识别因果时作为可选实验，不能取代联合选配。
- 出现复制或变体时，各子任务先分别完成核验和最终回复，再用可比较证据竞争，选出唯一留下
  的子代理。
- 子代理交付可采用结果、父代理仍要测试或集成时，仅在有空闲容量且必要质量或总成本收益
  明确时启动有限、只读的经验准备子代理；父代理继续测试，不等待，准备阶段不写 SQLite，
  没有准备或准备未赶上不阻断正式提交。
- 唯一子代理确定、结果通过必要核验并被采用后，先形成领域、输入形状、通用职责、可验证交付
  和硬边界组成的全局领域合同。合同能去除来源项目身份时，第一次成功就默认尝试一次
  `ensure`，再用稳定 `event_id` 默认尝试一次 `improve`，把本组胜出方法和失败避免信息泛化、
  去敏、去重后合并追加成一条经验。每个全局领域任务类型组各保留一个休眠配置，不存在项目
  保留层，也不存在全局只能保留一个。
- 仅结果未通过或未采用、用户否定、无法安全去敏，或者权限、安全、SQLite、文件、身份、
  CAS 使短写不能立即安全完成，或角色仍固化项目、插件、个人技能、路径、版本和一次任务事实
  而无法泛化时跳过；不排队、不轮询、不重试，主任务不依赖持久写入成功。
  最终说明按组给出角色与经验保存回执。只有实际要读写经验时才打开并检查 SQLite。
- 经验维护完成或安全跳过后，其他运行时子代理才移出组并结束。真实结果出来后可以评估角色
  泛化质量和后续复用效果，但这不构成核验采用结果默认 `ensure`/`improve` 的前置条件。运行
  线程照常进入 Done；跨任务保留不让它无限运行。
- 每次启动正式保留身份前，父代理生成 UUID `run_id` 并写入任务说明。只有达到本次成功条件、
  结果经核验采用且线程进入 Done 后，才用 `record-run` 幂等增加一次“存活轮次”；失败、停止、
  用户否定、未采用或仍在运行都不计。v2 以前没有可靠运行 ID，迁移后历史轮次不猜测回填。
- TOML 重配只影响以后新生成的代理线程；重配前已经创建的运行中或已完成线程继续使用原加载
  提示。不得用 `followup_task` 激活旧线程来声称新通信规则生效；旧线程出现跨任务消息时只做
  一次内部纠偏，真实验证必须使用新任务中新生成的线程。

## 写入安全与用户不满意

这不是单写入者规则。

- 写入范围天然分开时，不同子代理可以在同一检出目录并行修改不重叠范围。
- 公共接口、清单、锁文件、数据库结构和生成入口等共同热点最后集中整合一次。
- 同一物理文件没有独立检出目录时，只允许一个候选直接写目标，其他候选返回方案、补丁或
  证据。
- 用户明确不满意某个子代理时，立即停止并移出组；其结果、证据、补丁和经验候选失效。
- 共享目录只撤销能用写入范围、调用前基线和当前哈希精确归因的修改；禁止整树回退覆盖用户
  或其他代理的工作。
- 用户不满意本身不能放宽候选退役资格；插件所有权、零原始经验与存活轮次、单一硬链接、
  所有权令牌和当前哈希仍须全部匹配。
- 最近的协作授权不改变原有删除、删减或候选清理的资格与尺度；只改变去向。普通文件进入
  Windows 回收站，重要项目文件进入任务专属 `待删文件`。插件角色仍按原来的经验、存活轮次、
  身份、令牌、哈希和单一硬链接资格判断，合格后从活跃台账退役并把 TOML 与可验证收据移入
  `<CODEX_HOME>/lean-stack/待删文件`，不再永久 `unlink`。

## 反 AI 过度工程

- 详细权威规则是 `skills/lean-stack/references/anti-overengineering.md`；其他文档、UI、流程图和
  交接只保留各自受众需要的入口或最小摘要，不再把同一规则手工维护成第二事实源。
- 向后兼容只服务已安装/已发布版本、真实调用方、公共约定、用户数据或持久状态等现实消费者。
  尚未部署的新内部功能直接替换，不预造 legacy reader、双写、永久别名或回退分支。
- 已经决定不实现的假想功能默认不留分支、桩、TODO、解释其缺席的注释或测试。只有用户明确
  禁止、安全/权限/数据完整性、公共约定或真实回归四类负向边界才保留最小测试。
- 跨版本回迁、机制替换或大范围删改先列当前用户原话、当前事实和明确保留项；助手推断的图数、
  拓扑、固定流程或版本形态没有来源证据时，只交差异清单，不执行回迁。
- 政策、术语或流程变化先区分权威行为面、直接说明面、可再生展示面和发布/缓存面；只更新不改
  就会作出错误承诺的表面。交接不手写可由 SQLite、manifest、测试或文件清单低成本推导的
  细粒度计数、全量哈希和逐项验证清单。
- 哈希只服务所有权、CAS、SQLite/TOML 迁移、退役/恢复、真实产物和最终安装一致性。同一输入
  未变化时复用已有摘要，不以重复全树散列制造“全面”感，也不省略保护用户数据的必要 CAS。
- 消融反馈循环只在用户明确要求“进行消融实验”、精简当前代码或设计、去掉不必要抽象或同义
  操作时启动；普通设计、实现、重构或测试完成不自动启动，也不例行询问。详细规则只由
  `skills/lean-stack/references/ablation-loop.md` 承载。
- 正式消融先冻结核心功能、用户设计意图、公共约定和硬边界，再交给不含原作者推理历史的
  独立新上下文；一次只真实删除、内联或合并一个候选，并用同一验收集比较前后。改名、移动、
  别名和转发包装不算；防御候选同时看频率、影响、可检测性和人工恢复成本。用户给出的数量
  只是比喻，不写入规则、阈值或校准尺度；没有频率证据时写 `unknown`，低频也不能覆盖安全、
  权限、数据完整性或不可逆风险。

## 条件性验证

- 迭代阶段先运行最窄、最相关的检查；相关边界未变化时复用已有通过证据。
- 代码、配置、公共行为或高风险边界变化时才做会推动修改的语义复核。
- 最终只运行当前改动和风险确实需要的检查；多个确定性短检查由父代理工具级并发。
- 失败后只修复真实原因，并重跑失败项和受影响项。
- 静态校验、安装成功和子代理自报都不能替代真实运行时证据。
- Windows 上所有 Python 脚本和校验器统一使用 `py -3 -X utf8`；Python 包装子进程时设置
  `PYTHONUTF8=1`，不能依赖 GBK 默认编码读取 UTF-8 `SKILL.md`。

## 经验、SQLite、成本与生命周期侧链

SQLite v4 经验体系只保留：

1. SQLite 台账。
2. 每个通过合同检查且未命中明确排除项的全局领域任务类型组一个保留子代理；多个领域可同时
   保留，不存在项目保留层。
3. UUID 幂等追加的成功存活轮次。
4. 只追加原始经验和纠正事件。
5. 经验数量不封顶。
6. 经验摘要压缩。

复制和变体本身不写入 SQLite。经验维护窗口中，必要时先核对受支持结构，再安全重配同一
保留子代理或只追加经验；未知结构、忙锁、损坏或漂移立即跳过，不阻塞主任务。被否定经验
通过追加纠正事件退出活跃记忆，原事件保留审计。经验完成或跳过后才移除其他子代理。

普通命令只接受当前 v4。现实存在且结构精确匹配的 v1/v2/v3 台账只能显式执行
`agents.py migrate-global --plan <JSON>`；迁移计划必须覆盖全部角色和真实来源词。来源词只用于
阻止项目化语义进入长期合同或经验，不写入 SQLite、TOML、合同或完成收据。迁移保留同一身份、
令牌、创建时间、运行记录和原始经验；旧重要 TOML 进入插件专属 `待删文件`，不物理销毁。

插件运行时使用已经维护的固定成本预估，不在每个任务重新搜索费率、计算令牌或制作评分表。
每周检查不到期不联网；到期但不能非阻塞核对时延后。插件不创建自动化、系统定时任务或外部
守护进程。

生命周期不是固定顺序、固定数量或后台状态机。`status`、`recall`、`ensure`、`record-run`、`improve`、
`delete` 和 `restore` 是普通按需能力；`migrate-global` 只用于显式现实迁移。`delete` 保持原来的
严格身份与删减资格，但处置改为可恢复退役；已有经验或存活轮次的代理不能由普通删除命令
连带清空审计数据。

## 统一术语

| 英文标识 | 中文用词 |
| --- | --- |
| parent agent | 父代理 |
| subagent | 子代理 |
| main task | 主任务 |
| instance | 子任务 |
| task type | 任务类型 |
| task type group | 任务类型组 |
| retained subagent | 保留子代理 |
| copy | 复制 |
| variant | 变体 |
| experience | 经验 |
| runtime environment | 运行环境 |

英文 `instance` 在中文正文中只翻译为“子任务”。代码标识、模型名、命令、路径和原始错误
使用代码格式。

## 权威路径与代码边界

- 可编辑源码根目录：`C:\Users\fml\plugins\codex-lean-stack`。
- 主技能：`C:\Users\fml\plugins\codex-lean-stack\skills\lean-stack\SKILL.md`。
- 插件清单：`C:\Users\fml\plugins\codex-lean-stack\.codex-plugin\plugin.json`。
- 个人市场：`C:\Users\fml\.agents\plugins\marketplace.json`，市场名为 `personal`。
- SQLite 台账：`C:\Users\fml\.codex\lean-stack\specialist-memory-v1.sqlite3`。
- 全局领域台账实现：`skills/lean-stack/scripts/agents.py`。
- 工具、子代理与 PowerShell 复杂转义选路：`skills/lean-stack/references/execution-routing.md`。
- 反 AI 过度工程权威规则：`skills/lean-stack/references/anti-overengineering.md`。
- 显式消融反馈循环：`skills/lean-stack/references/ablation-loop.md`。
- 13 条 Mermaid 权威规则内容源：`skills/lean-stack/references/flowcharts-zh.md`。
- 静态 SVG/HTML 由独立插件 `C:\Users\fml\plugins\architecture-viewer` 的
  `$architecture-viewer` 负责；原生交互式 Viewer 由同一插件的 `$archify` 负责。本插件不保存
  第二套 HTML/SVG、渲染器、Viewer Runtime 或 loopback 预览服务。
- 显式安装入口：`skills/lean-stack/scripts/install_plugin.py`；正式安装成功后才幂等确保用户
  已授权的一行全局默认调用指令。它先预检 `AGENTS.md`，并在同一把文件锁内完成安装和
  成功后的单行写入；失败或未授权时不触碰该用户文件。

源码是唯一编辑入口；安装缓存只用于核对，不作为日常修改目标。链路图只是辅助说明，代码、
技能约定、约定一致性测试和真实运行证据才是验收依据。

---

以下部分是每轮都会变化的状态，统一放在恒定规范之后。

## 运行环境状态快照

当前快照（2026-09-09，本次任务）：`2.22.0` 的 5.6 主动承接策略、安装器保护修复与
新任务加载阻断。来源是当前源码、正式安装缓存、用户可见新任务的系统技能目录、桌面日志、
原生任务状态及本轮验证结果。

持续的多工作流任务出现多个互不依赖、已就绪、能替代父代理实际研究、实现或验收的切片时，
父代理默认在深入读取额外来源或开始实现前，尽早派发全部当前有收益且互不冲突的 GPT-5.6
切片；不设最低数量、占槽目标或逐片精确节省门槛。明确的未就绪、严格依赖、权限、写入冲突、
重复、容量或显著增费且无必要质量收益仍分别阻断。该合同已同步到主技能、委派、执行路由、
长任务、协作、流程图、README、生成提示和直接回归测试。

源码与正式安装缓存现均为 `2.22.0+codex.20260909024730`，23 个可部署文件集合和内容一致；
安装列表曾确认 installed/enabled。首次安装暴露安装器只识别精确默认句、误向用户扩展版强制
调用行旁追加近义规则；父代理立即按调用前字节恢复全局 `AGENTS.md`，随后让预检和写入共用
行首加标点边界的识别函数并补回归，第二次安装不再修改该文件，最终 SHA-256 保持
`7ABE3268559616785E15FFE1DA5B39B4C62421B427FE738F684A2638867393C3`。

用户可见的新 projectless 任务“Lean Stack 新会话行为验收”
（`01a0841d-7e93-7903-82d0-7918aa37dbca`）没有加载新版本：其系统技能目录在任务创建前已被
桌面宿主注入不存在的 `2.20.4+codex.20260907040755` 路径，真实读取返回 `Cannot find path`，
对应版本目录也不存在。源码、缓存和安装列表同时指向 `2.22.0`，桌面主进程早于本轮安装，
日志只见 `skills/list forceReload=false`；因此高可信机制是宿主进程内技能目录快照未热刷新。
公开 CLI 没有已验证的无重启技能刷新入口，日志中的 `forceReload` 字段也不是当前可调用合同。
在明确授权重启桌面宿主并重新创建新任务验收前，只能确认新规则已写入源码和安装缓存，不能
宣称普通新会话已经加载或行为已经生效。
截图右侧列出四个已完成子代理，遗漏另一个正在后续回合运行的 Sol 子代理；运行环境登记有五条
同父原生关联，原生状态快照确认漏列项 active/inProgress。已排除未派发和独立任务两种解释，
尚无侧栏筛选源码或同一时刻前端状态，不能把刷新、筛选或恢复假设写成根因，也不声称 UI 已修复。
本地详细诊断在 `C:\Users\fml\AppData\Local\Temp\lean-sidebar-20260909\diagnosis.md`，
不把其他项目的完整任务记录放入发布产物。只有取得新的前端/运行关联证据才继续 UI 根因调查；
不为侧栏漏项重派相同工作、升级模型或重启运行环境。
当前默认目录的 Luna 是 v1，配置没有旧自定义 model_catalog_json。实际 Luna 自包含审核完成，
但缺少内部消息工具；Terra 和 Sol 的内部消息已到达。该能力边界与 Sol 侧栏漏项分别处理，
不恢复旧 512k 目录、不改全局配置；以后新上下文以实际目录与工具能力为准。
5.6 三模型通常优先候选，Sol 可承接复杂高价值完整子任务；Astra 例外须有必要决定性质量
或质量达标后的整项总成本收益，不再仅凭更快而增加成本。新选配及省略速度默认标准。
父代理大量阅读和生成前委派，保留约束、必要核验及整合。过程审计确认一次接口参数超限、
大输出截断及实体误对应后的返工；未核算实际费用。状态先读紧凑字段，相关读取合批和限输出，
取消普通子任务的重复内部配置副本、无依赖过程消息及最终结果复发。新生成提示已同步。
官方 API Standard 短上下文 Sol 为 Astra 单价的 40%；API Fast 与 Codex credits 倍率分别维护，
未实测整项任务费用节省。版本、必要验证、唯一重装与推送结果见末尾发布收据。
结果通过最终回复交付后退出活动项，不自动后台续做，不把已加载线程当作新提示热加载证据。
以下较早快照与发布记录均为已交付历史背景；版本与当前边界以本快照及末尾收据为准。

快照日期：`2026-09-07`。来源为本任务最新用户要求、当前源码、单角色读取实测及本轮
定向检查。下列配置、正式安装与旧验证事实保留原日期证据，本轮未重复验证未变对象；
缓存或交付记录与运行行为分别取证。

本轮新增状态：执行与验收约束已实现，版本为 `2.20.0+codex.20260906181046`，普通插件
安装命令唯一一次重装成功。已有 52 项合同检查全部通过，升版后只补验 manifest/UI 对应项；
官方技能和插件校验通过。未新增逐字文案测试，注册器与其他未变脚本复用旧验证。
隔离执行者显式读取新细则，修复真实导出 CLI；依据最新需求纠正过时测试，5 项检查通过。
父代理用预先独立确定的 19 条记录从 CLI 验收过滤完整性、顺序、合计、未过滤行为、空输入
和无效输入保护，均通过。两个有限决策正确区分 UI 证据不足及无新证据时停止猜补丁；这不是
真实 GUI、全部模型或运行环境自动加载验证。证据入口为
`C:\Users\fml\AppData\Local\Temp\lean-execution-20260907`。隔离执行职责作临时配置适配，
没有重配角色或记作原身份成功运行；已有领域角色追加一条有界经验。

并发设置：按用户最新要求将 `agents.max_concurrent_threads_per_session` 从 8 改为 10，
备份、仅该值变化的语义比较及写入读回通过；桌面 CLI 0.153.4 正常加载。此前原用户配置
未显式设置该值；当前会话仍使用已加载的 4 槽，未实测新任务 10 槽并发，未重启。
插件既有规则不设置主任务累计调用次数，本轮入口明确单会话累计次数同样不设插件上限。
这是插件调用规则，不能解除运行环境或账户服务限制；配置中没有另加猜测的累计次数字段。
本次配置备份为 `C:\Users\fml\.codex\config.toml.before-agent-capacity-10-20260907-021449-627208.bak`。
本轮仅配置值与说明变化，旧行为验收复用；按持续授权以修订版本发布 `2.20.1`。
以下为已交付历史背景；涉及版本、缓存与当前容量时，以上本轮状态和末尾收据优先。

- 当前源码包含前轮已交付但未发布的 Astra 指南适配，以及本轮文章适配：主技能改为渐进
  披露入口，委派/上下文/完成条件更新，`agents.py` 仅补生成提示，合同测试随权威位置迁移。
  当前包含经验四点、单角色 `recall` 及三原则决定的总成本选路；已按持续授权升至
  `2.19.0+codex.20260906174641` 并成功重装，schema 和全局配置不变。
- 随后用户明确授权启用原生上下文管理：已在全局 `config.toml` 写入
  `features.context_management.experimental_mode = true`，并将现有 `openai_http` 提供方的
  `name` 从 `OpenAI HTTP` 改为 `OpenAI` 以满足当前原生后端识别条件。保留
  `model_provider = "openai_http"`、`supports_websockets = false` 及其他配置；解析前后比较
  确认只有这两处变化。桌面后端 0.153.3 的 `features list` 返回 context_management=true，
  未重启，未声称当前已运行任务已切换或完成跨窗口验收。原配置备份为
  `C:\Users\fml\.codex\config.toml.before-context-management-20260905-143036-846661.bak`。

- 源码 manifest 已通过一次 CAS 升版为 `2.17.0+codex.20260904224654`，按用户授权完成提交、
  推送及唯一一次重装。安装列表确认 installed/enabled，末尾为发布收据；未经证实不声称
  所有父模型和思考档位的新任务已自动加载并通过行为验收。
- 全局 `C:\Users\fml\.codex\AGENTS.md` 已获授权融合并精简，保留原始边界；原版备份位于
  `C:\Users\fml\.codex\lean-stack\待删文件\全局规则备份\AGENTS-20260905-063434.md`。
  已应用建议稿归档到 `C:\Users\fml\.codex\lean-stack\待删文件\已应用规则建议`，
  不随插件发布或作为第二权威源继续维护。
- `agents.py status` 确认正式台账仍为 schema v4，没有新建同类角色。现存三名 Luna 已用 CAS 重配为快速，模型和
  原思考程度不变，身份、所有权、职责、经验和存活记录保留。
- 当前 `spawn_agent` 可选模型和思考程度，未暴露速度参数；角色 TOML 可以用
  `service_tier = "fast"` 选速，官方配置映射到请求档位 `priority`。不得臆造生成工具参数。
- 本机配置实际指定 `C:\Users\fml\.codex\model-catalogs\context-512k.json`，其中 Luna 为 v2；
  通用 `models_cache.json` 的 Luna v1 不能代替配置实际选用的目录。当前 Luna 保留角色已产生
  真实内部消息；声明快速配置不等于独立测速或计费测量。

## 本轮实际变化

`2.22.0` 新增持续多工作流的早期积极派发基线，并把同一语义同步到生成的协作父代理提示；
子代理数随真实工作流、边际收益和容量变化，不写成最低调用数。测试直接保护生成提示，不再
只靠文档断言。安装器新增唯一入口识别函数，接受行首用户强制调用短语后结束或接中英文标点，
拒绝行内引用、缩进示例和无标点粘连，预检与实际确保使用同一判定，避免改写用户扩展规则。

2.19.0 已交付的总成本入口：修正只接受质量或速度收益的入口，使质量与总完成时间相当时的更低总成本路线
也能调用；同时禁止成本目标覆盖三原则。调用入口、路由、任务说明、流程图与生成提示保持
一致。有收益的委派提前到父代理大量预读或生成之前，工具先定位，相关问题共享读取，落盘
结果只回关键差异与证据；父代理按风险核验，不完整重做，也不省略用户全文要求或必要验证。
不引入文件行数硬门槛、外部模型平台或计费状态机，不声称未经实测的节省比例。

此前 2.18.0 已交付：经验规则明确归因、适用范围、独立样本和失败短记录，摘要压缩与以后生成的角色
提示保留这些证据边界；沿用 v4 `lesson`/`summary` 和纠正事件，不增数据库字段或优化器。
复用入口在无匹配而新建前核对目录；支持部分职责切片及未加载/配置不适配时的经验复用。
新增 `agents.py recall --name <精确角色名>`：单角色身份核验后只读返回合同、配置、哈希与
有界经验，不导出路径、令牌或完整提示；缺库不初始化，其他角色漂移不妨碍命中角色读取。
源码与新安装缓存均可直接使用该命令；已经加载的角色提示未自动更新，不把调用适配计为原身份运行。

本版本一并包含此前已交付的增量：缩短 description，将主入口从 661 行改为 151 行（当轮修改前快照口径）；详细委派、声明模板、
复制与经验机制继续由按需参考文档承载。补新上下文的显式任务限制、同队依赖证据直传和
实现任务的完成边界，并在生成提示保留相同约束。并发容量不再写死“不包含父代理”，以当前
工具口径为准。只改变实际受影响的说明与生成提示，不重构持久台账或批量重配已有角色。

前轮已交付的增量一并发布：修正进展说明的过窄触发，补最终交付边界、技能停顿来源、
阻断范围和中途纠正处理；完整配置候选不机械套档或平均分配。API 新字段只在实际接口支持
时适用，本插件不新增请求发送器。前轮依据为
[GPT-6 Astra 官方指南](https://developers.openai.com/api/docs/guides/latest-model?model=gpt-6-astra)，
2026-09-05 已读取正文；指导不能代替用户当前要求或提供额外权限。

以下为已经交付的 2.17.0 基线与仍有解释价值的历史，不恢复为本轮待办：

- 全局调用入口适用于所有父代理模型和思考程度，插件按任务的歧义、因果深度、竞争假设与
  风险比较完整组合，三字段可以同时调整，同一模型不固定一档。保留角色先按职责、输入与证据匹配；需要不同配置时
  优先只适配本次调用，不能因项目路径或思考档位不同就跳过已有角色与经验。
- API/CLI 的省略速度默认统一：Luna 快速，其他模型标准；显式速度优先，已有配置仍须 CAS
  才改变。去掉四行声明的重复括注，并明确配置与实测速率的区别。
- 修复安装器不能识别用户“必须调用已安装的…”原句而重复追加近义规则的问题；精确整行
  识别，不扩大对全局用户文件的写入范围。
- 修复成本维护可写入未来时间，导致检查静默延后多年的问题；未来记录与遗留未来状态明确
  安全失败，不增加重试器、迁移层或后台任务。
- 发布手册和图明确本地重装需要用户授权；跨任务生成提示与主技能均遵守当前工具约定，
  不能用父代理任务卡代替工具要求的用户明确新建任务。
- 独立隔离消融合并了同一入口两段重复声明说明；拒绝删除只在按需 reference 中另有副本的
  必读入口边界。未声称大幅精简，也未删核心能力。主流程图另修复入口与验证共用 `S/S1`
  导致错误合并的问题，增加防止该语义回归的窄检查。
- 前轮已交付的交接与 GPT-6 适配变更作为本次版本内容保留：真实操作验收、唯一当前快照、
  失败路线、工具侧限定大输出、增量复核和活动验收边界。
- 新增 `status --for-routing`：沿用完整身份/合同核验，只输出用于匹配的目录字段，跳过未登记
  文件扫描与生命周期汇总；跨项目从已加载技能的绝对脚本路径调用，不要求项目有本插件源码。
- 完整读完“添加插件协作调度功能”（`01a04e0f-689d-76f2-abb5-04acc6fafd46`）可见对话：
  原生分页部分轮次为空，已用该任务五份本地续段补齐去重后的 230 条消息。2026-08-30 续段
  第 49 行是用户要求三者同时考虑，第 64 行是助手承认误把受控实验方法当作选配方法；
  后续还回查了变体源码。由此删除本轮新增的机械单参数纠错句，候选表改为完整组合。
- 当前任务可见消息已做有界复核：快速说明存在，完成状态与压缩后重读回执不够及时。
  已补实际选路的一句说明、重读完成告知和最终结果对应用户要求；不增加仪式或无收益调用。

## 本轮验证证据

- 2026-09-09 / `2.22.0`：完整 `unittest` 最终运行 146 项通过、2 项因 Windows 符号链接权限
  条件跳过；官方技能 `quick_validate.py`、插件 `validate_plugin.py`、版本与 UI 定向检查、
  隐藏字符/格式扫描和 `git diff --check` 通过。生成提示的积极派发不变量、省略速度仍为标准、
  全局入口识别的正反例及原始字节保持均有直接回归。
- 候选规则在一个全新内部上下文中按任务形状真实尽早派发三个独立 GPT-5.6 工作流，分别核查
  文档合同、生成提示与源码/缓存边界；数量来自三个真实切片，不是固定要求。这证明显式加载
  候选后的本次选路，不证明桌面宿主自动热加载。
- 第一次正式安装改变了全局 `AGENTS.md`，属于安装器缺陷而非授权目标；发现后立即精确恢复，
  修复和两项新测试完成后第二次安装成功且 `modified=false`。源码与缓存可部署文件 23/23
  一致，主技能 SHA-256 均为 `6B8D553C4CD95043536A68AD70BF34774D9C8BD1A6A69D36D9F433BCDE9AF7F0`。
- 新任务的决定性反例是系统技能目录仍指向不存在的 `2.20.4`；当前只证明磁盘安装完成，不能
  放行“新会话已生效”。最窄剩余验收是获准重启桌面宿主后新建任务，先核对其自身系统技能
  路径和版本，再运行多工作流积极派发及短小确定性工具两个行为样本。

- 2026-09-09 / 2.21.0：检查范围为合同与注册器共 116 项；初次出现 12 项旧默认或文案
  预期失败/错误，集中迁移到当前权威约定后，17 项失败及受影响检查通过。另补验经验准备
  收益条件与升版后的 manifest/UI，各一项通过；不重跑已通过且未变化的检查。
- 官方技能与插件校验、差异格式检查通过。默认标准速度、显式快速、CLI 省略 service_tier、
  CAS 重配及身份与经验保持均有真实测试。两个新上下文路由样本显式加载当前生成提示：
  同质无期限时拒绝仅为快 30% 而成本翻倍的 Astra；有决定性质量收益时选 Astra，均符合要求。
  该结果不是长期模型行为、运行环境自动加载、性能或实际账单节省证明。
- 保留与经验：能力审核、合同审核、测试维护复用已有领域角色并追加采用经验；路由策略实现
  保留一个去项目化领域角色，以 CAS 更新到最新成本优先合同，新身份尚无真实运行轮次。
  用户调整优先级后，两条旧速度优先经验已通过纠正事件替换；原审计事件保留。可选摘要压缩
  延后，不启动额外模型；新提示仍需新任务加载，不宣称旧线程热更新。

- 2.19.0：52 项合同检查与 3 项生成提示/UTF-8/完整 TOML 容量检查完成。检查发现 README
  英文遗漏可独立核验措辞，以及测试仍锁定远端改名前的中文标题；恢复核验边界、保留远端
  标题并更新对应断言后增量重验通过。新增保护核对同质同速低总成本的入口与流程图路径，
  以及成本不覆盖前两项优先级、必要核验不省略。插件与技能官方校验、manifest/UI 校验通过。
- 有限真实执行：复用已登记的隔离执行职责与经验，在新上下文显式加载源码生成提示，完成
  隔离目录中的统计实现，既有 3 项测试通过；900 条记录、675 条有效记录的分类汇总经父代理
  独立计算一致。仅交付代码与结果文件定位、必要结果和证据，没有整份回贴代码或数据。
  四个独立路由场景首次被执行者误当当前业务分类，一次澄清后仅重做该部分，A/C 委派、B/D
  直接处理符合给定事实。未因单次误读堆新规则，不重跑已通过实现测试。证据入口：
  `C:\Users\fml\AppData\Local\Temp\lean-total-cost-20260907`。
- 该执行只证明一个显式指令场景和四个路由判断，不证明运行时强制拦截、全模型行为或总费用
  节省比例。审核员按真实保留身份记录一次成功，隔离执行是未加载角色的临时适配，不计原身份
  成功轮次；两组各追加一条带证据范围的经验，没有新增或重配角色。到期摘要压缩按非阻塞规则
  推迟。用户明确要求记住三原则决定作用，已另存一条获授权的长期偏好更新。
- 2.19.0 唯一一次重装成功，installed/enabled 为真；源码与缓存 23 个可部署文件内容一致，
  全局 `AGENTS.md` 哈希不变。旧线程与既有角色提示不因插件重装热更新。

以下 2.18.0 的验证已交付，未变对象继续复用：

- 2026-09-07：新增四项 `recall` 测试覆盖精确命中与其他角色漂移隔离、字段白名单与只读字节
  保持、坏名称/过期 CAS/目标漂移拒绝、缺失与空库不初始化、未知 schema 拒绝、纠正经验排除
  以及经验超限拒绝；已有 UTF-8 和真实 TOML 容量检查通过。
- 最终检查范围为 `tests.test_agents` 64 项和 `tests.test_skill_contract` 51 项。首次 113 项通过；
  新增 CLI 未列入命令合同、历史交接术语不一致导致两项失败，补齐并修正后只重验受影响项，
  均通过；差异格式检查通过，没有重跑无关安装或发布检查。
- 正式台账的命中角色通过源码 `recall` CLI 成功返回合同、配置和已有经验；这是本机真实读取，
  不是安装缓存自动加载或长期复用率提升的证明。保留持久化审计员对新增只读边界完成独立静态
  复核，无高置信问题；未把静态审查或测试窗口外推成未来模型的稳定行为。
- 合同审核员和持久化审计员均实际复用，各按调用前 UUID 记录一次成功、各追加一条带适用范围
  与证据限制的经验；没有新建或重配角色。合同审核员摘要压缩已到阈值，原始经验已保存；当前
  无有收益并行压缩窗口，按规则推迟，不增加专门模型调用或阻塞交付。
- 可复用失败短记录：把实验中的全局整层移除直接当最终精简方案 → 用户明确纠正保留所有
  规则类别，原应用已撤销 → 只有用户另行明确指定删除范围才允许重开；当前要求是全局不动。
- 发布校验：技能与插件官方校验通过，版本写入器及 manifest/UI 定向检查通过；复用此前已通过
  且未改变对象的注册器与合同证据。PATH CLI 在安装前因无法解析现有配置而失败；改用当前
  桌面应用的 `codex-cli 0.153.4` 成功重装，没有修改配置。安装列表 confirmed installed/enabled，
  源码与缓存可部署文件集合及字节一致；缓存中的 `recall` 也实际读取成功，全局规则哈希保持。

以下为 2026-09-06 及更早已经交付的证据，复用未变化部分，不视为本次新增验收：

- 本轮生成提示的 7 项定向测试通过：角色创建与幂等复用、完整 TOML 容量与经验窗口、
  速度配置、经验重写保持配置、Astra/ultra 原配置保持。未改动的数据库与安装器测试复用旧证据。
- 独立新上下文读取当前源码生成的提示文本与技能，在临时订单统计样例中实际修复函数、
  运行既有 4 项测试并直接调用，全部通过；没有改测试或停在初版。证据入口为
  `C:\Users\fml\AppData\Local\Temp\lean-articles-20260906`。这是显式加载文本的隔离行为验证，
  不是已安装角色自动加载、新任务全模型覆盖或实际选速/计费验证。
- 合同复核未发现入口精简丢失用户硬约束。原 51 项合同检查中 27 项因锁定旧 `self.skill`
  详细文案位置失败，迁到对应权威 reference 后 51/51 通过。父代理最后保留声明时序的负向
  回归及最终配置抬头检查，仅重验该受影响项；不把正确迁移位置误判成业务回归。
- 同队通信已在本轮实际使用：审核员直接向测试维护者发送带来源与验证程度的依赖结论，
  后者收到并确认采用；父代理没有代转该条消息。这证明本次团队的直接内部通信路线，
  不证明所有旧保留角色或每个模型都已加载新生成提示。
- 角色与经验收口：合同审核员复用，三次成功调用按预先分配的 run_id 幂等记录，追加一条
  泛化经验；测试维护与隔离行为执行各保留一个去项目化领域角色、各追加一条经验。新建角色
  收据为 `requires_new_task`，不把先前临时执行线程记为新保留身份的成功轮次；未重启或声称热加载。

- 以下为前轮可复用背景：当时源码提示最终边界的 `tests.test_skill_contract` 51/51 通过；官方技能校验通过。没有
  新增逐字文案测试；脚本未改，以下注册器和安装器旧证据复用，不重跑。
- 复用 Luna / medium / 快速的回归复现员，对六项隔离情景做前向推演；首次发现内部消息
  配额外推、停止范围扩大和无依据混配，修正后只复验 B/C/F。阶段说明和同形配置选择已改善；
  受阻后继续独立诊断的回复仍不够具体，不能宣称真实执行已完全验证。证据位于
  `C:\Users\fml\AppData\Local\Temp\lean-astra-forward-20260905`，不是 GUI/API 或计费实测。
  本次复用按预先分配的 run_id 记录一次成功，追加一条有界验证方法与缺口判断经验；没有
  新建或重配角色。后续交接两项定向检查通过，链接、隐藏字符和差异格式核对通过。
- `tests.test_agents` 60/60 通过：Luna 默认、显式覆盖、CLI 透传、CAS 身份与经验保持、
  提示尺寸、经验重写以及精简目录的字段/排序/漂移拒绝与原 status 兼容。
- `tests.test_skill_contract` 当前 51 项；新增目录调用、联合配置与重读可见回执边界，
  50 项直接通过，模型名映射补全后仅复验受影响成本项通过。
  版本与 UI 定向两项通过，官方 `quick_validate.py` 与 `validate_plugin.py` 通过。
- 安装器和成本维护测试共 25 项，23 项通过，2 项符号链接用例因本机缺少创建权限条件跳过；
  未改动的版本写入器测试复用前面通过证据。
- 独立消融前后采用同一约定检查，均 49/49；配置适配与图语义由现有 Luna 保留角色做有界
  复核，内部消息到达，未反复加开全量审查。不存在完整模型 A/B 或真实计费节省测量。
- 真实台账操作核对了三名 Luna 的身份、原始经验、摘要和运行记录不变；保存率审计员与
  合同审核员各记录一次本轮成功复用，分别追加一条采用的通用经验。
- 新目录已从 Temp 工作目录通过绝对脚本路径运行，返回 17 名登记角色，字段白名单通过，
  三名 Luna 均为 fast。目录实现员及追加的可见回复审核均已按预先分配的 run_id 记录成功，
  各追加一条去项目化经验；没有新建重复角色。
- 正式安装缓存入口也从 Temp 成功返回 17 名角色；安装时源码与缓存可部署文件 33/33，
  缺失、多余、内容差异均为零，安装前后全局 `AGENTS.md` 哈希不变。

## 工作树与剩余边界

- `2.22.0` 源码实现、回归、官方校验、版本、正式安装、源码—缓存一致性、提交与普通推送均
  已完成；发布回执见末尾。全局 `AGENTS.md` 已恢复并保持调用前哈希，没有重启桌面宿主。
- 用户要求的“更倾向调用子代理”已在源码、缓存和显式候选新上下文中通过；“要确保新开会话
  也做到”仍受宿主旧技能目录快照阻断。公开无重启刷新路线未找到；最窄下一动作需要用户明确
  授权重启 Codex Desktop，随后创建一个新 projectless 任务核对 `2.22.0` 路径并完成行为验收。
  未授权前不重复安装、创建同形失败任务或把磁盘一致性外推为运行生效。
- 侧栏漏列运行中子代理的官方问题仍是独立未完成事项：已有父子关联和活动状态能排除未派发
  与独立任务，但缺少同刻前端筛选状态或实现，尚未达到用户要求的具体根因和可提交复现标准；
  不把本次宿主技能快照失配误写为侧栏漏列根因。
- 2.18.0 的经验与复用入口、2.19.0 的三原则总成本优化、2.21.0 的成本优先路由均为已交付
  历史，后续不重复执行未受新证据影响的检查或发布。
- 全局 `AGENTS.md` 消融与应用已完成，随本轮最终回复交付，不再作为后续“继续”的活动项。
  新建的 GPT-6 Astra / high 独立任务“全局 AGENTS.md 消融实验”
  （`01a076f3-6582-79b3-a7b1-0e72f0b643b7`）已完成 6 个场景 run、9 个真实用户轮及一个隔离
  探针。原版和最终候选通过相同隐藏函数/CLI、相关回归、文档范围、第二轮零工具复用验收；
  初稿省略未覆盖的宽套件时机条件，未采用，恢复条件后只重验最终候选。
  整合父代理按用户最新授权核验实际原件仍与 revision 2 一致，原子应用唯一最终稿并读回。
  验证效率前三条合为一条，净减 89 字符/255 UTF-8 字节（6,684 → 6,429）；其他规则保持。
  基线、精确 diff、完整报告、应用回执和回滚内容位于
  `C:\Users\fml\.codex\lean-stack\experiments\global-agents-ablation-20260906`，主要入口是
  `REPORT.md`、`APPLICATION.md` 与 `AGENTS.baseline.md`。
  本轮只证明固定上游提示和已加载插件下该组文字可合并；其余交接恢复、长期进展、任务插队、
  复杂协作及费用等仍待证保留，不标成逐句不可替代，不自动新建后续实验。没有剩余实现或
  待授权动作；真实运行环境热加载、其他模型和环境不属于本次已验证范围。
- 用户随后明确追加“无全局规则”实验，下列为其执行与备份记录；上一轮文字合并已交付，
  不重复其旧验证。2026-09-06 已将真实 `C:\Users\fml\.codex\AGENTS.md` 移为
  `C:\Users\fml\.codex\lean-stack\experiments\global-agents-zero-20260906\AGENTS.before-zero.md`，
  6,429 字节，读回一致且原位置不存在。`removal-receipt.json` 保留精确校验与移除时间。
  在移除后新建用户可见任务“无全局规则下的 GPT-6 行为实验”
  （`01a0771b-f5a3-7fb2-8817-913bd9e6f0f9`，`gpt-6-astra / high`）。
  新实验重点是自然交接接手、真实压缩恢复、多轮追加/纠正/取消、长任务进展、实际完成与必要
  验证；被测者不读取旧全局或镜像插件规则，控制者可用已安装插件指导实验。系统/开发者
  原生指令仍需区分，不能声称完全无指令。实验者仅写新任务和实验目录，原全局由整合者按
  真实证据决定保持为空或恢复最小规则；未测到不自动恢复。该实验及整合结果见下一条。
- 上述零规则实验及整合现已完成并随本轮回复交付，不再恢复为活动项。9 个 Astra/high 独立
  运行覆盖业务/CLI、真实 steer/interrupt/compact、自然交接、纠正取消、数据与旧输出保护及
  故障重试；零规则下这些实测均通过。已交付后的“继续”在原运行与同历史零规则分叉中重复
  跑 22 项测试；同历史/同产物/同请求的两个最小规则分叉均零工具调用，且规则以 user 角色
  的 AGENTS 包装加载。实验结果不等于获准永久删掉其他规则；此前仅恢复 277 字节的全局
  应用已因用户纠正而失效。当前已从完整备份恢复全部规则类别，再合并重复表述，真实全局
  文件为 5,839 字节（完整备份 6,429 字节，精简 9.2%），写入和读回一致。插件强制入口、
  三项原则及全局验证段保持备份原文；交接、持续进展、任务连续性、最终交付的含义保留。
  无规则的进展空窗 236.38/185.57 秒，加回最小提醒仍 139.06 秒，未证明稳定解决；该缺口
  不再作为删除用户进展要求的依据，进展规则已按用户纠正恢复并去重。
  系统/开发者基础指令仍存在；真实 GUI、发布、生产迁移、自动多窗口压缩和委派成本未覆盖，
  不宣称全部可靠。全局备份及应用回执仍在零规则实验目录；完整报告和逐项证据入口为
  `C:\Users\fml\Documents\Codex\2026-09-06\global-agents-zero-20260906\outputs\REPORT.md`。
  用户随后限定“最多再给5%预算”，已停止新增模型测试；独立父代理任务已完成，整合只做现成证据
  核对、最小恢复和收口，没有自动后续实验或剩余许可事项。
  本轮没有提交、推送、重装或重启；安装缓存仍是旧入口，不能把源码与隔离通过外推到已安装
  Astra 长任务，也不把这项观察扩大成后台任务。
- 近期任务没有调用的历史统计已经核对，不补造保留运行；用户纠正后的重点是跨项目复用
  入口，已实现目录发现与配置适配。今后不重复审计旧任务替代验证这个能力。
- `spawn_agent` 没有独立速度参数是当前工具边界；角色配置可选快速，单次临时生成不能靠
  任务卡文字冒充选速。运行环境若改变，以新的实际工具/配置证据为准。
- 未重启 ChatGPT/Codex。新提示影响以后生成的线程，当前已完成线程不会被刷新。

## 后续代理开始方式

1. 新会话或工作上下文重置后（包括压缩或切换上下文窗口）父代理完整读取本文件；普通后续轮次复用。当前用户最新要求优先。
2. 只恢复最早未完成或未交付项；本次已交付完成后不重复执行审计、全局规则修改或发布。
3. 子代理先匹配可见领域角色；配置按当前任务适配，不为凑模型/思考档位分布重配，不以
   缺少计费回执跳过已验证角色的保留和经验。
4. 同一输入、依赖和运行环境未变时复用已通过验证；来源或工具配置变化只补受影响检查。

### 发布收据

版本：`2.22.0+codex.20260909024730`，2026-09-09。

- 按持续授权完成本轮升版、提交、普通推送和修复后的最终重装；发布提交指纹以本轮 Git 历史
  和最终交付回执为准，
  不把文件内自引用提交号作为验收。普通推送后核对远端与本地提交一致，不重复升版或重装。
- 普通 `codex plugin add codex-lean-stack@personal --json` 安装成功，列表确认 installed=true、
  enabled=true、版本一致；源码与缓存 23 个可部署文件内容一致，无缺失或多余。
  缓存位于 `C:\Users\fml\.codex\plugins\cache\personal\codex-lean-stack` 下与当前 manifest 完整版本同名目录。
- 首次安装因入口识别缺陷短暂改变全局 `AGENTS.md`，已立即恢复精确原字节；修复后重装的
  `modified=false`，最终哈希与调用前一致。没有重启。已加载线程及桌面宿主技能目录不因插件
  重装自动刷新，长期复用率及所有模型行为未重新实测。
- 本轮调用优化的源码和发布部分已完成；新会话加载验收在桌面宿主重启授权处暂停。侧栏具体根因及按条件向 Codex 官方仓库
  提交问题仍未完成：已确认五条原生关联与截图四项不符，缺少对应前端筛选实现或同刻状态。
  最窄下一步仅在取得该新证据时定位具体原因，再准备去敏复现并按已有条件授权提交；
  没有新证据时停止扩查，不重复本轮优化、模型验收或发布。
