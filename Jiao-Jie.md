# Codex Lean Stack 项目交接

## 根本准则

- 所有父代理调用已安装的 `codex-lean-stack`；实际委派、模型与思考程度的联合选配、上下文、通信、保留状态、生命周期、并行写入、测试链与收口，完全由插件当前规则决定。本交接不形成第二套路由，也不收窄插件的调用判断。保留子代理 TOML 的其他宿主预配置由插件生命周期实现维护，本交接不复制其细则。
- 在本项目中完成的插件优化，功能目标是让任意新项目的新会话自动得到相同效果，包括委派、模型与思考程度联合选配、可见的子代理名称/模型/思考程度三行配置、存活轮次/经验状态两行保留状态与生命周期；不得依赖本项目交接、当前会话注入、手工补充提示或直接读取源码。正式安装缓存与全局调用入口是验收这一功能目标的取证边界；只有本项目中的成功不能算插件功能完成。
- 修改 `Jiao-Jie.md` 或全局用户文件本身不算插件功能实现，也不触发插件升版。交接规则只约束当前项目，不能自动成为插件功能；需要成为插件能力的内容，必须有插件内实际消费者、触发动作和可核验证据。
- 用户最新明确要求决定目标与授权。源码、安装缓存、已加载任务和真实运行分别取证；文案、静态检查、任务卡或安装收据不能互相冒充。
- 三项根本原则由插件保持：模型与思考程度联合达到必要质量；在质量充分的组合中选择与任务相称的成本；委派对父任务实际完成速度有正贡献。它们是新增调用的唯一根判断。
- 调用数量只描述委派拓扑和并发形状，本身不构成成本错误。其他执行细节按各自规则处理，不形成第二套新增调用判断轴。

## 委派规则的权威映射

本轮已对照当前插件源码。以下类别均有完整的插件承载，因此从本项目交接删除重复执行细则；后续变更只修改对应的插件权威位置，并按其实际消费者验证。

| 类别 | 权威位置 | 当前消费者或可核验证据 |
| --- | --- | --- |
| 调用判断、数量/容量、工具与 PowerShell 路线 | `skills/lean-stack/SKILL.md` 的“一次调用判断”；`references/execution-routing.md` | 父代理按入口与路由执行 |
| 模型、思考程度和最小上下文 | `skills/lean-stack/SKILL.md` 的“选配与上下文”；`references/execution-routing.md` 的一次联合选配 | 父代理原生 `spawn_agent` 的 `model`/`reasoning_effort` 选配与 `MODEL_ROUTE` 收据 |
| 任务卡、可见配置声明、来源覆盖、内部消息和结果采用 | `skills/lean-stack/SKILL.md` 的“委派与收口入口”；`references/delegation.md`、`references/collaboration.md` | 子代理名称/模型/思考程度三行配置、存活轮次/经验状态两行保留状态，以及父代理任务卡和真实 `collaboration` 调用 |
| 共享写入、复制与变体 | `references/write-parallelism.md` | 父代理的 `WRITE_ROUTE` 与文件所有权边界 |
| 保留子代理、经验、迁移和生命周期 | `references/specialist-memory.md` 与 `skills/lean-stack/scripts/agents.py` | `status`、`recall`、`ensure`、`complete-run`、兼容命令和迁移命令 |
| 迭代测试、发布边界与新项目运行验收 | `skills/lean-stack/SKILL.md` 的“按需细则”；`references/execution-routing.md`、`references/versioning.md` | 定向验证与正式缓存后的隔离新项目验收 |

若将来某类别在插件中缺失、弱化，或没有实际消费者，不得仅靠本交接声称已迁移；先在插件中补足并验证，或把明确缺口列为活动事项。

## 项目交接维护

- 本文件是本仓库的父代理跨会话交接，不是隐藏记忆、保留子代理经验或插件安装入口。新会话或上下文重置时，父代理先读当前用户要求、此文件的当前快照和与下一动作直接相关的项目事实；再按当前操作读取已安装插件入口与必要引用。普通后续轮次复用未变化的范围。
- 只保留会改变后续动作或能避免已证实错误复发的内容：当前目标和授权、权威来源、关键事实、证据边界、活动缺口、最窄下一步与停止条件。动态状态只保留一个当前快照；已交付事项、旧版本、任务 ID、探针、截图、完整测试流水和思维过程不保留。
- 本项目源码根为 `C:\Users\fml\plugins\codex-lean-stack`；可编辑插件入口为 `skills/lean-stack/SKILL.md`，运行时保留状态实现为 `skills/lean-stack/scripts/agents.py`。源码是日常修改入口；正式安装缓存只用于安装后核对与隔离验收。
- 用户文件 `C:\Users\fml\.codex\AGENTS.md` 仍需当次明确授权才能修改。插件安装辅助器只能处理已获授权的全局调用入口，不能把本项目交接复制成全局或插件规则。
- 每轮有插件代码、配置或公共行为改动且通过必要验证后，依项目 `AGENTS.md` 执行一次升版；本轮仅改交接文件时不升版、不重装、不提交、不推送。全局用户文件的单独修改同样不构成插件改动。
- 本项目已有的持续发布授权允许在插件源码、配置或公共行为完成必要验证后，本轮执行一次升版、提交、推送和重装；仅修改 `Jiao-Jie.md` 或全局用户文件不触发这些动作。

---

## 当前快照与接手入口

当前快照（2026-09-13）。来源为当前插件源码、必要测试、正式安装缓存、保留子代理台账和隔离新项目运行收据。

### 已确认事实

- 当前正式发布为 `5.2.6+codex.20260913125436`。任何层级、任何子代理的每次 `spawn_agent` 都必须显式传 `model` 与 `reasoning_effort`；具名保留子代理从 TOML 取得实际值后仍在原生调用中传入，不能省略为继承。新派发禁止 `fork_turns="all"`，规则递归到下游。
- 每个新当前子任务的第一条可见 commentary 必须以子代理名称、模型、思考程度、存活轮次和经验五行开头；保留子代理和运行时子代理没有例外。Sol `max` 可按任务需要选配；普通 UI、视觉和简单审计只限制 Sol `ultra`，Astra 仍最高 `xhigh` 且是唯一高成本专家路线。
- 新项目或新会话只识别 `lean-simplify` 与 `lean-stack` 两个按需入口，当前动作命中哪个才读哪个；禁止开头通读两份、增加第二段插件前言、预造验收交付物或连续创建同根因探针。全局用户文件已按本轮用户明确要求同步一句式启动规则，未来修改仍需当次授权。
- 当前保留台账有 28 个活动保留子代理、82 次成功、0 次失败和 73 条有效经验。每个子代理的生成式开场已经核对为严格五行合同，公开第一行统一为“子代理名称”。
- 保留子代理复用与新加入采用同一完成链：父代理在派发前生成稳定 UUID `run_id`，新加入先执行一次 `ensure`；采用结果后只执行一次 `complete-run`，在同一事务内登记明确结果与可选经验。经验事件 UUID 由脚本确定；重放幂等，TOML 或数据库失败时整体回滚。
- 隔离新项目任务从正式缓存加载本版：一句开场后立即创建 `Jiao-Jie.md` 与 `result.txt`，没有额外准备链；复用“有界上下文读取员”时显式使用 Terra `medium` 与 `fork_turns=none`，五行开场完整，`MODEL_ROUTE` 只含三项原则，采用结果后正式台账成功登记一次完成结果且未写临时经验。
- `lean-simplify` 已采用可选准备动作的语义触发：父代理已理解目标却准备做不能直接产生可验证主任务结果、也不能解除已命名阻断的准备动作时，立即收窄到第一项实际动作。不计时、不监控、不建立 Hook、脚本、持久状态或新子代理，也不阻断 `lean-stack`。
- 隔离任务首次无经验登记多带 `--origin-term` 时被拒绝且未写台账；源码已消除这个无意义失败分支，无 `--lesson` 时忽略来源词并直接登记结果。相关回归和完整合同/单元测试共 151 项通过。
- 正式缓存已安装本版，源码与缓存各 35 个交付文件逐文件 SHA-256 一致，没有缺失、额外或内容差异。
- 保留子代理 `--speed` 与 `service_tier` 的详细合同只由 `references/specialist-memory.md` 承载；`agents.py` 的创建、重配、迁移、经验重写和状态读取实现保持不变，委派、成本、流程图与本交接只保留生命周期指针。

### 活动事项与最窄下一步

本轮无遗留实现或发布事项。除非源码、输入或用户要求变化，不再重开同一验证路线。
