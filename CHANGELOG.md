# 版本说明

这里只记录每个版本新增或改变的用户可见行为。最新版本置顶；更早且未补录的历史以 Git 提交
和项目交接为准，不在这里恢复成长日志。

## 5.2.0 - 2026-09-13

- 非平凡主任务默认先由 `$lean-simplify` 选择必要步骤和最窄验证，只有选中的步骤需要模型或
  子代理时才进入 `$lean-stack`；两个技能和插件表面共享同一条组合入口，避免只加载一半功能。
- 直接 `spawn_agent`、`followup_task` 和通用 `explorer`/`worker` 的任务卡也必须携带四行配置、
  存活轮次与经验状态，不能假定只有保留角色才需要用户可见开场。
- 测试范围不再由仓库现有测试文件数、函数数、历史全套命令或发布标签决定；合同测试停止读取
  项目 `Jiao-Jie.md`，并移除 README、Mermaid 和交接布局的固定快照。链接逃逸默认测试改用
  确定性元数据，不再因 Windows 创建符号链接权限不足而跳过，运行时安全目标保持不变。

## 5.1.0 - 2026-09-13

- 同一长来源改为一个所有者完成发现和完整读取，其他子代理通过父侧 `SOURCE_ROUTE` 消费可定位
  证据包；只有具名缺口、来源变化、结果冲突或必要的高风险事实才定向独立复核。
- 保留角色固定提示移除父代理拥有的模型路由、成本、生命周期和跨任务编排规则，只保留子代理
  自己要执行的职责；首条回复在四行配置后显示客观的存活轮次和经验状态，不要求判断经验适用性。
- SQLite v6 可把调用前核验过的经验版本摘要与后续明确成功/失败关联，并由 `recall` 显示反馈；
  旧调用保持兼容，v4/v5 只经显式迁移且不回填历史经验复用。

## 5.0.0 - 2026-09-13

- 按用户明确要求将发布版本从 4.3.3 提升为 5.0.0；本次只改变版本身份，不改变双功能归属、调用路线或运行行为。

## 4.3.3 - 2026-09-13

- 修正双功能归属：工具与 PowerShell 路径精简是 `$lean-simplify` 和 `$lean-stack` 共同遵守的执行底座；主任务侧用它收窄实际步骤与包装，子代理调用侧用它先判定直接工具路线。只有模型与子代理的调用选配归 `$lean-stack` 独占。

## 4.3.2 - 2026-09-13

- 一个插件内拆出两个并行、分别验收的功能：`$lean-simplify` 是主任务精简，负责最小完整任务方法、状态与沟通、调查与实现手册、测试/复核/重验范围、反过度工程、显式消融、发布安装和文档维护；`$lean-stack` 负责工具与 PowerShell、调用判断、四模型/成本/时间、上下文与结果复用、内部交流、迭代测试、角色经验和生命周期。条件性语义复核与最终验证、失败后的最窄重验归前者；精确删除与否定重做归后者。
- 恢复早期最小完整任务阶梯，并禁止双入口拆分恢复历史上已经删除的大规模中间机制；两边继续保留当前授权、安全、权限、数据完整性、无功能回退和相称证据，正式消融仍只在用户明确要求时启动。
- 同一卷内只改变路径的移动不再生成迁移前后整树逐文件长度与 SHA-256 清单；实际字节复制、正式发布/安装清单、安全完整性和明确逐字节核验仍按声明范围验证。安装后子代理调用测试只核对真实启动事件、模型与思考程度，随后立即中断，不等待子代理完整做题，也不外推为业务质量证明。

## 4.3.1 - 2026-09-13

- 移除插件对项目交接文件存在、名称、创建、读取、更新和结构的全局要求；这些行为只由当前用户与项目自身规则决定。
- 删除 `project-handoff.md`，并清理技能入口、流程图、公开说明和合同测试中的交接生命周期依赖；四模型路由、`MODEL_ROUTE`、`WRITE_ROUTE` 及既有全局安全能力保持不变。

## 4.3.0 - 2026-09-12

- 两个或更多子代理将修改文件或共享状态时，父代理在首次可写派发前记录 `WRITE_ROUTE`，明确写入者、范围、共同热点、隔离路线、撤销依据和受影响检查；不能隔离或精确归因时退回单一直接写入者。
- 写入安全、用户否定、反 AI 过度工程、消融和条件性验证现在由插件全局合同统一承载；项目交接只保留项目特有状态、例外、证据、缺口与权威入口，不再复制完整规则章节。

## 4.2.0 - 2026-09-12

- 路由优先顺序统一为质量、成本、时间，资源成本不再重复计算墙钟时长；处于可接受成本带的少量低成本并行可以换取更短关键路径。父代理以 `MODEL_ROUTE` 记录四模型选择、质量底线、低成本替代路线及其差距。
- `gpt-6-astra` 只处理 GPT-5.6 存在决定性质量差距或能降低整项资源成本的当前专家切片；普通视觉任务不触发，也不要求低价模型先失败。派出的 Astra 仍最高为 `xhigh`。子代理第一条可见回复必须以本地化名称、模型、思考程度和速度四行开头，前面不出现计划或运行 ID，且不回显父代理专用 `run_id`。
- 完整保留角色 TOML 不再设置 KiB 总大小上限；单项角色说明、活动记忆、迁移计划和受管文件数量等逐项边界，以及所有权核验和真实 TOML 往返仍保留。规则收束为一个权威消费者、明确触发和可观察证据，避免规则增长却无法执行。

## 4.1.0 - 2026-09-11

- 派出的 `gpt-6-astra` 子代理思考程度上限从 `high` 放宽为 `xhigh`；原有较低档位继续有效，
  `max` 和 `ultra` 仍由保留角色生成入口在任何文件写入前拒绝。
- 这项兼容性放宽不改变 Sol 父代理的专家协助触发条件，也不限制用户显式选择的 Astra 父任务
  配置；新会话探针仍以真实启动事件为准，命中后立即停止而不等待子代理业务结果。

## 4.0.0 - 2026-09-11

- 派出的 `gpt-6-astra` 子代理思考程度现在最高为 `high`；`xhigh`、`max` 和 `ultra` 配置会由
  保留角色生成入口在任何文件写入前拒绝，避免运行任务继承父代理的更高思考档。
- Sol 父代理在具体专家难题下调用范围明确 Astra 子任务的能力保持；这项上限只限制派出的
  Astra 子代理，不擅自改动用户显式选择的 Astra 父任务配置或 GPT-5.6 子任务选配。
- 新会话路由探针达到“父代理已发出预期模型与思考程度的子代理启动事件”后立即停止，不再为
  验证派发决定等待子代理完成无关业务。

## 3.4.0 - 2026-09-11

- 合格低成本 GPT-5.6 切片现在依据维护后的模型价差和完整父子反事实路线直接取得正向总成本
  判断，并在昂贵父代理继续大块工作前尽早派发；确定性工具、依赖、冲突、权限和容量等内部
  硬性限制不再给父代理增加面向用户逐项解释的负担。
- “安装后新会话生效”现在必须由全新项目中的独立父任务真实派发并取得可采用结果来证明；
  静态关键词、当前会话手工加载候选文本或硬阻断说明不能替代行为验收。
- 中文说明、生成提示、测试和全局 `AGENTS.md` 已统一使用“规则”“范围”“程度”或具体限制；
  Python/JSON/SQLite/TOML 标识、英文 API、路径、命令与原始错误保持不变。

## 3.3.0 - 2026-09-10

- 新版本推送前同步更新 README、插件清单中的用户可见说明和本文件，并用版本一致性检查防止
  版本号已变化而说明仍停留在旧版。
- 插件说明现在直接概括当前路由的三项主要变化：直接处理决定只对当前任务形状有效、路线总
  成本包含父代理继续独立工作的 token/时间/返工，以及具体困难出现时及时调用范围明确的专家。

## 3.2.0 - 2026-09-10

- 防止昂贵父代理把一次“直接处理”决定沿用到整个长任务。
- 把父代理继续独立工作的 token、时间和返工也计入路线总成本。
- 遇到已经暴露出来、适合独立处理的困难时，及时调用有针对性的专家，而不是继续由父代理
  长时间试探。
