# 版本说明

这里只记录每个版本新增或改变的用户可见行为。最新版本置顶；更早且未补录的历史以 Git 提交
和项目交接为准，不在这里恢复成长日志。

## 5.2.14 - 2026-09-17

- 修复显式 Codex home 未传给安装子进程、有效 override 被忽略，以及代码示例误报默认激活；
  保留原文件内容、编码、换行与安装失败后的零写入边界。
- 修复长 UTF-8 经验导致 recall/路由目录不可用、摘要预算漏算标签，以及经验写入异常后的
  SQLite/TOML 不一致，并串行保护成本检查状态的比较写入、清理失败的首次写入；原始经验、纠正、旧格式迁移、运行记录和清理能力全部保留。
- 修复数据库链接与持久输入校验缺口、两处失效文档锚点和过时的宿主参数说明；增加跨平台
  回归与 Windows 原生参数探针，不改变双入口、四模型选配或非全量上下文约束。

## 5.2.13 - 2026-09-16

- 将 Windows exec 的低转义风险执行合同纳入插件的共享执行路由：简单命令直执行，复杂或跨语言
  代码落盘为脚本，原生程序分离可执行文件、参数和数据，并检查 Parser 预检、实际结果与退出码。
- `$lean-stack` 与 `$lean-simplify` 入口都指向同一份插件内详细规则；不写入全局 `AGENTS.md`，
  不增加额外 Shell、包装器或自动重试层。

## 5.2.12 - 2026-09-16

- 新会话分别完整返回两个 Lean 入口并为外层工具结果预留完整预算，不再把入口、交接和源码串成
  一个容易截断的 shell 输出；无明确截断时不重读，明确截断时只补缺段，并同时开始首个确定性动作。
- 两个入口的默认预读提示改为短路由；三原则、显式模型与思考程度、有限上下文、五行开场和
  `ensure → complete-run` 收口仍由入口正文及按需 reference 完整承载。

## 5.2.11 - 2026-09-15

- 新会话在同一次启动热路径中快速实际读取已加载的 `lean-stack` 与 `lean-simplify` 两个入口，
  不预读引用细则；读取后两者继续独立或并行触发，入口未变化时后续轮次直接复用；安装器会把
  新全局文件中“所有父代理必须都使用插件”的展开句式识别为已配置，不再追加旧式入口。
- 保留子代理的固定身份、模型和思考程度只约束该保留身份，不再经继承开发指令覆盖下游任务卡；
  子孙级开场五行与最终三行均使用自己的实际任务卡值。
- 新运行时组的首次 `ensure` 若明确要求重配或合同刷新，可用返回 SHA 对同一已采用配置作一次
  CAS 确认；锁、CAS、文件或结构失败仍立即跳过，不排队、不轮询、不反复重试。

## 5.2.10 - 2026-09-15

- 安装插件的所有项目和新会话保持主动委派；三项原则成立时可为父任务完成速度同时派发多个
  子代理。主任务启动句不再覆盖子代理五行开场，中文会话的可见名称不能照抄技术 `task_name`。
- 普通派发只读取一次 `lean-stack` 入口；`status --for-routing` 以只读方式一次返回全部轻量候选，
  命中后才 `recall` 完整合同，不再以前置方式通读参考文档或初始化缺失台账。
- 保留子代理新加入统一走 `ensure → complete-run`，复用直接走 `complete-run`；采用结果后必须
  完成该收据或说明明确跳过原因，`improve` 不再被误当成完成入口。

## 5.2.9 - 2026-09-14

- 安装器现在把合并后的“所有模型和思考程度的父代理必须使用已安装插件”全局规则识别为现有
  调用入口，不再补写旧默认句造成重复规则。

## 5.2.8 - 2026-09-14

- 将每次原生派发收敛为调用时硬门：`spawn_agent` 必须同时提交显式模型、思考程度、有限上下文和
  五行任务卡；缺字段、使用“继承”占位或给普通 UI 选择 Sol `ultra` 时不得调用。该硬门置于
  插件公开提示最前部，避免长任务在首次读取入口后又回退到父代理配置。
- 新增公开提示顺序合同，确保新项目加载的正式缓存先看到派发硬门，再读取其余选配细则。

## 5.2.7 - 2026-09-14

- 新项目和新会话首次准备派发前，父代理必须实际读取已加载的 `lean-stack` 入口；技能目录中
  “已识别”或口头声明采用不再算调用。该触发前移到技能描述，并同步正式入口提示。
- 撤销 5.2.2 将 `task_id` 扩大成所有派发前置条件和去重器的错误声明；它只保留为协作子树任务卡
  字段。交接与全局规则增加逐条语义保全条件，禁止用关键词消失、文件映射或格式检查证明迁移。

## 5.2.6 - 2026-09-13

- 保留子代理的 `--speed` 与 `service_tier` 配置合同集中到生命周期权威文档；创建、重配、迁移、
  经验重写、状态读取和实际 TOML 生成能力保持不变，并由原有行为测试继续保护。
- 模型与思考程度的委派热路径、成本基线、流程图和项目交接只保留生命周期指针，不再重复维护
  速度配置细则或把它混入 `MODEL_ROUTE`。

## 5.2.5 - 2026-09-13

- 委派根判断收敛为三项原则：模型与思考程度联合达到必要质量、质量充分组合的成本与任务相称、
  委派对父任务实际完成速度有正贡献。调用数量本身不构成成本错误，其他执行细节不形成第二套
  新增调用判断轴。
- `lean-simplify` 增加可选准备动作的语义触发：已经理解目标却准备做不能直接产生可验证主任务
  结果、也不能解除已命名阻断的准备动作时，立即收窄到第一项实际动作；不计时、不监控，也不
  建立 Hook、脚本、持久状态或额外子代理。
- 保留子代理复用与新加入统一使用稳定 `run_id` 和单次 `complete-run`；明确结果与可选经验在
  同一 SQLite 事务中提交，TOML 写入或事务失败时精确回滚，重放保持幂等。无新增经验时，即使
  收到多余的 `--origin-term` 也只登记结果，避免无意义的失败与第二次命令调用。
- 当前公开说明统一使用“子代理”，并保持所有层级的新当前子任务以名称、模型、思考程度、存活
  轮次和经验五行开场。

## 5.2.4 - 2026-09-13

- 子代理开场的公开字段统一为“子代理名称、模型、思考程度”，并同步刷新现有保留子代理，防止
  新项目继续显示旧称。
- 成本判断改为核算每个新增调用替代的父代理工作、质量或关键路径收益与完整增量成本；调用数量
  只描述拓扑和并发，不再被直接判为成本错误。
- 保留经验的 `event_id` 明确要求稳定 UUID；命令帮助说明省略时仅为无需重放的新事件生成随机
  UUID，避免创建成功后经验写入因非 UUID 标识而断链。

## 5.2.3 - 2026-09-13

- 所有层级和所有子代理的每次 `spawn_agent` 都显式传入模型与思考程度；保留子代理的 TOML 只提供
  实际值，不能代替原生调用参数。禁止会强制继承配置的 `fork_turns="all"`，下游委派同样适用。
- 每个新当前子任务的第一条可见 commentary 固定以子代理名称、模型、思考程度、存活轮次和经验
  五行开头；运行时子代理与保留子代理执行同一合同，任务卡不得使用“继承父级”或“任务卡未提供”。
- 恢复 Sol `max` 的按任务选配；只有 Sol `ultra` 需要高价值复杂边界以及 `xhigh`、`max` 均不足
  的具体依据。Astra 仍最高 `xhigh`，并作为唯一的高成本专家路线。

## 5.2.2 - 2026-09-13

- 修复新项目和新会话中的委派回退：通用 `default`、`explorer`、`worker` 使用原生
  `spawn_agent` 显式传入模型与思考程度，并限制普通 UI、视觉与简单审计的 Ultra 使用。
- 子代理可见配置收敛为“专家子代理、模型、思考程度”三行，后接存活轮次与经验两行；移除逐次
  速度选择和声明。具名子代理可保留 TOML 的 `service_tier` 预配置，但它不属于 `spawn_agent`
  参数、`MODEL_ROUTE` 或公开回执。
- 增加旧 v6 保留子代理的两阶段 CAS 刷新：补齐 canonical global contract、移除旧可见速度行，
  同时保留子代理身份、固定服务层级、运行记录、原始经验与摘要，并在失败时回滚。
- 新项目开场只报告已完成的必要入口读取或初始化和紧接着的实际动作；禁止自行扩写用户未要求的
  任务分解、交付物、交叉复核、汇总文件、升版或发布动作。
- 新项目启动识别两个按需入口并只读取当前动作命中的入口，随后立即执行第一个实际动作；禁止
  开头通读两份入口、增加第二段插件前言，或用无关记忆、完整细则、委派和版本解释阻塞。
- 同一个运行时根因在首次失败和修复后只做一次能区分修复前后的复验；覆盖目标条件后停止，禁止
  因措辞同步或模型档位变化连续新建验收会话。

## 5.2.1 - 2026-09-13

- 修正 5.2.0 把两个功能串成顺序入口的回退：`$lean-stack` 可独立、直接判断和调用子代理，
  不等待 `$lean-simplify`；`$lean-simplify` 独立精简主任务，也不阻断符合三项原则的委派。
- 恢复两个技能各自的公开调用提示，并保留六行子代理开场、按真实改动与风险确定测试范围、
  确定性链接安全测试及已完成的合同测试精简。

## 5.2.0 - 2026-09-13

- 非平凡主任务默认先由 `$lean-simplify` 选择必要步骤和最窄验证，只有选中的步骤需要模型或
  子代理时才进入 `$lean-stack`；两个技能和插件表面共享同一条组合入口，避免只加载一半功能。
- 直接 `spawn_agent`、`followup_task` 和通用 `explorer`/`worker` 的任务卡也必须携带四行配置、
  存活轮次与经验状态，不能假定只有保留子代理才需要用户可见开场。
- 测试范围不再由仓库现有测试文件数、函数数、历史全套命令或发布标签决定；合同测试停止读取
  项目 `Jiao-Jie.md`，并移除 README、Mermaid 和交接布局的固定快照。链接逃逸默认测试改用
  确定性元数据，不再因 Windows 创建符号链接权限不足而跳过，运行时安全目标保持不变。

## 5.1.0 - 2026-09-13

- 同一长来源改为一个所有者完成发现和完整读取，其他子代理通过父侧 `SOURCE_ROUTE` 消费可定位
  证据包；只有具名缺口、来源变化、结果冲突或必要的高风险事实才定向独立复核。
- 保留子代理固定提示移除父代理拥有的模型路由、成本、生命周期和跨任务编排规则，只保留子代理
  自己要执行的职责；首条回复在四行配置后显示客观的存活轮次和经验状态，不要求判断经验适用性。
- SQLite v6 可把调用前核验过的经验版本摘要与后续明确成功/失败关联，并由 `recall` 显示反馈；
  旧调用保持兼容，v4/v5 只经显式迁移且不回填历史经验复用。

## 5.0.0 - 2026-09-13

- 按用户明确要求将发布版本从 4.3.3 提升为 5.0.0；本次只改变版本身份，不改变双功能归属、调用路线或运行行为。

## 4.3.3 - 2026-09-13

- 修正双功能归属：工具与 PowerShell 路径精简是 `$lean-simplify` 和 `$lean-stack` 共同遵守的执行底座；主任务侧用它收窄实际步骤与包装，子代理调用侧用它先判定直接工具路线。只有模型与子代理的调用选配归 `$lean-stack` 独占。

## 4.3.2 - 2026-09-13

- 一个插件内拆出两个并行、分别验收的功能：`$lean-simplify` 是主任务精简，负责最小完整任务方法、状态与沟通、调查与实现手册、测试/复核/重验范围、反过度工程、显式消融、发布安装和文档维护；`$lean-stack` 负责工具与 PowerShell、调用判断、四模型/成本/时间、上下文与结果复用、内部交流、迭代测试、子代理经验和生命周期。条件性语义复核与最终验证、失败后的最窄重验归前者；精确删除与否定重做归后者。
- 恢复早期最小完整任务阶梯，并禁止双入口拆分恢复历史上已经删除的大规模中间机制；两边继续保留当前授权、安全、权限、数据完整性、无功能回退和相称证据，正式消融仍只在用户明确要求时启动。
- 同一卷内只改变路径的移动不再生成迁移前后整树逐文件长度与 SHA-256 清单；实际字节复制、正式发布/安装清单、安全完整性和明确逐字节核验仍按声明范围验证。安装后子代理调用测试只核对真实启动事件、模型与思考程度，随后立即中断，不等待子代理完整做题，也不外推为业务质量证明。

## 4.3.1 - 2026-09-13

- 移除插件对项目交接文件存在、名称、创建、读取、更新和结构的全局要求；这些行为只由当前用户与项目自身规则决定。
- 删除 `project-handoff.md`，并清理技能入口、流程图、公开说明和合同测试中的交接生命周期依赖；四模型路由、`MODEL_ROUTE`、`WRITE_ROUTE` 及既有全局安全能力保持不变。

## 4.3.0 - 2026-09-12

- 两个或更多子代理将修改文件或共享状态时，父代理在首次可写派发前记录 `WRITE_ROUTE`，明确写入者、范围、共同热点、隔离路线、撤销依据和受影响检查；不能隔离或精确归因时退回单一直接写入者。
- 写入安全、用户否定、反 AI 过度工程、消融和条件性验证现在由插件全局合同统一承载；项目交接只保留项目特有状态、例外、证据、缺口与权威入口，不再复制完整规则章节。

## 4.2.0 - 2026-09-12

- 路由优先顺序统一为质量、成本、时间，资源成本不再重复计算墙钟时长；处于可接受成本带的少量低成本并行可以换取更短关键路径。父代理以 `MODEL_ROUTE` 记录四模型选择、质量底线、低成本替代路线及其差距。
- `gpt-6-astra` 只处理 GPT-5.6 存在决定性质量差距或能降低整项资源成本的当前专家切片；普通视觉任务不触发，也不要求低价模型先失败。派出的 Astra 仍最高为 `xhigh`。子代理第一条可见回复必须以本地化名称、模型、思考程度和速度四行开头，前面不出现计划或运行 ID，且不回显父代理专用 `run_id`。
- 完整保留子代理 TOML 不再设置 KiB 总大小上限；单项子代理说明、活动记忆、迁移计划和受管文件数量等逐项边界，以及所有权核验和真实 TOML 往返仍保留。规则收束为一个权威消费者、明确触发和可观察证据，避免规则增长却无法执行。

## 4.1.0 - 2026-09-11

- 派出的 `gpt-6-astra` 子代理思考程度上限从 `high` 放宽为 `xhigh`；原有较低档位继续有效，
  `max` 和 `ultra` 仍由保留子代理生成入口在任何文件写入前拒绝。
- 这项兼容性放宽不改变 Sol 父代理的专家协助触发条件，也不限制用户显式选择的 Astra 父任务
  配置；新会话探针仍以真实启动事件为准，命中后立即停止而不等待子代理业务结果。

## 4.0.0 - 2026-09-11

- 派出的 `gpt-6-astra` 子代理思考程度现在最高为 `high`；`xhigh`、`max` 和 `ultra` 配置会由
  保留子代理生成入口在任何文件写入前拒绝，避免运行任务继承父代理的更高思考档。
- Sol 父代理在具体专家难题下调用范围明确 Astra 子任务的能力保持；这项上限只限制派出的
  Astra 子代理，不擅自改动用户显式选择的 Astra 父任务配置或 GPT-5.6 子任务选配。
- 新会话路由探针达到“父代理已发出预期模型与思考程度的子代理启动事件”后立即停止，不再为
  验证派发决定等待子代理完成无关业务。

## 3.4.0 - 2026-09-11

- 合格低成本 GPT-5.6 切片现在依据维护后的模型价差和完整父子反事实路线直接取得正向总成本
  判断，并在昂贵父代理继续大块工作前尽早派发；确定性工具、依赖、冲突、权限和容量等内部
  硬性限制不再给父代理增加面向用户逐项解释的负担。
- “安装后新会话生效”现在必须由全新项目中的独立父任务真实派发并取得可采用结果来证明；
  静态关键词、当前会话手工加载候选文本或硬阻断说明不能替代行为验收。
- 中文说明、生成提示、测试和全局 `AGENTS.md` 已统一使用“规则”“范围”“程度”或具体限制；
  Python/JSON/SQLite/TOML 标识、英文 API、路径、命令与原始错误保持不变。

## 3.3.0 - 2026-09-10

- 新版本推送前同步更新 README、插件清单中的用户可见说明和本文件，并用版本一致性检查防止
  版本号已变化而说明仍停留在旧版。
- 插件说明现在直接概括当前路由的三项主要变化：直接处理决定只对当前任务形状有效、路线总
  成本包含父代理继续独立工作的 token/时间/返工，以及具体困难出现时及时调用范围明确的专家。

## 3.2.0 - 2026-09-10

- 防止昂贵父代理把一次“直接处理”决定沿用到整个长任务。
- 把父代理继续独立工作的 token、时间和返工也计入路线总成本。
- 遇到已经暴露出来、适合独立处理的困难时，及时调用有针对性的专家，而不是继续由父代理
  长时间试探。
